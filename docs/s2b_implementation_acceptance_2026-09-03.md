# S2-B有限实现验收

时间：2026-09-03 00:30（北京时间）。结论：`PASS`。实现提交推送确认后可以立即启动冻结
S2-B历史描述性比较。此时**没有真实S2-B训练结果或较晚评分**。

## 已实现的科学计算

- `slip_rate.py`只读519段原表的ID、简化线及两个总速率字段；保持共同385段、测地完整515段。
  缺测不填零，不丢地震目标，不自动给详细迹线关联属性，不改变原历史可用标记。
- 沿原投影边3.125 km固定中点、长度×原速率加权，高斯三尺度、真实格面积归一，远端尾部保留
  在float64对数空间。共同等权、地质、测地及完整等权、测地五层合计15个主空间场。
- 6.25 km只做一次无目标粗细诊断。主场及诊断一起保存到新S2-B检查点，未封口和已封口恢复
  都复用已有完整负载，不重复求积、不修改旧S1缓存。分块只控制内存，不改变候选或核支持。
- `slip_predict.py`只读复用旧目录、原更早内层OOF及有限选择规则；十新模型、396个起报—时限
  对，α=0保持原目录。各折/时限可恢复，不用外折结果重新选参数。
- `slip_score.py`先核验全部四折预测，再读取原开发目标；十新模型与四旧参考、14预定配对，
  原空期/锚点/面积/0与70 km规则不变。旧S1/S2-A源码与产物不改，输出不能进入任何`s2a_*`旧目录。

## 验收证据

主代理在项目venv与数值库单线程下运行四份聚焦测试：45项通过（11.93秒），Ruff通过，CLI
`--help`和`git diff --check`通过。检查涵盖有限直线高斯解析对照、线长/单位缩放、常速率等权
一致性、缺测支持、面积归一、远尾、十模型、更早标签可见性、α=0、396对检查点恢复、预测未齐
禁止评分及旧目录保护。全部为合成/配置核验，没有读取真实较晚目标或生成科学成绩。

独立数学/科学复核确认核心公式、共同与完整支持、选择时间边界和统一评分没有阻断问题。
00:19:48资源核验无相关Python后台，24物理/48逻辑核，两CPU13%/8%。真实运行默认2折线程、
数值库单线程、BelowNormal，至少留2物理核。

## 精确运行与科学复审

入口：`scripts/run_multitask_s2b.py --phase predict|score --project-root <活动工作树>
--data-root D:\AIPred\SeismoFlux\data --workers 2`；默认输出
`outputs/multitask_s2/s2b_slip_rate_v1`。设置PYTHONPATH为工作树src，使用根venv，所有数值库
线程为1。先predict完成四折，再score；后台隐藏窗口，启动前检查无重复实例。
当前协议SHA仍为`a57218c9fbedc21ae28a62de4980fc26b17e990aa32f3613b9ae95fc1db19d29`。

本次仍是必要使能，不是新的效果提升；工作直接服务于“相同报警面积下加入速率能否少漏震区”。
没有偏离总方案，不新增模型/平台/数据补齐；下一步立即计算并据实保留正负证据。
实际提交、PID、日志、检查点和下一安全动作写入`docs/restart_handoff_2026-09-03_s2b.md`。

## 00:36展示适配就绪（不构成新预测证据）

新增`render_multitask_s2b.py`复用已验证离线序列化和交互，保持14模型全部时限/震级/预算/容差
及空期。三组聚合图、诚实正反案例和本机HTML已用合成资料核验；主代理10项测试通过
（27.16秒，含Node 1,400组合）、Ruff通过，四张合成图已独立目视。未读取真实S2-B成绩或绘制
真实结果，也未改正在运行的预测/评分源码。待真实评分完成后直接渲染，不追加展示功能。
