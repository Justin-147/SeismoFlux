# S1-C0 目录模型开发筛查执行层科学验收（2026-09-02）

> 验收结论：**GO——允许提交、推送和远端回读；闭合后才运行真实四折预测。**
>
> 科学价值：`necessary_enabler`，不是预测效果证据。
>
> 最终独立审计：P0/P1/P2=`0/0/1`；P2仅为恶意并发改写文件的理论TOCTOU风险，不阻塞本地单用户科学运行。

## 1. 外行版结论

本阶段完成的是一套“先作答、密封答卷、再拿标准答案评分”的历史模拟考试程序。它会用过去能看到的
地震目录，分别预测未来地震更可能发生在哪里、未来一段时间大约发生多少次、震级如何分布，以及三者
合并后的表现。现在只证明这套考试不会偷看未来、不会临时换题或换面积，**还没有真正跑出模型成绩**。

因此，本次通过不等于模型已经预测得好。下一项真正的科学工作，是把已推送代码固定下来后运行四个
开发时间折，比较简单目录模型相对基线是否在同样报警面积下覆盖更多独立地震区域，并检查时间、震级
和联合预测是否改善。无论初筛结果正负，仍必须执行S1-C1局地目录完整度主检验。

## 2. 使用的数据与冻结样本

无评分真实预检只读取并核验了下列输入身份，没有构造外层目标或计算成绩：

- 权威地震目录：40,898条；起报只能使用当时已经发布且不晚于`T-24h`的记录；
- 中国大陆研究区：精确裁剪面积9,415,305.754432771 km²；
- 25 km等面积格网：15,697格，格网ID
  `3aacdbdda04fed652dd5ee3674906f674c127cb735dea5d5e989527b20809763`；
- 四个开发外折：2000–2004、2005–2009、2010–2014、2015–2019；
- 外层周起报账本：5,215行；互不重叠主暴露每折99个、合计396个；
- 更早内层选参暴露：4折×3时间块×5预测时长，共60组，主暴露合计1,180个；
- 预测时长严格分开：7/30/90/180/365天；震级档严格分开：M5–6和M6+；
- 主地点评价固定600,000 km²报警面积、严格同一格命中和30天/75 km固定首震锚点独立震序。

一次误把仓库根目录作为数据根目录的预检被程序按缺文件规则拒绝；改为实际数据根
`D:\AIPred\SeismoFlux\data`后完整通过。该失败没有读取外层目标，也没有生成成绩。

## 3. 将要公平比较的方法

### 地点

- `L0`：全国均匀背景；
- `L1`：固定原点500 km区域常率，1/5/10年收缩期由更早内层数据选择；
- `L2`：只用起报前可见目录的因果KDE，75/100/150/200/300 km带宽按one-SE规则选择；
- `L2_75`：固定75 km旧尺度对照；
- `L3`：长期背景与最近30天活动混合，权重0/0.25/0.5/0.75由更早内层选择。

地点输出是全国格网的**相对强度/排序**，不是绝对发震概率。评价时每个模型只能圈出相同实际面积。

### 时间、震级和联合

- `T0`：扩展历史Poisson常率；`T1`：允许过度离散的NB2；7天M6+只共享M5–6的离散参数及
  不确定性，自己的均值和方差仍来自M6+样本；
- `M0`：1970年以来M4+的截断Gutenberg–Richter分布；`M3`：1900年以来M5+长尾敏感性；
- `J0–J4`：把地点、时间、震级的预登记组合放在同一主起报上比较，M5+事件只计一次。

所有候选只用更早内层块选参；不同预测时长、报警面积和震级档不得为了增加样本而混在一起。

## 4. 封存与评分边界

1. `predict`阶段只能读取因果历史和更早内层标签，先写四折预测NPZ、逐折封印和四折总封印；
2. 预测区精确固定为`overall/prediction_phase`，其中不允许出现目标或成绩；
3. 每折NPZ的数组键、shape、dtype、数值不变量、文件大小和SHA-256均实际复核；选出的L1收缩期、
   L2带宽、L3权重和T1状态必须与NPZ逐折逐时长交叉一致；
4. 代码身份绑定已推送Git commit对象的标准对象编码SHA-256；脏工作树不能授权；
5. 四折总封印通过后，`score`才可先写评分授权，再首次从冻结输入内部构造外层目标；官方评分入口
   不接受调用者传入的目录、格网、起报或目标；
6. 成绩只写入并列的`overall/score_phase`，不得覆盖历史输出。

独立攻击复核已确认伪造源数据、伪造目标、错误根目录、空或任意选参、选参与NPZ不一致、文件篡改、
同一文件冒充四折、额外答案/成绩文件和伪授权均被拒绝。最终结论为`GO`，P0/P1=`0/0`。

## 5. 验证证据

- 主执行者在数值库单线程下复跑S1地点、时间/震级、合同、输入、预测、封存、指标、评分、摘要和
  运行器：**128 passed in 53.46s**；
- 独立攻击复核的封存、评分、运行和摘要定向集合：**50 passed**（与主集合有重叠，不相加冒充数量）；
- Ruff：通过；格式检查：15个文件已格式化；strict Mypy：8个源文件无问题；
  `git diff --check`：通过；
- 无评分真实输入预检：通过；
- 尚未读取或构造真实外层目标/成绩；holdout、2023+ audit和一次性locked test均未打开；
- `science_first`工作树既有15个Stage4未跟踪草稿未触碰。

关键文件SHA-256：

| 对象 | SHA-256 |
| --- | --- |
| S1-C0运行YAML | `454fa5775397dde69bff1fc8d6385fd2c75a058076934557660801d0ac89576d` |
| 真实输入加载 | `b5e7fe0e4206f3f5f398ff004a2bd070c4e2f8248783f88d80cb4e13fcc25889` |
| 预测核心 | `3efc4bc406ec69bbc9a81cf1b899ceb97af174070c6230aacb3d03a88307292b` |
| 预测封存 | `fdaa8d2b9d42f1a034a30c77e67b150f28239f829ec03b152cdec20392f29d72` |
| 授权评分 | `5392a2efed7a96377825584b138de253663fa652a40776df95e2f496b06f59be` |
| 科学摘要 | `78f18889fd22e60757f9188bf172a624e9d774f96d0ea46f74ad587367927931` |
| 两阶段运行器 | `6206b01f834929ee843e408147404bdcef423eb8f27b763385b96efec78399c2` |
| 命令行入口 | `4ddb2908efa654ce2bcc3a065512dd3b371a766700dfb23cefe31b1979f55700` |

## 6. 科学限制与价值复审

- 本验收没有地点召回、时间计数、震级对数分数、联合信息增益或模型排名；测试数量不能替代预测效果；
- S1-C0只使用全研究区M4+目录，属于快速方向筛查，不能选最终冠军或授权留出；
- 现有原始评分行不能机械完成固定原点500 km“去掉最大贡献地区”敏感性，因此摘要必须标为
  `not_evaluable`，不能看完成绩后临时用经纬度近似；
- S1-C1因果局地完整度无条件必做：局地Mc过高只排除该固定500 km训练单元，不缩小全国评分范围；
- 恶意进程在核验与读取之间并发改写文件是剩余P2理论风险；本地单用户运行下不值得继续扩张工程。

本阶段没有直接提高最终预测效果，但它使下一次真实历史模拟考试可信，属于必要使能。当前没有偏离
“受控报警面积下提高独立物理地震区域召回”的目标，也没有理由继续增加封存功能；应立即转入真实
四折科学筛查。

## 7. 机器可检索阶段决定

```yaml
science_value_category: necessary_enabler
evidence: "128项S1回归检验、独立攻击复核GO且P0/P1为0、真实无评分输入预检通过；尚无真实外层预测成绩"
decision: GO_COMMIT_PUSH_REMOTE_READBACK_THEN_RUN_PREDICT
next_scientific_test: "在已推送且干净的提交上运行四折S1-C0 predict，核验总封印后单独授权score，比较同面积独立震序召回及时间/震级/联合效果"
stop_condition: "任何P0/P1、输入身份、预测根、封印、代码身份或评分授权失败立即停止；真实结果无稳定增量则停止对应模型方向，不用工程扩张追分"
```

## 8. 下一步

提交、推送并远端回读本执行层；随后严格按`predict → 四折封印核验 → score`运行。第一批成绩一形成，
立即制作静态图和离线交互回放，报告各折训练与检验表现、代表性命中和失败震例，并从科学效果而非
代码数量决定哪些方向保留。S1-C0完成后进入S1-C1，不打开留出、audit或locked test。
