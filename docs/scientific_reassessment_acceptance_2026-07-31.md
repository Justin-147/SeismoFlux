# S0 全数据与科学路线复审验收（2026-07-31）

## 验收结论

- 阶段：`S0_scientific_reassessment`
- 状态：`accepted`
- 预测模型训练：未启动
- 新真实预测成绩：无
- 锁定测试：未运行
- 下一阶段：`D1-0_executable_scientific_contract`

本阶段通过的是“数据是否完整、现有真实证据是否被正确解释、新路线是否能直接进入最近一次科学
检验”，不是模型效果门。

## 完成的工作

1. 重新盘点唯一蓝图规定的全部真实输入；
2. 核对每类数据的物理位置、文件数、字节数、哈希、时间范围、字段、质量和实际使用状态；
3. 排除合成数据、代码测试和流程验收，只汇总真实训练/验证/测试成绩；
4. 复审旧路线对小样本地震研究是否过严或偏离最终目标；
5. 重设“历史开发回放 + 真正前瞻确认”的并行科学路线；
6. 冻结 D1 的科学问题、六个模型、一级指标和 D1-0 前置合同；
7. 生成静态总览和完全离线交互浏览器；
8. 更新唯一蓝图、项目约束和重启交接。

## 数据验收

| 检查 | 结果 |
| --- | --- |
| 原始输入类别 | 7 类 |
| 原始文件 | 216 |
| 总字节 | 43,610,994 |
| 全文件 SHA-256 复核 | 216/216 与清单一致，0 缺失、0 漂移 |
| Stage 1 标准表 | 11 个 |
| 去重地震 | 40,898 |
| 研究区内地震 | 29,637 |
| 异常报告/观测 | 205 / 59,904 |
| 异常状态/特征行 | 166,189 / 3,217,885 |
| 断层段/点/真实迹线 | 519 / 7,339 / 7,216 |
| 蓝图规定但遗漏的原始输入 | 0 |

原始逐行数据没有加入 Git；公开文档只包含聚合计数、方法和许可安全的证据。

## 真实效果边界验收

唯一合法真实正证据是 75 km 空间 KDE 相对均匀背景的信息增益：

- 开发折：`+0.53430 / +0.59124 / +0.57680 / +0.61139 nats/事件`；
- 最终验证：`+0.40215 nats/事件`；
- 2,000 次逐事件有放回 Bootstrap 条件 95% 区间：
  `[+0.23564,+0.56286]`；
- 最终 96 个目标是支持域内 `M>=4` 事件，不是 96 个 M5–6 独立震群；
- 同期实际只有 8 个 M5–6 和 1 个 M6+；
- 尚无 600,000 平方公里固定面积召回成绩。

以下边界全部写清：

- ETAS 为 `not_evaluable`，不是预测效果为负；
- Stage 2S 在形成预测前失败，没有正负科学结论；
- Stage 3 使用全部真实异常但只生成特征，没有训练或评分；
- Stage 4A 没有读取真实目标或形成异常增量成绩；
- Stage 2P-1B 是纯合成已知答案检查；
- 断层、长期危险性、复杂模型、锁定测试和真正前瞻均没有真实成绩。

## D1 科学可执行性验收

独立复核最初发现六项 P1：基线目标震级易被误读、时间折未闭合、样本不足却特征过多、模型公式
不唯一、统计判定过软、新前瞻授权不清。全部已经在接受版本中收口：

1. 明确 KDE 的 96 个目标为支持域 M≥4；
2. 冻结三个 `Asia/Shanghai` 起报区间、90 天成熟/隔离和跨折唯一震群；
3. 先做 D1-0 样本水位表，最多只用 10 个预定义异常组分数；
4. 用拟合期未来 M4+ 训练空间修正，再在 M5–6 独立震群上评价迁移；
5. 冻结 B0 因果重建、R30 凸混合、`α` 和 ridge 候选、Poisson 偏置及归一；
6. 冻结 2,000 次震群 Bootstrap、各 200 次置乱、根种子、面积档和并列规则；
7. 以原始多命中震群数为主要效果量，不再设置无法分辨的 2 个百分点门；
8. P1 并行只允许目标盲准备，真实 issue 必须先完成新协议验收、提交和推送。

第二轮独立只读复核结论为 P0/P1 = `0/0`。39 区确认来自目标无关静态地块线，不涉及
2026-07-13 危险性数据倒灌。

## 轻量验证

由于本阶段只修改科学文档和展示，没有运行与改动无关的全量代码测试，也没有在系统 CPU 约 55%
时启动长时间模型计算。

已完成：

- 数据清单、11 个标准表行数、KDE 信息增益区间和 72/783 异常特征数字的机器复核；
- `git diff --check`；
- 本阶段八个预定文件均为有效 UTF-8 且无行尾空白；
- 静态 SVG 可被 XML 解析，画布为 1400×900；
- 离线 HTML 的 JavaScript 通过 Node 语法检查；
- 会话内交互片段为纯 HTML fragment，10,966 字节，JavaScript 语法通过；
- 未编辑、暂存或删除 15 个既有未跟踪 Stage 4 草稿；
- 未修改仓库根工作树中的不相关 ETAS 改动。

本阶段图件用于说明数据和证据边界，并醒目标注“尚无新的真实预测成绩”。D1 形成真实成绩后，
地图、曲线和交互回放必须再做逐项视觉复核。

## 交付物

- `docs/ALL_DATA_SCIENTIFIC_INVENTORY_2026-07-31.md`
- `docs/SCIENTIFIC_REASSESSMENT_2026-07-31.md`
- `docs/restart_handoff_2026-07-31_scientific_reassessment.md`
- `docs/scientific_reassessment_overview.svg`
- `docs/scientific_reassessment_explorer.html`
- `SEISMOFLUX_IMPLEMENTATION_HANDOFF.md` 当前执行条款 1.1
- `AGENTS.md` 科学优先和固定汇报规则

## 科学价值复审

- `science_value_category`: `necessary_enabler`
- `direct_prediction_improvement`: `none`
- `evidence`: 全部数据和真实成绩边界已经核对，旧路线的科学阻断已经转化为一个可执行、低维、
  小样本诚实的 D1-0/D1 设计
- `uncertainty_change`: 没有降低异常或近期地震的真实效果不确定性；降低了数据遗漏、证据误读和
  下一实验不可解释的风险
- `decision`: `accept_s0_commit_push_then_enter_d1_0`
- `next_scientific_test`: D1-0 冻结精确样本水位、特征公式、模型和统计，然后才运行 D1 真实回放
- `stop_condition`: D1 所有增量平均无正方向或与置乱无区别时，停止对应组件复杂化，保留 75 km KDE
