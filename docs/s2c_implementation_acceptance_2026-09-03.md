# S2-C独立应变比较：计算前验收

时间：2026-09-03 02:39（北京时间）。状态：`PASS_IMPLEMENTATION_PENDING_COMMIT_PUSH`。

## 科学问题与数据

在同一批历史地震、同一报警面积下，GSRM独立应变信息能否补充已保存的目录多尺度模型？
只比较四个登记模型：变形区同支撑等密度、应变强度，以及二者各自和目录的混合。
原GSRM均值单元、两份Ms目录、25km研究格、四折/五时限/两震级档与全部空期不变。
没有全国95%或80%完整性否决门，没有震级换算、类型空缺删事件、Mc掩膜或人工底噪。

## 已核对什么

- 原单元以相交面积累计UNIT和面积×官方第二不变量累计STRAIN，再全国归一；没有用震中生成格网。
- 真零保留，零区发生地震如实计负无穷对数分数，不删事件、不删验证块；空块单独标记。
- 内层更早I2/I3成熟标签选择五个有限alpha，外折只读复用原目录场。纯应变层没有拟合参数。
- 原对数密度排序、零值并列规则、整格前缀和实际面积保持一致；alpha=0的评分与旧目录算法相同。
- 四折20个预测检查点、396起报—时限对全保存并验证后才允许打开开发目标，留出和锁定测试不可读。
- 四折总结果及预先登记2015—2019分列各保留四新模型/四参考、六配对；真实主分母分别核对147/32。
- 续算复用已存检查点且不改旧目录；新结果全部是开发研究，不能称为独立前瞻或时间/震级概率提升。

02:37聚焦合成检查42项通过（24.63秒），Ruff检查、差异检查通过。包括面积归一、剪切分量、
真零与极小正值排序、内层因果选择、20断点/396对、评分前目标访问限制和后期分列。
独立科学复审完整检查三个新模块，结论无影响科学结果或阻止实际计算的实质问题。
这些检查只证明本轮比较可以按方案执行，**尚无新的真实训练或预测效果**。

## 下一步及停止边界

协议`6603a6cdc15cc0be7f1fe0ec16396a2ed872739e`已于02:21:36远端确认。本实现提交推送后，
直接运行`python scripts/run_multitask_s2c.py --phase predict --project-root <活动树> --data-root <数据根> --workers 2`。
使用项目`.venv`，先设`PYTHONPATH=<活动树>/src`及数值库单线程；后台Hidden、BelowNormal。
预测全完成后用同入口`--phase verify`，再`--phase score`，不得反向用评分调整模型。

输出为`outputs/multitask_s2/s2c_strain_v1`；未完成运行依赖原检查点恢复，不能覆盖不同身份内容。
开始前先查重复实例；本机24物理/48逻辑核，默认2、最多3折线程、至少保留2物理核心。
02:37:40无本项目Python后台；CPU瞬时读数11%/85%波动，后续以实际进程占用和新采样为准，
不把其他软件占用误报为本项目任务。不会启动多份模型任务。

当前价值为真实新数据检验的必要准备，不是增益。实现没有另建通用平台或扩展模型搜索。
下一证据必须是同样本同面积下新增/丢失的独立地震区域；无增量也结束应变分支并转S3异常，
合理任务专用小增益留给S5组合验证。结果完成后补全静态图、离线回放、科学复审和交接。

只新增S2-C文件，不改S1/S2-A/S2-B或P1，不读未来目录、2020—2022留出、2023+审计，
不触碰science_first的Stage4草稿。原源数据、逐事件数组、案例及交互页仅本机，按白名单同步。
最新提交、进程和检查点以`docs/restart_handoff_2026-09-03_s2c.md`后续追加记录为准。
