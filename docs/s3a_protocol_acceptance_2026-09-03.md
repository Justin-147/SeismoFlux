# S3-A评分前最小协议验收

时间：2026-09-03 03:57，北京时间。结论：**协议及纯数学检查通过，待本提交推送回读后开放本轮开发输入。**
这不是真实模型训练验收，更不是预测效果改善。S3-A真实目标尚未读取，外层分数尚未计算。

## 已确定的科学内容

- 只用既有两本地Ms目录的长期因果背景及两A开发折内的异常报告，不混入后期审计、人工预测或事后字段。
- COV5/SNAP16/DYN20严格嵌套，报告覆盖与异常内容分开；动态相比快照只增加三变化量和其缺失控制。
- 地点用已实现的条件空间ridge；时间用含空窗的Ms5+ offset-Poisson，保留仅截距校准参照。
- 以更早成熟标签训练，两正式震级、五时限沿用S0；长窗口无样本记NA，不设置95%/80%全国否决。
- 时间与空间各200复本；时间仅离线反事实、开发范围内按实际拟合/验证起报截止分池，不进入真实预测。
- 当前尺度200km只是有限起点，S3-B其他尺度/学科消融及S4—S7尚未完成。

## 必要检查与独立复审

`tests/test_multitask_s3_models.py`与`tests/test_multitask_s3_protocol.py`合计29项纯合成/配置检查
于03:56通过（1.07秒），未调用任何真实目标读取器。新模块及这两测试Ruff检查通过，`git diff --check`通过。
独立数学审查未发现阻断协议的实质问题；计数损失梯度、完整h天单位、空窗、未惩罚截距、
解析率校准、训练内预处理和全零回退均与方案一致。

补充保留`predict_log_mean()`，极小期望次数即便浮点下溢，也可使用有限log均值评分，
不添加底噪、不改训练参数。测试通过只证明这些计算关系，不证明异常有效。

## 科学价值复审与接续

1. 属必要使能：将报告信号拆成地点与时间两个可检验增量，尚无新增召回或时间评分。
2. 未偏离多数据/三任务路线；本轮不学震级比例，对震级改善不作过度声明。
3. 未重建数据平台、未扩展目录/静态搜索；下面直接接薄输入和真实训练，不继续堆协议。
4. 当前继续；结果若没有增量则结束此变体，小幅任务专用线索可保留且报告不确定性。

03:55左右实际资源检查：无Python/pythonw后台进程，整机CPU约11%；没有重复训练任务。
此前无Python不代表卡死，本轮正在准备新的科学试验。心跳继续每30分钟，进入新输入前再次查重复。

提交白名单仅本协议、方法/交接/验收、新数学模块、纯合成测试与蓝图增补；不含原始数据、事件数组或案例图。
旧S1/S2结果、P1、root旧工作树和science_first Stage4未跟踪草稿均未修改。
