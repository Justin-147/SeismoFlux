# 最新交接：S2-B断层速率增量

更新时间：2026-09-03 01:36（北京时间）。状态：`S2B_CLOSED_S2_NEXT_INPUT_REVIEW`。
本文件接替`docs/restart_handoff_2026-09-02_s2a.md`。旧结果不覆盖；按本文件最后的更新记录恢复。

## 1. 一句话状态

项目没有全部完成。目录模型已经取得一些地点预测小增益，单纯加入断层图未普遍改善；现在开始
检验“断层活动速率”这一尚未用过的数据。输入核对、独立科学复审及配置必要检查已完成并推送；
速率空间层与四折396个起报—时限对已于00:43:53保存，20/20预测检查点核验通过；
四折评分与最终汇总于01:14:14完成，评分进程已退出；六产物及身份核验通过。主条件目录58/147，
完整覆盖等权断层混合60/147，测地混合59/147；最好的主成绩来自几何层，不是速率加权。
**有小幅、任务专用的历史开发线索，尚未证明真实未来提升；最终图件、科学验收及公开同步均完成。**
科学提交`1c2d5009a1a02805da3b5debc954875a9160cb35`于01:35:24远端回读一致。

最终目标仍是充分利用已有及可合法获取的数据，提升未来地震时间、地点和震级预测；本轮回答
其中的地点问题：同样大的报警范围内，能否多覆盖独立地震区域。

## 2. 已完成内容与不再重跑的内容

- S1已闭合：科学提交`544360e34b139581508deba5c62d14bccf46d535`，闭合
  `7164e3b8cd2b8af939f18e40337d6c89ac476295`均已推送。主条件30天、Ms[5,6)、60万km²，
  原目录方法54/147个首锚点，多尺度58/147，年龄加权57/147。是开发期小增益，不是最终确认。
- S2-A已闭合：科学提交`f2f1bf48e8cdf036a3f9b664430f26ddf48cd22c`，闭合
  `543678824bdb233a78aaa0d3930fe59deb196ed6`均已推送。主条件目录/简化断层混合/详细迹线混合
  都是58/147；简化混合30天45万km²为49→52、90天60万km²为76→79，但大震部分条件退步。
  纯几何和详细图不继续扩搜。原图、正反案例、离线回放与科学验收全部完成。
- S2-A使用两用户Ms目录、519简化段和7,215条迹线；内层更早历史选尺度/权重，四折预测保存后
  才评价。静态地图为2026快照，只能作历史描述性研究；不能说历史起报时已具备这些地图。
- 95%与Ms审计已完成，详见`docs/s1_support_threshold_magnitude_audit_2026-09-02.md`。
  95%未改旧成绩，但挡住C1在预测前继续；已取消全国比例一票否决，不是机械换成80%。随后C2A
  实际固定参数对照主成绩54→52/53，无稳定收益，分支已结束。两用户目录原数字默认Ms，
  旧计算未因类型缺失删事件或换算，因此不需全面重跑；该说明不覆盖ComCat等外部目录。
- 不重启旧S1/S2-A进程，不重新评分或渲染已验收产物，不再围绕95%/80%、Ms或旧ETAS反复审计。

## 3. 当前新数据和方法

方法唯一入口：`docs/s2b_slip_rate_methods_2026-09-03.md`；配置：
`configs/multitask_s2_b_slip_rate.yaml`。已读国内滑速/GPS、国际断层/目录混合原始研究，清楚
区分长期物理信息与短期预测，未自行假装拥有地震矩率所需的完整物理参数。

519简化段内385段有地质总速率、515段有测地总速率；共同385段，测地多130段，两者均缺4段。
单位mm/a，总速率直接采用原表。不以缺测代替零活动，也不删除缺资料地区的地震目标。
不自动把属性关联给详细迹线；不将上次强震年、2026离逝率、复发周期或综合危险性混入本轮。

五层：共同集合的等权几何/地质速率/测地速率，测地完整集合的等权几何/测地速率。
每层单独预测和混合已保存目录，各一个模型，共十个。二维高斯沿原断层线积分，长度×速率作权，
再按真实格面积归一；不是绝对概率。三个尺度25/75/150 km、每混合13唯一候选，14组预定配对。
主求积步长3.125 km，仅一次6.25 km目标盲数值对照，不增科学选参项或全国比例门槛。

原四折、五时限、两震级、五面积、0 km主/70 km辅助、所有目标/空期不变。新参数只看外折前成熟
的I2/I3；I2目录按原规则K75回退。既有目录及组件缓存只读复用。当前静态快照的历史描述性限制
保留；不能将这轮说成时间/震级概率或真正未来提升。

## 4. 最高优先级安全下一步

1. 本轮预测与评分均已完成，不再运行predict或score，不重做静态积分、内层选参或展示程序。
   六产物及身份已核验，旧S1/S2-A产物只读，协议与源码身份不变。
2. 真实最终图与离线回放已经完成且通过逐项/目视核验，不重新开发、评分或渲染。
3. 本轮公开白名单已提交推送闭合，不再重复同步原科学结果或用“等待闭合”拖延下一资料。
4. 直接进入现成危险性/应变的有限输入与经典方法核对，不追加速率搜索。S5保留等权
   几何与地质长时限小面积线索，测地主任务小收益留档，不把速率当成通用必需模块。

## 5. 工作位置与资源

唯一活动工作树：`D:\AIPred\SeismoFlux\data\interim\worktrees\p2r_multitask_multidata`；
分支：`codex/p2r-multitask-multidata`；数据根：`D:\AIPred\SeismoFlux\data`；
Python：`D:\AIPred\SeismoFlux\.venv\Scripts\python.exe`；`PYTHONPATH=<活动工作树>\src`。

所有数值库设单线程，`PYTHONDONTWRITEBYTECODE=1`、`PYTHONIOENCODING=utf-8`。
计算默认2折、最多3折、BelowNormal，至少留2物理核；后台必须隐藏窗口，先查唯一实例再启动。
2026-09-03 00:04:19核验：无SeismoFlux相关Python后台，24物理/48逻辑核，两CPU34%/2%，
整机约18%；这是检查时快照，不可当下一次的实时值。

原始速率表SHA-256：`8f37d08f2427aab78f45dc9ae32130582706eb8c5d58d4d9b1e8b18d719f8627`。
原S2-A配置SHA-256：`d6e19dca67063030e8eafdfd766f13f2310a5e52790cc4b2b7fe8707cf58b5c9`。
旧C2B/S2-A有效运行位置、身份和成绩详见各自交接；不用重新计算来“确认还在”。

不读2020—2022留出、2023+审计或锁定测试，不下载未来目录，不改冻结P1、根工作树或
`science_first`的Stage4未跟踪草稿。原始数组、案例地图、含事件HTML仅本机；公开只包含代码、
文档、聚合成绩及无事件位置的聚合图。禁止`git add -A`；保留所有既有未跟踪结果。

## 6. 完整工作线、心跳与本次科学复审

|阶段|工作|当前状态|
|---|---|---|
|S0|数据清单、任务和样本|完成|
|S1|目录模型与输入比较|完成，保留小幅任务专用收益|
|S2|断层、速率、危险性、应变|几何和速率阶段闭合；危险性/应变输入及经典方法核对开始|
|S3|动态异常的历史重建与贡献|待开展|
|S4|有限树模型、小型神经与多任务模型|待开展|
|S5|多数据组合与消融|待开展|
|S6|稳健性、综合展示、论文/PPT/可编辑poster|待开展，阶段图件持续交付|
|S7|外部检验|并行，不阻塞历史研究|

心跳`seismoflux`每30分钟保持ACTIVE。用户要求每次通俗汇报已实现/正开展/尚缺什么，明确训练与
较晚评价、使用与未用数据，不用工程指标冒充科学效果。每次复审是否实质推进目标、是否偏题、
是否陷入无益工程、是否应纠偏或停止，并更新本地交接。

本次已经**得到新数据的任务专用历史开发线索**，不能声称普遍或真正未来增益。方向未偏离多数据目标；
不纠结补齐所有物理参数，不重跑已关闭环节。下一项真正证据是同面积同目标下加入速率多命中或
多漏多少独立震区。整个项目离完成仍缺S2后续、多数据组合以及独立验证，不能给出已完成百分比。

## 7. 00:08协议验收与同步状态

协议SHA-256：`a57218c9fbedc21ae28a62de4980fc26b17e990aa32f3613b9ae95fc1db19d29`。
九项必要配置检查通过（0.25秒）、Ruff通过、独立科学复审无阻断。已纠正汇总行数漏写容差轴的
文字说明：14模型×5时限×2震级×5预算×2容差=1,400行，不是1,400次独立检验。
没有读取较晚目标或运行任何S2-B模型。当前验收允许同步协议后进入最小实现，不允许跳过实现
验收直接启动真实运行；提交和远端确认完成后在下方追加唯一恢复记录。

### 00:10:35远端确认与唯一下一动作

协议提交`176e1c9b8e2c7a1f0d806a85f450f2c469793799`已推送至
`origin/codex/p2r-multitask-multidata`，00:10:35远端回读一致。方法/配置/必要检查已闭合，
**下次直接进入薄实现，不再研究或扩充同一协议，不重新审计95%/Ms/属性单位**。
仅需要新增速率空间层与复用目录的预测/评分入口；必要合成核验和验收推送后立即运行有限比较。
目前没有实现中的代理、S2-B后台进程或真实预测检查点；四折较晚评价仍未启动。
每30分钟心跳继续，恢复入口已改为本文件及蓝图第1.16节；整个项目尚未完成。

## 8. 00:23实现中，尚无真实计算

协议闭合记录`18da61ece2c3962f0f3e8b58ec5892dffb288b01`已于00:18:26推送并远端确认。
00:19:48续接核验无项目Python进程，两CPU13%/8%，整机约10.5%；没有重复任务。

新增实现分为`src/seismoflux/multitask_s2/slip_rate.py`的五层空间权重，以及
`slip_predict.py`、`slip_score.py`、`scripts/run_multitask_s2b.py`的有限预测/评价接线。
旧S1/S2-A模块不修改。当前文件可能仍未齐或测试未完成，不能因出现脚本就启动；以随后实现验收
及提交记录为准。静态主层与一次粗细诊断将保存在S2-B新运行目录，恢复直接复用，不回写旧目录缓存。
合成核验只检验公式与连接是否忠实于冻结方案，不算科学成绩。完成必要验收推送后立即实际计算，
不再进行同一协议的资料审计、文献扩张或参数调整。

## 9. 00:30实现验收，同步后立即运行

实现完整，验收见`docs/s2b_implementation_acceptance_2026-09-03.md`：主代理45项聚焦合成/
配置核验通过（11.93秒）、Ruff通过、独立科学复核无阻断。没有改变冻结协议、旧源码、旧预测
或目标，未读取真实外折目标。新脚本已经存在并通过入口检查。

工作树中的`python scripts/run_multitask_s2b.py --phase predict --project-root <活动工作树>
--data-root D:\AIPred\SeismoFlux\data --workers 2`将写入默认新run
`outputs/multitask_s2/s2b_slip_rate_v1`；score用同入口的`--phase score`，必须四折预测完成后执行。
首次会先生成主空间层和一次粗细诊断，再保存20个折×时限预测检查点（总396对）。恢复同一
run时先核对唯一进程；主层和已完成预测/评分检查点复用，禁止改协议或另起重复实验。

当前尚无真实S2-B进程/检查点；同步确认及实际启动后在下方更新。最终图/离线回放适配正在用
合成数据准备，不读真实新成绩、不触碰旧图；这项展示工作不影响预测身份，也不是科学提升。

## 10. 00:32:44已真实启动，优先检查同一进程

实现提交`c51c788140857069634241dae3b5c751e7baf9c5`已于00:31:57推送并远端回读一致。
启动前确认无SeismoFlux相关Python；以隐藏窗口启动唯一PID **34788**，BelowNormal，2折线程、
数值库单线程，默认输出`outputs/multitask_s2/s2b_slip_rate_v1`。旧产物和源码不改。

日志：`outputs/multitask_s2/s2b_slip_rate_v1/logs/predict_20260903_003244.stdout.log`及同名stderr。
00:33:04进程活跃，CPU累计19.95秒、内存约223 MiB、BelowNormal；`run_identity.json`与run锁
于00:32:46落盘，stderr为空，首个空间层/预测检查点尚未出现。此时为输入验证/读入，不是等待
未来地震，也不能假称已训练完成。启动快照两CPU3%/7%，整机约5%。

续接优先查看这个PID是否仍对应S2-B脚本、最新日志与检查点。活跃时不另起实例；若退出且没有
完整prediction_manifest，先读错误再决定从同目录安全恢复，不能改模型/输入或另换run躲避。
空间层保存后将记录15个主层和一次粗细诊断；四折20个时限检查点、396对完成后才允许score。
不要在全预测保存前读取外折目标或声称较晚效果。当前展示适配独立进行，只用合成数据。

### 00:36—00:37最新检查与展示准备

输入读入已完成，stdout记录00:33:24进入`s2b_static_surfaces_started`：五层、3.125 km主求积、
一次无目标诊断。00:36:09 PID34788仍活跃、CPU累计203.8秒、内存约267 MiB、BelowNormal，
stderr为空；空间层检查点和20个折×时限预测检查点尚未落盘。界面暂不变化是整组静态积分尚未
结束，而非进程停止；不增加新实例或边运行边改公式。

展示适配`scripts/render_multitask_s2b.py`和对应测试已完成。主代理10项纯合成核验通过
（27.16秒），包含Node 1,400种切换组合；Ruff通过，独立逐张检查四张合成图无截断/重叠。
**这只是显示程序准备，不是新的模型成绩或真实最终图件**。真实summary完成后才运行：
`python scripts/render_multitask_s2b.py --output-root outputs/multitask_s2/s2b_slip_rate_v1`。
输出默认`rendered`，前三组6个聚合PNG/SVG可公开；案例图与`seismoflux_s2b_slip_rate_replay.html`
仅本机。五个混合模型中按主条件召回及固定顺序选展示候选，同时展示增/失或明确回退，不制造好例。

下一步只有：等同一静态/预测进程完成并验齐四折，然后统一评分、真实绘图和科学复审。不要再开发
renderer、重复协议核验或为界面暂不变化增加无关工程。异常、危险性/应变等仍未纳入这一速率轮。

### 00:40心跳收尾快照

展示与启动交接提交`95e65150f0199dc5e9d1c82124febbe28e341e80`已于00:38:29推送并远端确认。
00:39:02空间层manifest和预测manifest均未生成，预测检查点0/20，stderr仍为空。
00:39:47系统核验只有一个相关Python（PID34788，命令为S2-B predict），CPU累计420.52秒、
内存253.2 MiB、BelowNormal；两CPU14%/4%，整机约9%。CPU累计持续增加，正在完成固定积分，
不是卡死或等待授权。不能由耗时臆测完成百分比，也不能重启另一个实例来“加速”。

本轮交付了必要实现和显示准备，并已经真实计算；仍无新的预测效果证据。科学方向未偏离，
没有新增长期工程任务，冻结方案不调参。下一心跳直接检查同run，完成预测就统一评分，随后真实
图件/回放、贡献复审和验收。核心源码已冻结在运行身份中，不为监控加功能或修改运行中的程序。
心跳继续ACTIVE，未运行留出/锁定测试，冻结P1、根工作树及science_first的Stage4草稿均未触碰。

## 11. 01:03预测完成，统一评分已启动

00:41:21静态15场及一次粗细诊断完成；00:43:53.855225全部四折预测保存。每折99个起报—时限对，
总396对、20/20时限检查点。主代理调用冻结`verify_prediction_manifest`完整核验通过：
预测manifest SHA-256为`461878b164816bf1f5cb9d3daf079f2f151d197ff05295c385103d29edc7b4a0`，
`outer_targets_read=false`。预测PID34788已正常退出；不要重复预测。

00:56:16系统核验无任何SeismoFlux Python进程，CPU0 11%、CPU1 1%，整机约6%。启动前再次核对
无重复实例，于**01:02:34启动评分PID36892**，隐藏窗口、BelowNormal、数值库单线程。
日志：`outputs/multitask_s2/s2b_slip_rate_v1/logs/score_20260903_010233.stdout.log`及同名stderr。
01:03:10评分仍活跃、stderr为空，折评分0/4、summary尚不存在，此时正在读取与核验已保存预测。
这些是当时快照，续接必须查实时状态；评分入口保持同一run，不重新训练或读取留出。

### 更早训练信息（不是较晚评价）

独立代理只读20份已保存选择记录及静态审计，没有读取任何S2-B外层评分。五层混合仅选择α=0或
0.25；五种纯物理层在20组内层比较全部低于目录。共同等权/共同地质/共同测地/完整等权/完整测地
混合的内层优于目录组数分别16/11/14/18/16，其余为α=0平局；平均内层Δlog密度分别
0.02571/0.00952/0.01892/0.02824/0.02916。候选包含α=0，所以内层“不差于目录”不能当新证据。
20组并非独立重复；其I2/I3验证块均非空，每组103—1132个Ms4+标签。30天四折内层标签为
329/409/593/662；I2目录20次按既定规则K75回退，不能误称新速率层无样本回退。

静态3.125与6.25km的15场总变差为2.048e-6—7.411e-5；75个面积对照中67个报警格完全相同，
8个仅交换一个格。没有需要追加数值优化的证据，生产保持冻结的3.125km主步长。

下一项唯一科学动作是从已冻结的较晚预测中核对独立震区召回及新增/丢失；评分完成即真实渲染、
科学价值复审与验收。尚未使用异常、危险性/应变等数据，不因这一小轮已算完预测就说全项目完成。
心跳仍每30分钟ACTIVE；95%/Ms审计与C2A敏感性已闭合，无需全面重跑，也不再重复审计。

### 01:08心跳交接快照及科学复审

01:08:26系统确认仍只有评分PID36892一个相关Python；CPU累计351.41秒持续增长、内存1591.0MiB、
BelowNormal。24物理/48逻辑核，两CPU负载10%/9%，整机约9.5%；数值库保持单线程，不占满核心。
`C_DEV_2000_2004/fold_score_manifest.json`于01:05:55落盘，评分1/4折（25%，仅指评分折数），
其余仍在同一进程计算；最终summary和score_manifest尚无，stderr为空。预测早已20/20、396/396
完成，不能把评分25%解释成项目总进度，也不能因stdout暂时无新增行称为卡死。

四点复审：①已经完成真实新数据预测并进入较晚检验，属于直接检验科学假设，增益尚未知；
②保持S2多数据路线，未偏离最终时间/地点/震级目标，但本轮只回答地点；③不追加数值、展示或
监控工程，已验证足够的静态求积不再加密；④先完成14组既定比较再决定保留/停止速率变体，
不看一折就修改模型。下一心跳按同run检查评分，完成即真实图件/离线回放、科学复审及验收。

现有心跳`seismoflux`已更新为ACTIVE、每30分钟，续接入口指向本文件与当前评分，不再指向旧
预测PID。本次只更新进度文档；科学阶段尚未收尾，不提前宣称最终验收或重新提交旧科学结果。
未触碰根工作树、science_first的Stage4草稿或冻结P1，未运行留出/锁定测试。

## 12. 01:23统一结果已经完成，不得重复评分

四折评分检查点依次于01:05:55、01:08:34、01:11:10、01:13:46保存；最终summary于01:14:14.753、
score_manifest于01:14:14.874保存。01:20:12检查PID36892已退出，stdout正常返回summary路径、
stderr为空。预测100%、评分100%，但最终图件/验收/公开同步尚未闭合。

主代理核验六产物哈希、协议、预测清单、旧目录评分清单和三个评分源码身份一致。summary含
1,400曲线与1,400预定配对条件，396起报—时限对、792震级分组起报，主锚点147；留出/锁定/新的
独立测试标记均为false。score_manifest SHA为
`9ea286a13915d5fe55d908e9fb34af6d2ca10e1e3ffa54c24cba07c982ce5111`；summary SHA为
`81647652ce3e6965438497449177e56bcb66ea030c19069790a20df883962f36`。

独立科学复审确认无超预算或分母/增失算术矛盾。主30天Ms5–6、60万km²：完整等权混合58→60/147，
新增4丢失2、四折+2/+1/0/−1；共同测地混合59，比同支持等权58多1；完整测地59却比完整等权60
少1。地质混合主任务回到纯目录。不能把主成绩最好的一项归功于速率，也不能忽略纯层远弱于目录。

任务局部线索：365天Ms5–6、30万km²地质混合48→53/229，相比共同等权50仍多3；365天Ms≥6、
45万km²共同等权几何比目录多5/45。负结果：180天Ms5–6、60万km²完整测地比目录少7、比等权少5。
这些是相关开发条件，不是独立确认。完整分析与最终保留决定将落入本轮科学结果文档。

当前正在现成展示程序上生成真实聚合图、诚实正反案例和本机离线回放，不再开发功能或调整参数。
阶段闭合后停止速率扩搜，保留几何与地质长时限的有限线索供S5；下一项为现成危险性/应变的
有界数据与经典方法核对，再开展S3异常。整个项目仍未完成，心跳继续。

## 13. 01:31本轮科学验收完成，下一步只做白名单同步

真实四张图与离线回放已完成，主代理/展示代理均逐张目视；1,400种交互轴与792期回放通过Node
模拟DOM核验，186,620行事件命中/坐标、55,440行报警前缀/面积和110,880曝光与原表一致。
独立科学复审及终稿复核无阻断，九项冻结协议检查再次通过（0.24秒），源码与协议没有修改。
详细结果/验收为`docs/s2b_scientific_results_2026-09-03.md`和
`docs/s2b_scientific_acceptance_2026-09-03.md`；蓝图新增第1.17节作为当前科学决定。

01:30:37系统确认无SeismoFlux Python后台，CPU0 14%、CPU1 12%，整机约13%。本轮计算结束，
不要重启PID36892或旧预测器，不为没有后台而自动恢复已完成任务。

公开白名单仅四份文档/蓝图更新、本run的summary与score_manifest、前三组PNG/SVG共六聚合图。
案例图、HTML、全部事件/报警数组、render_manifest、real_replay_qa保留本机。不得全量git add。
先提交、推送并回读远端，再记录闭合；同步前不开始新的物理数据模型。全部旧未跟踪产物保留。

## 14. 01:35:24远端闭合及下一安全工作

科学提交`1c2d5009a1a02805da3b5debc954875a9160cb35`已推送到
`origin/codex/p2r-multitask-multidata`，01:35:24远端回读一致。共12个白名单文件：四份蓝图/文档、
聚合summary与score_manifest、六张聚合PNG/SVG；没有上传事件数组、案例或HTML。
**S2-B已经完整闭合，不得重新预测、评分、渲染、扩搜速率或等待某个未来起报。**

下一任务已经开始有界只读核对：现有长期危险性、应变/GPS/GNSS究竟有哪些源文件、字段、单位、
覆盖与历史属性，哪些只是专家/模型综合分数，哪些依赖2026时点或上次强震。先利用已落地账本与
schema，不先假定资料齐全，不因缺字段启动大规模补齐工程；之后结合国内外经典方法冻结一次
有限增量对照。当前没有新模型训练、预测或评分，不得称危险性/应变已用出效果。

本轮主图已请求在当前任务右侧打开；应用返回queued，仅表示等待该任务显示，不能声称用户已经
看到。文件与离线回放可直接由结果文档进入。心跳保持每30分钟ACTIVE，随后切换到本节的新任务。
