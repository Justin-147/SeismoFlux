# S2-C首次无效运行：只纠正域外检查范围

2026-09-03 02:50（北京时间）。本记录是实施范围修复，不是新科学模型或参数方案。

实现`d29d46bb7368ff9fdbcdfa159e097be8f4ee29cf`于02:41:49远端确认后，02:42:39启动
单一预测进程29604，两折线程、BelowNormal、亲和性15（最多4逻辑核）。02:43:27在读取源
单元时因重复中心报错退出。**没有生成任何S2-C静态层、内层选择、外折预测或评分**。
`outputs/multitask_s2/s2c_strain_v1`的身份、目录缓存清单及stdout/stderr原样保留，不回写。

真实源文件仍为冻结SHA的145,086行14列；有145,036个唯一中心，50组中心各重复一次，全部
在经度-0.125°或+0.125°，纬度-54.8°到84.0°。部分分量不一致，不能擅自平均或挑一条。
但这些源矩形全在中国研究域之外，相交面积A_ij恒为0；其内容不会进入任何中国格的积分。

故修复为：先按**完整源矩形**与既有研究域包围盒相交选出候选，再对候选执行原重复/重叠检查。
跨边界单元即使中心在域外仍保留；域内重复或重叠继续报错。全源身份、形状、有限值检查不变，
全球重复数在audit中如实记录，不再把域外问题作为中国试验的一票否决。不清洗或修改原文件。
原标量、面积积分、模型、权重候选、折、地震样本、评分和协议SHA均未改变。

这与已撤销的全国95%否决门遵循同一纠偏原则：质量问题应限制在真正受影响的空间、时期与输入，
不能因为一个与当前任务无关的局部问题阻止所有研究。也不需要全球资料清洗或追加下载。

独立科学复审确认该局部修复不会改变域内数学假设或输入数值。合成回归覆盖域外冲突重复不影响
中国层、跨边界单元不遗漏、域内重复仍拒绝，以及完整预测/评分边界；44项回归通过（24.86秒），
Ruff通过，协议SHA未改变。
修复测试、提交推送后，以相同冻结协议运行新目录`outputs/multitask_s2/s2c_strain_v1_attempt2`。
不得覆盖首运行身份，恢复时显式传入新的`--output-root`；评分和展示也使用同一attempt2路径。

本轮没有新的命中率证据，首次停止属于输入实现故障而不是模型负结果。下一步仍是已登记的
四模型有限真实比较，不延伸为新质量门或工程项目。
