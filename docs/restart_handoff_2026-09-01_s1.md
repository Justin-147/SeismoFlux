# SeismoFlux S1 实时中断恢复交接（2026-09-01）

> 科学优先：每次阶段结果都要回答是否真实推动时间、地点、震级预测及同面积下独立区域召回；没有
> 实质推动时先调整假设，不继续堆叠工程。

## 2026-09-02 S1-C0 执行层最终安全点

- 当前阶段仍是 `S1-C0_all_M4_preliminary_development_screen_implementation`，但执行层已经完成
  最终验收；正式记录见 `docs/s1c_acceptance_2026-09-02.md`。此时仍没有真实外层预测成绩，不能
  宣称地点召回、时间、震级或联合效果已经提高。
- S1-C0执行层科学提交为 `cfdc102798e5aedff3d46402a18479151284699e`，已推送到
  `origin/codex/p2r-multitask-multidata`并于2026-09-02 04:26（Asia/Shanghai）远端回读一致；
  闭合记录见 `docs/s1c_remote_closure_2026-09-02.md`。
- 实际数据根目录明确为 `D:\AIPred\SeismoFlux\data`；工作树项目根目录为
  `D:\AIPred\SeismoFlux\data\interim\worktrees\p2r_multitask_multidata`。不得把仓库根目录误当
  数据根目录，也不得让程序从当前目录自行猜测。
- 无评分真实输入预检最终通过：权威目录40,898条，25 km格网15,697格，格网ID为
  `3aacdbdda04fed652dd5ee3674906f674c127cb735dea5d5e989527b20809763`，精确研究面积为
  9,415,305.754432771 km²；外层起报5,215行，四折各有99个互不重叠主暴露；内层暴露60组。
- 数值下溢修复后的最终S1相关回归测试为 `129 passed`；Ruff、格式检查、strict Mypy和
  `git diff --check`全通过。
  独立攻击复核结论为`GO`，P0/P1=`0/0`；50项封存、授权评分、运行与摘要定向测试通过。
- 已关闭伪造目录/格网/起报/目标进入官方评分、错误预测根目录、空选参记录、选参结果与NPZ不一致、
  续跑文件被篡改等主要旁路。剩余非阻塞P2只有恶意并发TOCTOU理论风险；不为这一低现实风险继续
  扩展工程，以免推迟科学成绩。
- 7天M6+只共享M5–6的NB2离散参数及不确定性，仍使用自己的均值和方差；每折每时长的L1收缩期、
  L2带宽和L3混合权重均与封存NPZ交叉绑定。四折总封印前官方评分入口不能读取外层目标。
- 500 km固定原点地区的“去掉最大贡献地区”敏感性当前原始评分行缺少冻结等面积投影地区字段，
  因而摘要必须诚实标为`not_evaluable`，不得临时用经纬度近似发明地区。S1-C1会在其冻结协议下
  正式处理局地完整度和地区敏感性。
- `seismoflux`心跳保持`ACTIVE`、每30分钟一次。大型运行最多3个外折线程，数值库每线程1核，
  至少保留2个物理核心；本安全点检查无重复后台Python训练/评分进程。

### 2026-09-02 首次真实predict数值下溢与修复安全点

- 从远端闭合提交`6ed63a37139bd387afec53d3ccd2ab605a62c733`启动的首次真实`predict`在更早
  内层L3近期权重选择处安全停止。失败前只写了1138字节的规范`run_manifest.json`，SHA-256为
  `31214f60c43872a8375eb5e6165a6513068233050f8c7178be81d689a94e5c45`；没有
  `parameter_selection.json`、折预测、封印、`score_phase`、外层目标或成绩。
- 原因是75 km高斯近期分量在目标距近期活动约40个带宽以上时，理论上的极小正质量低于双精度范围
  而表示为0；程序误把这个“数值0”当成“科学支持域为0”。L3从不单独使用近期分量，冻结权重最高
  0.75，始终至少保留25%严格为正的长期背景。
- 最小修复只允许L3近期分量在混合前保留该数值0；独立L1/L2长期质量仍必须严格为正，负值、非有限
  值和最终L3混合为0仍全部拒绝。候选带宽、alpha、数据、折、目标、面积和指标没有改变。
- 新测试用实际Gaussian KDE在0/4000 km两格、75 km带宽下重现近期尾部下溢，并验证alpha=0.75
  后长期+近期混合仍为`[0.875,0.125]`。独立复核为`GO`、P0=0、当前测试P1=0；一般长期分量
  极端下溢保留为非阻断长期改进，不在本次扩展成大规模log-space重写。
- 修复后完整S1集合`129 passed in 49.30s`，Ruff、格式、strict Mypy和差异检查通过；正式记录见
  `docs/s1c_predict_underflow_repair_2026-09-02.md`。修复提交和远端回读前不得重跑真实预测。
- 修复提交闭合后，先把旧`prediction_phase`原样移动到同一忽略输出根下的
  `failed_attempts/attempt_20260902T043107+0800_6ed63a3/prediction_phase`保存审计历史，再以新
  干净提交重建规范`prediction_phase`；不得删除或覆盖旧运行清单。

### 从本安全点继续的唯一顺序

1. 提交并推送本次数值下溢最小修复与交接记录，远端回读提交OID；不要预先创建没有依据的tag；
2. 在完全干净且已推送的提交上只运行`predict`，使用上述显式项目根、数据根和忽略的输出根，最多
   3个外折线程；不得读取外层目标、holdout、audit或locked test；
3. 核验四折NPZ、逐折封印和总封印，记录总封印SHA-256；
4. 单独运行`score`并显式提供总封印SHA-256；只有评分授权先落盘后才首次读取外层目标；
5. 立即生成不挑震例的静态图和离线交互回放，分别报告地点、时间、震级和联合效果；只作C0方向
   判断，不选冠军、不打开留出；
6. 无论C0正负，进入预登记的S1-C1因果局部完整度主检验。若真实结果没有实质推动，就停止对应
   模型方向，不用更多工程或更复杂模型掩盖。

## 2026-09-01 S1-C0 恢复后实时安全点

- 用户已要求按本文恢复工作并恢复心跳；现有 `seismoflux` 心跳为 `ACTIVE`，每30分钟一次，
  汇报必须包含S0-S7全线、当前科学问题、是否偏离目标、是否陷入无效工程细节及是否需要纠偏。
- 当前唯一工作树仍为
  `D:\AIPred\SeismoFlux\data\interim\worktrees\p2r_multitask_multidata`，分支仍为
  `codex/p2r-multitask-multidata`。不要在根仓库或 `science_first` 工作树继续本阶段；后者15个
  Stage4未跟踪草稿未触碰。
- S1-B科学提交为 `4cc46aa2cc0af2eba6a4d0763cc28e5e40018fd2`，远端闭合提交为
  `08858e2222d57d66bb9215ad7b3824d954de615b`；S1-B不得重新扩展。
- 当前阶段为 `S1-C0_all_M4_preliminary_development_screen_implementation`。成绩出现前的执行协议为
  `configs/multitask_s1_development_run.yaml`，通俗说明为
  `docs/s1c_runner_contract_2026-09-01.md`。本轮只用四个开发时间折，不打开2020–2022留出、
  2023+审计或一次性锁定测试。
- S1-C0先用起报当时可见的全部研究区M4+目录，比较L0–L3地点、T0–T1时间、M0/M3震级和
  J0–J4最小联合。它只是快速筛查，不能选最终冠军或授权留出；无论正负，下一步都必须做
  S1-C1因果局部完整度主检验。局部Mc过高只排除其固定500 km训练单元，不影响其他地区，也不
  缩小全国预测与评分区域。
- 已完成无成绩真实输入预检：冻结目录40,898条、25 km格网15,697格、全国精确面积
  9,415,305.754 km²；四折外层周起报5,215行，其中互不重叠主暴露396行；12个内层块、
  5个时长共1,180个主暴露。一张真实全国KDE表面约0.865秒，因此继续用精确计算，不引入近似。
- 已落盘但尚未形成阶段提交的部分包括：真实输入加载、评分纯函数、实际预测文件封存，以及正在
  实现的预测核心和授权评分核心。预测文件封存必须实际读取NPZ并绑定/复核相对路径、SHA-256、
  大小、数组键、shape和dtype；四折全部封存前，外层目标与成绩不可读取。
- 独立攻击复核随后发现旧封存接口仍可用非NPZ、四折共用同一文件、预存答案/成绩或伪造公开授权
  旁路。当前状态因此为 `NO_GO_FOR_REAL_SCORE_PENDING_MINIMAL_SEAL_REPAIR`；修复只限于每折唯一
  规范NPZ、预测/评分区分离、真实输入/代码身份重算和每次评分入口磁盘重授权。尚未读取任何外层
  目标或成绩，既有科学样本没有污染。
- 预测核心第二轮独立只读复核为P0=0、P1=1、P2=2，整体可进入运行器集成，但必须先修正一项
  不改变预测值的诊断记录：7天M6+只共享M5–6的离散参数`k`及其不确定性，M6+样本均值和方差
  必须仍由M6+自身内层计数计算。该最小修复及回归测试正在完成，尚未写入真实不可变预测。
- 2026-09-02恢复过程中又拦下预测区路径混用：冻结合同规定封存API的`output_root`就是
  `prediction_phase`，其下只能有运行/选参记录、四折NPZ与封印；答案和成绩只能写入并列的
  `score_phase`。封存实现与测试统一到这一含义并通过再次独立攻击复核前，真实评分继续禁止。
- 当前仍**没有S1-C0真实训练/检验成绩**，没有新的召回率、信息增益或模型排名。现阶段科学价值仍为
  `necessary_enabler`；其必要性仅在于让即将产生的历史盲测成绩可信，不能冒充预测提升。
- 2026-09-01本次恢复检查时没有后台Python训练/评分进程，整机CPU瞬时约18%。大型运行仍最多
  3个外折线程、每线程数值库1核，并至少保留2个物理核心。

### 下一安全动作

1. 完成预测核心、实际NPZ封存和授权评分核心，独立复核其不读外层答案；
2. 聚焦测试、Ruff、strict Mypy及真实无成绩输入预检通过后，形成S1-C0执行层验收、提交、推送和
   远端回读；
3. 运行四折真实预测并封存，全部封印复核后才授权第一次读取外层目标；
4. 成绩形成后立即制作静态图和离线交互回放，通俗说明每种数据/模型的训练与检验效果，并判断对
   “同面积下提高独立物理区域召回”是直接提升、无提升还是证据不足；
5. 停止无增量方向，不用更复杂模型掩盖；随后执行已经预登记的S1-C1局部完整度主检验。

## 2026-09-01 S1-B 当前最高优先级安全点

- 心跳`seismoflux`已按用户要求恢复为`ACTIVE`，间隔30分钟。
- 地点、时间/震级和无成绩开发合同均已独立复审`GO`；最终剩余P0/P1均为`0/0`。
- 合同首次审计的3个P1——整套日历可平移、输入身份可删改、稀疏回退/零块/水位可改写——
  均已锁死并由同一审计者重放确认拒绝。
- 41项聚焦测试通过，Ruff、格式检查、strict Mypy和`git diff --check`通过；正式验收见
  `docs/s1b_acceptance_2026-09-01.md`。
- 尚未训练或读取任何真实开发成绩；尚无命中率、召回率、信息增益或模型排名。锁定测试、
  holdout、audit均未打开，`science_first`的15个Stage4草稿未触碰。
- S1-B科学提交`4cc46aa2cc0af2eba6a4d0763cc28e5e40018fd2`已推送并远端回读一致；闭合记录见
  `docs/s1b_remote_closure_2026-09-01.md`。下一步可以建立最小四折runner，但runner必须强制
  传项目根目录、实际核验S0配置和两份账本哈希；不能使用跳过磁盘核验的默认调用。

科学价值类别为`necessary_enabler`：当前规则使后续结果可信，但没有直接提高预测效果。下一项真正
科学工作是用四个开发时间折比较L0–L3地点、T0–T1时间、M0/M3震级及最小联合；成绩形成后立即
判断是否稳定改善时间、震级或相同报警面积下独立地区召回，无增量方向立即停止。

下方“重启前紧急安全点”和“当前安全点”保留为历史过程；恢复时以本节及最新远端闭合记录为准。

## 2026-09-01 重启前紧急安全点

用户明确要求重启电脑并暂停心跳。本节是重启后的最高优先级恢复入口，以实际工作树
和本节记录为准，不得猜测未落盘的进度。

### 2026-09-01 重启后恢复记录

- 已重读本文并核对工作树；恢复时没有 Python 后台进程或重复训练/评分实例。
- 用户明确要求恢复心跳；`seismoflux` 已恢复为 `ACTIVE`，间隔仍为30分钟。
- S1-B地点纯算法已经独立复审 `GO`：P0/P1/P2=`0/0/0`，11项合成测试通过。复审曾发现
  内层标签只早于外折1天也会被接受的P1，已改为机械强制至少30天禁运：恰30天通过，
  29天和30天减1微秒均拒绝。
- S1-B时间/震级纯算法已经独立复审 `GO`：P0/P1/P2=`0/0/0`，16项合成测试通过。复审曾
  发现6.3等一位小数震级可能因浮点边界错入前一箱的P1，已改为精确十进制0.1半开分箱，
  GR概率质量与归一未变。
- 两组模块已由主执行者独立复跑pytest，并合并通过Ruff与strict Mypy；未读真实模型成绩、
  未运行holdout或锁定测试。
- 四个开发外折的三内层日历与无成绩样本水位已只读核对 `GO`；最早内层始于1985年，有
  1970年以来约15年历史可拟合。M6+ 7天很稀疏，必须冻结为不独立调参，零事件块保留且不事后并块。
- 当前正在将精确日期、水位、禁运和稀疏回退写成S1-B无成绩执行合同；合同验收、提交、推送前
  仍不允许运行真实开发评分。

### 已安全收尾

- 心跳自动化 `seismoflux` 已设为 `PAUSED`；重启后除非用户明确要求，不要自行恢复。
- 地点模块、时间/震级模块和内层时间切分复核三个并行任务已全部中断，不再写文件。
- 收尾检查时没有 Python 后台进程，CPU瞬时约 `17.5%`；没有真实训练或评分任务需要恢复检查点。
- 未运行锁定测试，未读取S1候选模型成绩，未触碰 `science_first` 的15个Stage4未跟踪草稿。

### 当前已落盘但未验收的S1-B文件

当前 `git status --short` 应至少包含：

```text
 M docs/restart_handoff_2026-09-01_s1.md
?? src/seismoflux/multitask_s1/
?? tests/unit/test_multitask_s1_location.py
```

1. `src/seismoflux/multitask_s1/location.py`：已落盘L0均匀、L1区域常率、L2因果KDE、
   L3长期+最近30天混合及 `tau/alpha/one-SE` 选择器。子任务报告为11个合成测试通过、
   ruff通过，但主执行者尚未独立复跑，导出和mypy/差异复核未闭合，**不得直接视为已验收**。
2. `tests/unit/test_multitask_s1_location.py`：地点纯函数合成测试已落盘；重启后应在数值库单线程下
   独立复跑，尤其核验近期窗口精确为 `(T-30d,T-24h]`。
3. `src/seismoflux/multitask_s1/time_magnitude.py`：重启前已出现在工作树（最后观测约19,525字节），
   但子任务在用户要求收尾时被中断；对应测试文件尚未观测到，也没有任何通过记录。
   **必须当作未完成、未验证的部分实现**，先读差异，再补合成测试，不得先读真实成绩。
4. `src/seismoflux/multitask_s1/__init__.py`：导出状态可能停在地点子任务的中间时刻，需要与两个
   实现文件一起独立核对。

上述文件均尚未提交、未推送；不要为了形式闭合提交未验收实现。

### 已得到但尚未冻结的内层时间复核结论

独立只读复核认为：对每个外折起点 `T`，可有条件使用三个时间向前内层评价块
`[T-15y,T-10y)`、`[T-10y,T-5y)`和 `[T-5y,T-30d)`；每块的参数拟合只能使用该块开始前
已知历史。这符合滚动起源/伪前瞻的经典思路，但尚需在任何成绩前完成四项核对：

1. 四个外折的精确日期；
2. 每个任务/预测时长的成熟标签数，少于2个有效内层块时不允许选参；
3. 最早外15年是否仍在1970+可支持目录范围内；
4. 将标签边界严格实现为 `issue+h<=inner_end`，将拟合标签禁运实现为
   `fit_issue+h<=inner_start-30d`。

24小时目录延迟与30天标签禁运是两个独立边界，不能互相替代。M6+单块稀疏不得用来
调参或事后并块。完成上述核对前，内层日历状态仍是 `reviewed_conditionally_not_frozen`。

### 重启后严格恢复顺序

1. 先读本文和 `git status --short`，确认没有重复Python训练/评分进程；心跳保持暂停，除非用户要求恢复。
2. 逐文件审读上述未跟踪实现，先独立复跑地点合成测试/ruff/mypy；再完成并验收
   时间、震级纯函数及对应合成测试。
3. 只读生成四个开发外折的三内层日历和各任务/预测时长样本水位；完成上述四项核对后，
   在成绩前冻结执行合同。
4. 完成地点/时间/震级实现的独立科学验收，形成可恢复提交并推送；仍不读真实开发成绩。
5. 然后才建立最小开发runner，只运行四个开发外折；先封存预测再读取目标评分。不得打开
   `C_HOLDOUT_2020_2022`，不得运行锁定测试。
6. 四折成绩一经形成立即做科学价值复审：是否在相同面积/时间成本下稳定推动地点、时间、
   震级或联合效果；无增量的方向立即停止，不用更复杂模型掩盖。

## 当前安全点

- 工作树：`D:\AIPred\SeismoFlux\data\interim\worktrees\p2r_multitask_multidata`
- 分支：`codex/p2r-multitask-multidata`
- S0科学提交：`68ce5d1957e2c864553a105d0c5cdc8af9dd58a1`
- S0闭合提交：`a1d83bb121ef0bfc5054804fd09a4b07717e0b0a`
- S0远端分支头已回读为：`a1d83bb121ef0bfc5054804fd09a4b07717e0b0a`
- 当前阶段：`S1_B_catalog_baseline_implementation_in_progress`
- 当前是否已打开S1模型成绩：否
- 当前是否训练或评分：否
- 锁定测试：未运行
- science_first的15个Stage4未跟踪草稿：未触碰
- 当前粗略进度：S1约`30%`；S1-A已验收、提交、推送并远端回读，正在进入目录基线实现，尚无S1新模型效果
- S1-A科学提交：`706098fba2d3074d4b237d25423f5b91eeb9646b`
- S1-A远端回读：2026-09-01 15:02（Asia/Shanghai），分支头与科学提交一致

## S0已经解决什么

S0已固定数据身份、时间折、7/30/90/180/365天、M5–6与M6+震级档、30天/75 km固定锚点episode、
周四00:00起报轴、同面积评价和39块次级空间迁移检验。正式水位为：1970+研究区28,602事件；
M5–6为1,050事件/792 episode；M6+为222/184。S0没有产生新预测效果，只建立公平实验地基。

## S1最终要回答的问题

只用完整长历史地震目录时，哪些经典或简单组合能够在严格时间向前和空间迁移检验中，分别改善：

1. 地点：相同实际报警面积下覆盖更多未来独立地区；
2. 时间：更准确地区分未来7/30/90/180/365天活动水平；
3. 震级：更合理地估计M5–6与M6+的相对频率和尾部；
4. 联合：地点、时间和震级合起来是否优于各自简单基线。

## 当前正在做什么

1. 在出现任何模型成绩前，冻结模型选择轴与39块空间压力测试的职责边界；
2. 同步完成国内经典、国际经典和当前主流目录基线复审，形成最小比较协议；
3. 暂不训练、评分或查看候选模型成绩。

S1-A复核发现一个新的科学P1：S0虽登记了候选k=5/4/3，却没有冻结功效检验统计量、最小有意义
效应、alpha、目标power、模拟事件率口径及“逐折还是池化通过”。空间检验功效取决于事件数和被比
模型差异，不能临时照抄通用事件阈值，也不能在S1自行发明门槛后声称k具有唯一科学依据。

因此在尚未读取任何S1成绩或评价期M5+/M6+震中时，决定停止物化39→k模型选择映射：S1模型选择
只使用与主估计目标一致的嵌套时间前推；39个冻结原子块仍按逐块留出后拼回全国OOF面，作为次级
空间迁移压力测试，但不参与调参。该决定避免为了完成形式要求而引入新的任意性，是对方案的科学
纠偏，不是模型负结果。

S1-A机器决定包已经完成，状态为`not_materialized_scientific_fallback`、`selected_k=null`、
`fold_mapping_materialized=false`。冻结脚本没有读取目录；在决定形成前曾有一次失败预检打开
`origin_time_utc/magnitude/inside_study_area`三个非空间列，但在产生计数前因时间标量错误中止，未读
经纬度、未读评价期震中、未用于决定。该事实已在配置、决定、manifest和说明中一致披露。

S1经典基线复审也已完成：S1只用目录，比较均匀/区域常率、因果长期KDE、B0_R30项目假设、Poisson/
负二项、全局及分层GR和通过数值资格的ETAS；所有候选、参数网格、回退与停止门在成绩前冻结。
B0_R30的alpha只能在每个外折更早内层因果选择，P1固定0.25只作旧结果参照；ETAS保持
`not_evaluable_not_scored`时不得阻塞或冒充负成绩。

S1-A独立科学验收已通过，剩余P0/P1为0/0；验收见`docs/s1a_acceptance_2026-09-01.md`。冻结
身份为：配置`4bb73e97a9eee8aab8eb18088097ce4fb50643b43d43f0051885b6503b1faa37`，决定
`d7a895071f349da1f95cbeb9e061c575a6aeeb3e6e9c15a4cce28543ebe771ee`，manifest
`05702fb7cc225e41078318f17f5ab8a195ac2dc7d56b3fdb8942d3e338b7e391`，经典基线协议
`288dd696d8911c11bec083deaf3bdee3b0252f465c634f52f1832d00e6408530`。主执行者复跑6项聚焦
测试、ruff和diff-check均通过。

S1-A已完成远端闭合，记录见`docs/s1a_remote_closure_2026-09-01.md`；不得返回继续修改候选、参数
网格、回退或停止门。当前开始S1-B最简单目录基线实现与开发时间外推。

S1-B只读复用审计已发现一个必须在真实评分前阻断的时间因果风险：主口径要求起报时刻`T`、目录
截止`T-24h`、近期窗口`(T-30d,T-24h]`三者分离；D1旧接口把同一时间同时作为起报和截止，若直接
传`T`会混入未来24小时，传`T-24h`又会把近期窗口错误平移为约`(T-31d,T-24h]`。因此S1不得
整搬D1构造器；新接口必须显式分离`issue_time`和`data_cutoff`，并在合成边界测试通过后才能读取
真实开发折成绩。这是防泄漏必要修正，不是模型效果负结果。

## S1拟比较的最小模型族

- 地点：均匀/区域常率、长期平滑KDE、近期活动B0_R30、可评价ETAS/Hawkes；
- 时间：Poisson常率、过度离散时的负二项、近期活动/ETAS条件率；
- 震级：Gutenberg–Richter及带年代/完整性敏感性的分层版本；
- 联合：地点强度 × 时间计数 × 震级分布，以及必要的共享参数组合。

以上仅是待经典方法复审后冻结的候选，不代表已经实现或有效。简单模型未通过前不进入大型神经网络。

## 下一安全动作

1. 完成S1-A决定包和经典基线协议的独立验收、提交、推送与远端回读；
2. 先实现最简单目录基线并只看开发/预登记外推折，不运行锁定测试；
4. 每完成一轮模型比较立即复审是否对最终预测目标有实质推动；
5. S1完成验收、提交、推送和远端回读后才能进入S2。

## 资源与中断规则

- 日常预检单进程、数值库单线程；
- 大型外推最多3个折线程，至少保留2个物理核心；
- GPU只用于后续真正需要的代表复杂模型，S1-A不使用；
- 每形成折映射、协议、模型比较或验收安全点立即更新本文件；
- 中断恢复先确认没有重复后台进程，再从已落地安全点继续。
