# S2-B断层速率协议验收

时间：2026-09-03 00:08（北京时间）。结论：`PASS`，允许协议提交推送后做有限实现，
**不代表已有预测提升，也不允许跳过实现验收直接运行真实比较**。

协议：`configs/multitask_s2_b_slip_rate.yaml`；SHA-256：
`a57218c9fbedc21ae28a62de4980fc26b17e990aa32f3613b9ae95fc1db19d29`。
方法与数据证据：`docs/s2b_slip_rate_methods_2026-09-03.md`。

## 必要科学核对

- 国内滑速/GPS原始论文、国际经典物理假设及目录/构造组合来源已实际阅读；未获得全文的来源
  只使用其作者摘要，不冒称逐式复现。本次高斯线积分明确登记为有限简化假设。
- 独立只读输入审计确认：519个唯一段，地质速率385段、测地515段、共同385段，原单位mm/a；
  缺测不当零，不丢对应地区的地震目标，不将未审核映射当作已知断层关联。
- 同支撑等权几何层与速率层分开，保留测地完整覆盖；十模型、14配对有限，不做全面交叉搜索。
  每线总贡献随地图长度×原速率变化，不做每线单位化，不伪称地震矩率或绝对概率。
- 当前静态快照的历史描述性局限清楚，保留原历史不可用标记；不混入上次强震年、2026离逝率
  或综合危险性分数。不变更冻结P1、旧S1/S2-A结果、目标域或震级数字。
- 时间轴、内层更早数据选择、原参考、面积成本和目标完全继承；取消全国百分比一票否决，
  不另造80%门。全部新预测保存后统一评分，不开留出或锁定测试。
- 数值只用固定3.125 km中点求积及一次6.25 km无目标粗细对照，不按外层效果改步长或不断加密。
- 独立方法复审无科学阻断；唯一文字修正是汇总计数明确包含0/70 km两容差，已修正。

## 检查与资源

主代理在项目venv、数值库单线程下运行`tests/unit/test_multitask_s2b_protocol.py`：
九项通过（0.25秒），Ruff通过、`git diff --check`通过。检查只读配置和方法文档，未读取
地震目标、未训练/预测/评分。旧阶段不重跑。

00:04:19资源检查无SeismoFlux Python后台；24物理/48逻辑核，两CPU34%/2%，整机约18%。
后续默认2折、最多3折、数值库单线程、BelowNormal，至少留2物理核心。

## 科学价值复审与下一步

本次是必要准备：确定了两类可以直接测试的新信息，尚无效果证据。没有偏离充分利用数据的科学
目标，也不因物理参数不全去建设复杂危险性平台。下一项必须直接服务于有限比较：实现新空间层，
复用现成目录和评价，再看同面积下新增/丢失多少独立震区。小收益保留，无益变体及时关闭，
不追加速率搜索。最新运行入口及同步记录统一写入`docs/restart_handoff_2026-09-03_s2b.md`。
