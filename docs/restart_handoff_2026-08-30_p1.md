# SeismoFlux P1 中断恢复交接（2026-08-30）

## 2026-08-31 Codex 重启安全点（最高优先级）

可以安全重启 Codex。当前没有 Python 后台任务，也没有需要续跑的模型计算。P1-0C 已完成科学验收、
提交、标签、推送和远端回读；科学提交为
`c71c97790adcf33f6c8121e367317857dc8dff31`，注释标签
`v0.2.7-p1-b0-r30-code` 解引用到该提交。闭合记录提交为
`24ea3627f9d10d254658d211fdae8d3fcf56f700`；本次重启安全点会在它之后增加一个只修改交接文档的
提交，因此重启后应以当前分支 HEAD 与远端跟踪分支相等为准，不要把 `24ea362` 误当作永远不变的
分支头。

当前工作树除交接文档更新外，只有既有 15 个 Stage4 未跟踪草稿：7 个
`src/seismoflux/anomaly_increment/kde_dev_*.py` 和 8 个
`tests/unit/test_stage4_kde_dev_*.py`。它们未暂存、未提交，重启后不得删除、移动、修改或批量暂存。

重启后按以下顺序恢复，不需要重新运行 P1-0C：

1. 进入工作树 `D:\AIPred\SeismoFlux\data\interim\worktrees\science_first`，确认分支为
   `codex/stage2-etas-science-first`。
2. 运行 `git status --short`，只允许看到上述 15 个 Stage4 未跟踪草稿。
3. 确认 `git rev-parse HEAD` 与 `git rev-parse origin/codex/stage2-etas-science-first` 相等；确认
   `git rev-parse 'v0.2.7-p1-b0-r30-code^{}'` 为科学提交 `c71c977...`。
4. 阅读本节、唯一蓝图第 1.6 节和 `docs/p1_0c_acceptance_2026-08-31.md`，不要重新优化 P1-0C。
5. `real_issue_authorized=false` 仍有效。没有用户另行显式 `RealIssueAuthorizationRecord` 时，只能
   维持科学心跳，不能创建真实 issue、联网取未来目录或运行锁定测试。

科学心跳自动化 id 为 `seismoflux`，每 30 分钟检查是否推进最终召回目标、是否跑偏、是否陷入工程
细节以及是否需要调整计划。Codex 重启不需要停止或重建该心跳。

## 2026-08-31 P1-0C 实时续接更新（优先于下文旧状态）

P1-0C 已完成科学验收、提交、打标签、推送和远端回读，阶段已经关闭；真实发行仍未授权。科学提交
为 `c71c97790adcf33f6c8121e367317857dc8dff31`，注释标签为 `v0.2.7-p1-b0-r30-code`；标签解引用
到该科学提交，远端分支随后包含闭合记录和重启交接文档。冻结历史适配稳定读取 40,898 条目录、
15,697 个 25 km 格，B0
入选 5,991 条、R30 为 0；空 R30 时两模型质量逐位相同，两边各圈 995 格、
599,494.3733448011 km²。

此前发现的坐标与格号 P0、回放月数自报 P1 均已修复。成熟回放现从 3,063 字节原始响应重新做
标准事件、合法暴露、10 个独立震群、代表事件落格、双模型命中和 `cluster_10` 复审；代表位置由同一
x/y 与 WGS84 往返，格号由正式 locator 生成，回放月数由起报时刻与回放时刻机械计算。换另一合法
格、协调修改月份和整条复审、替换 raw/hash/score/review 等攻击均被拒绝。

最终独立验收为 `127 passed in 228.40s`，Ruff、严格 Mypy、两次逐字节复算、离线检查，以及两张
静态 SVG 和两个离线交互页的实际渲染目视检查通过；独立科学审计为 `GO`、`P0=0`、`P1=0`。验收见
`docs/p1_0c_acceptance_2026-08-31.md`，七件结果见 `docs/p1_b0_r30_preflight/`。

科学价值分类仍是 `necessary_enabler`：本阶段没有新增真实命中，只保证未来成绩不能被事后改写。
因此不得继续扩展记录链或界面。已有预测改善证据仍是历史开发回放的 `5/21→9/21`；远端闭合后，
也必须另有显式 `RealIssueAuthorizationRecord` 才能真实起报，随后等待 30/90 天成熟结果。

既有 15 个 Stage4 未跟踪草稿继续禁止触碰。所有验证保持单进程且 BLAS 单线程，最多 3 个折线程并
至少保留 2 个物理核心。不得联网、不得读未来真实目标、不得创建真实 ledger、不得运行锁定测试。
未完成期间每 30 分钟科学心跳（Codex 自动化 id：`seismoflux`）必须复核：是否推进最终召回目标、
是否跑偏、是否陷入工程细节、是否需要调整计划。

## 一句话状态

整个项目尚未完成。历史回放已经找到一个值得真正前瞻检验的简单候选：在 30 天、相同
600,000 km² 报警面积下，长期背景 `B0` 命中 `5/21` 个独立 M5–6 震群，加入最近 30 天活动的
`B0_R30` 命中 `9/21`，多 4 群、提高 19.05 个百分点。异常数据没有显示额外贡献，因此已经停止
异常复杂化。真正前瞻规则、纯合成演练和 P1-0C 原始数据到得分闭环都已完成科学验收和远端闭合；
当前下一项真正科学工作是另行授权后开始按时保存未来预测，再等待 30/90 天成熟评价。

## 外行版：我们在做什么

可以把项目理解成比较两张每周更新的危险区地图：

- `B0` 只看长期哪里经常地震；
- `B0_R30` 在长期背景上再加入最近 30 天哪里活跃；
- 两张图必须圈同样大的总面积，未来真正发生的 M5–6 地震按余震簇合并成独立震群，再比较谁圈中
  的震群更多。

历史回放说明第二张图有希望，但还不是独立证明。P1-0A 已把未来比赛规则写死，P1-0B 用三套人造
答案确认程序既能识别“有帮助”，也能识别“没帮助”和“有害”，不会天然偏袒新模型。P1-0C 要保证
以后每一个命中都能从原始地震记录和报警格重新算出来，不能靠一张可被替换的汇总表。

## 完整工作线

| 工作线 | 状态 | 使用的数据与科学含义 |
| --- | --- | --- |
| S0 全数据与路线复审 | 已闭合 | 盘点 7 类、216 个原始文件；确定科学优先 |
| D1 历史因果回放与置乱 | 已闭合并推送 | 真实历史目录；`B0_R30` 为 `9/21`，`B0` 为 `5/21`；异常增量为 0 |
| P1-0A 前瞻预登记 v0.2.7 | 已闭合并推送 | 只冻结未来规则；真实 issue=0、真实效果=0 |
| P1-0B 纯合成双模型演练 | 已闭合并推送 | 只用内存合成数据；正/零/负方向全部恢复；非真实效果 |
| P1-0C 原始数据到得分闭环 | 已闭合并推送 | 40,898 条冻结目录；原始字节可机械重算到命中 |
| P1 真实按时发行 | 未授权、0 期 | 只能在 P1-0C、代码标签和显式授权闭合后从未来规则周四开始 |
| P1 30/90 天成熟评价 | 未开始 | 只评价按时保存且后来成熟的未来预测 |
| 锁定测试 | 未打开 | 继续禁止 |

## 已闭合的精确证据

### D1 真实历史开发结果

- 提交：`0dab57fd1491b5f4924cbae87c0b2001c6fc6b24`，已推送并远端回读。
- `B0_R30`：`9/21`；`B0`：`5/21`；多 4 个独立震群，`+19.05 pp`。
- 2,000 次配对 Bootstrap 95% 区间：`[+4.76,+38.10] pp`，`P(增益>0)=0.9905`。
- 这是历史时间外推开发证据，不是锁定测试或真正前瞻确认。
- 时间与空间置乱中的异常增量均为 0，停止异常复杂化。

### P1-0A 未来规则

- 当前协议提交：`0f43f15bc983a37157f1b129976c7ec0ea47fc7d`。
- 注释标签：`v0.2.7-p1-b0-r30-protocol`；远端标签解引用到同一提交。
- 每周四 `00:00 Asia/Shanghai` 起报，`Q=T-15min`。
- `B0_R30=0.75*B0+0.25*R30`，两路均为 75 km Gaussian KDE；未来不再训练或调参。
- 主终点为未来 30 天 M5–6 独立震群严格召回；90 天为次级。
- 两模型使用同一期快照、同支持域、同 25 km 格网，实际报警面积都不得超过 600,000 km²，挑战者
  不得比基准多圈。
- 10/20 群只报告并继续，36 个月前到 30 群或先到 36 个月时终判。
- 真值确实成熟且合计 0 群才可写“证据不足”；真值缺失或没有可评价窗口必须暂停，不能伪装成 0。

### P1-0B 纯合成验收

- 科学产物提交：`1f9a10dd9127e1978e407f5c36e3c3426c7e6b5e`。
- 注释标签：`v0.2.7-p1-b0-r30-synthetic`；远端分支与标签解引用均已回读到同一提交。
- 最终测试：`77 passed in 141.13s`；独立记录链审计和可视化审计均为 `GO`。
- 相同 600,000 km² 下，已知答案为：有帮助 `6/10→10/10`，无增益 `5/10→5/10`，有害
  `10/10→6/10`。
- 六件静态/交互产物逐字节复算通过；manifest SHA-256 为
  `00509ca8b34a51ae3226c2968de784e27cc74273aa2311ca2ed49a2866981762`。
- P1-0B 没有读取真实目录、异常表、网络或锁定测试，也没有发行真实预测。

## 数据在哪里、哪些已经使用

- 原始只读来源：`D:\AIPred\LocationPred`。
- 原始文件清单：`data/manifests/source_inventory.csv`；7 类、216 文件、43,610,994 字节。
- 当前权威标准化快照：`data/processed/stage1/debc98054172a4a1`。
- P1 将使用的冻结历史目录：
  `data/processed/stage1/debc98054172a4a1/earthquake_event.parquet`，截止
  `2026-07-09T04:25:56Z`。
- 异常因果特征包：
  `data/processed/stage3/anomaly_history/anomaly-feature-bundle-de7547faa9f87541`；已在 D1 检验但没有
  产生额外预测增量，所以不进入当前 P1。
- 断层、长期危险性和其余底图已整理，但当前简单 P1 不使用；它们以后只能作为单独慢窗口候选，
  不能混进本次已冻结比较。
- 人工预测地点、震级、时间和事后效能字段始终禁止进入模型。

更完整的逐表盘点见 `docs/ALL_DATA_SCIENTIFIC_INVENTORY_2026-07-31.md`。

## 当前唯一科学阻断

P1-0C 已把下面的链条机械闭合，并通过协调替换原始字节、坐标、合法格、评分、哈希、回放月份和
整条复审的主动攻击：

`原始目录字节 → 标准事件 → 选中 exposure → 独立震群成员 → 固定代表事件 → 两张报警掩膜 → 每群命中 → 复审`

因此当前不再存在原始数据到得分的本地科学阻断，阶段提交、标签、推送和远端回读也已闭合。
闭合不自动授权真实起报，仍必须由用户另行形成显式 `RealIssueAuthorizationRecord`。这一限制
避免把“能正确记分”误说成“已经证明能提高预测”。

## 中断后如何继续

工作树：`D:\AIPred\SeismoFlux\data\interim\worktrees\science_first`；分支：
`codex/stage2-etas-science-first`。

1. 运行 `git status --short`；只应看到既有 15 个 Stage4 未跟踪草稿，继续禁止触碰。
2. 核验科学提交 `c71c97790adcf33f6c8121e367317857dc8dff31` 与标签
   `v0.2.7-p1-b0-r30-code`；不得重新改模型、数据、指标或效果声明。
3. P1-0C 已结束，不再扩展记录链或界面；等待用户另行显式授权真实 issue。
4. 若获得授权，只按冻结 P1 规则在预定时刻保存预测；不得因后来的地震改写或挑选版本。
5. 到期后再读取真值，按独立震群和相同报警面积评价 30/90 天召回。
6. 每个新阶段形成结果后立即做科学价值复审；没有实质推动就调整或停止，不堆工程细节。

大型任务最多 3 个折线程并至少保留 2 个物理核心；日常验证优先单进程，避免电脑卡顿。

## 当前决定

- `acceptance`: `P1_0C_PASS`
- `stage_status`: `P1_0C_accepted_committed_tagged_pushed_remote_readback_complete`
- `science_value_category`: `necessary_enabler`
- `direct_prediction_improvement`: `none_new_in_P1_0C`
- `real_issue_authorized`: `false`
- `evidence`: D1 历史外推 `5/21→9/21`；P1-0C 只证明未来命中能由原始前像机械重算。
- `science_commit`: `c71c97790adcf33f6c8121e367317857dc8dff31`
- `annotated_tag`: `v0.2.7-p1-b0-r30-code`
- `decision`: `P1_0C_closed_wait_for_separate_real_issue_authorization`
- `next_scientific_test`: 获得另行授权后按时保存未来预测，等待 30/90 天成熟评价。
- `stop_condition`: 没有显式 `RealIssueAuthorizationRecord` 不得真实发行。
