# S2启动：断层信息能否补充目录预测

日期：2026-09-02 22:12（北京时间）。状态：`S2_METHOD_AND_INPUT_REVIEW_STARTED`。
这是已落盘的研究起点，**不是已冻结可执行协议，也没有S2预测成绩**。

22:30更新：精确首轮已另存`configs/multitask_s2_a_fault_geometry.yaml`及
`docs/s2a_fault_geometry_methods_2026-09-02.md`；22:36配置检查和独立复审已通过，续接以
`docs/restart_handoff_2026-09-02_s2a.md`为准。下文保留22:12研究起点，不当成最新运行状态。

## 1. 从哪里接着做

S1已完成科学验收并推送：科学提交`544360e34b139581508deba5c62d14bccf46d535`，
闭合记录`7164e3b8cd2b8af939f18e40337d6c89ac476295`，22:01远端回读一致。
最新完整工作线及既有结果见`docs/restart_handoff_2026-09-02_s1c2b.md`。
不重跑95%/Ms审计、C0、C2A或C2B，也不等待某个未来日期。

本轮先问一个直接问题：**同样的历史目录背景，加上断层在哪里，能否在相同报警面积内少漏震区？**
几何、滑动属性、危险性分量和应变分开研究，避免无法知道收益来自哪种资料。
首轮重点90/180/365天及Ms≥6，同时保留7/30天、Ms5–6参照；不临时改变原分档和区域限制。

## 2. 已有资料可以直接使用

权威数据目录：`D:\AIPred\SeismoFlux\data\processed\stage1\debc98054172a4a1`。

| 资料 | 只读核对的数量 | 首轮角色与白名单 |
| --- | --- | --- |
| `fault_segment.parquet` | 519段，235个断层ID | 主资料：`fault_segment_id`、`simplified_geometry_wkb` |
| `fault_point_raw.parquet` | 7,339原点；现成线形含7,331点 | 已处理8个相邻重复点，无须重新建线 |
| `fault_trace.parquet` | 7,216记录，7,215条可用线、1条零长度 | 独立几何敏感性：`trace_id`、`geometry_wkb`、`usable_for_geometry` |
| `fault_trace_crosswalk_audit.parquet` | 1,611条名称候选，全部未审核 | 不用于把519段属性自动赋给精细迹线 |

简化几何由原始经纬度按`fault_id/segment_number/point_number`连接，不由评价地震生成。
精细迹线有144组重复几何、涉及341条记录；最近线距离不受重复条目影响，不为此另开清洗项目。
以后若做长度/密度加权，再明确去重计权，不能把原始点数当断层密度。

两份主几何身份：

- 段SHA-256：`8f37d08f2427aab78f45dc9ae32130582706eb8c5d58d4d9b1e8b18d719f8627`；
- 线迹SHA-256：`33a6825538bf9e54706012892e606d4a570c686ff85257ce49602a16948aacc7`。

首轮不用滑动/测地速率、复发周期、上次强震年、当前离逝率、破裂空段/闭锁/小震稀疏/库仑应力
权重或综合危险性评分；这些留给后续独立消融。`elapsed_ratio_at_snapshot`使用2026快照年份，
不能原样当作早期历史起报时的特征。来源属性中的走向、倾角、滑动角也不混称为纯线形信息。

## 3. 历史可用性与空间距离的边界

全部地质条目的`source_available_at`统一为2026-07-12T16:00Z（北京时间7月13日00:00），
`historical_model_eligible=False`。这是本项目收集快照，不是各地图出版时间。文件修改时间及V4名称
不能证明2000—2019已经公开可得。**保留这些原始标记，不将其改成历史合格。**

S0与总方案已允许当前静态几何的历史描述性研究。因此本轮可以继续，但科学角色明确为
“当前静态断层几何对历史地震空间分布的补充检验”，不能称严格当时可发布的前瞻检验。
不以此阻塞已有数据探索；将来保存预测时再检验实际未来效果。新的地质结果不能无说明地与
S1目录的因果输入声明合并成更强证据。

WKB为不带SRID的经纬度，沿用既有WGS84解释；未找到原始坐标基准的独立说明，作为小尺度几何
局限记录，不展开新的溯源工程。复用冻结中国Albers等面积投影及25km格网的既有代表点；格点是
裁切后`point_on_surface`，不重新生成中心或以真实震中加密。投影线为米、既有点为公里，计算时
统一单位。投影距离不是精确测地距离，方法中明示即可。

## 4. 本次已经实际阅读的经典与主流方法

| 来源 | 原研究做法 | 对本次的采用或边界 |
| --- | --- | --- |
| [李峰等，2013，重庆市主要构造地震危险性评价](https://www.dzdz.ac.cn/CN/10.3969/j.issn.0253-4967.2013.03.006) | 国内常用的断层调查、历史强震/微震平滑与泊松长期评价 | 断层与目录并用有既有研究依据；百年危险性不等于本项目7—365天的有效预测 |
| [Rhoades与Stirling，2012](https://central.scec.org/publication/1521) | 地震邻近与断层邻近两分量；反幂距离衰减，原断层分量还使用滑动速率；线性组合 | 首轮采用邻近信息与目录分量配对的思想；去掉速率是纯几何消融变体，不冒称复现原模型 |
| [程佳等，2020，川滇地震危险性预测模型](https://html.rhhz.net/dqwlxb/2020-3-1170.htm) | 断层滑动速率、GPS应变率与平滑背景分量，最后计算地震动危险性 | 支持将几何、运动学与应变分别加入；不复制其Mw换算、震源分区或PGA目标到本项目 |
| [Bayona等，2021，TEAM/WHEEL混合研究](https://academic.oup.com/gji/article/224/3/1945/5996192) | 目录与构造/应变分量组合，在较晚时段比较 | 后续应变及模型组合的依据；全球结果不保证中国区域或单一时限改善 |

已打开上述原始期刊/研究机构页面；Rhoades 2012当前可读的是SCEC作者摘要，出版商全文读取失败。
因此没有声称已经核对其全部公式、参数或复现细节。下一步可以依据已明确的方法类别登记最小
纯几何变体，给出自己的完整公式和有限候选，并与目录/断层单独模型配对，不能冒用原文最优参数。
不需要为本项目安装完整PSHA或地震动计算平台。

## 5. 下一项有限比较怎样设计

拟采用“最近断层距离形成独立空间层，再与目录层有限组合”的第一轮。精确距离函数、尺度、
组合权重及唯一平分规则仍需写进下一份可执行协议，**现在尚未冻结、不得直接启动训练**。

必须在协议中保持以下比较关系：

1. 单独断层几何、单独目录、同一目录加断层分别报告，才能区分构造信息与混合收益；
2. 以S1多尺度为主要目录背景，年龄小面积互补与原L3保留参考，不做全部14模型全交叉；
3. 使用四个2000—2019开发时间折和原起报、延迟、目标、面积；各时限与震级分母分别报告；
4. 混合权重只用更早内层数据选择；内层中的目录参数也必须由更早训练生成，不能直接拿看过
   I2/I3目标选出的外折目录参数反填内层验证。可复用已存基础核，不重跑整套C2B；
5. S1模型族已按开发结果选择，因此S2仍是开发探索，不宣称新的独立确认；
6. 不重划已有39原子空间块，不按地震数挑有利块；后续按既定方案汇总空间迁移，不为小样本
   设新的全国百分比否决。当前固定全国域和受控报警面积不变；
7. 纯几何先出有限结果，再分别加入属性/危险性、GSRM或可得GNSS；现代静态资料的可用时间
   限制单列，不把短覆盖数据的共同期当长目录的全部样本。

候选形成后立即比较严格落区、相同实际面积预算的独立震序得失，并保留每折与全部任务正负结果。
没有增益就结束相应变体，不追着外折分数追加尺度或权重；小而任务专用的增益可保留继续检验。

## 6. 复用入口与中断恢复

以下文件相对于活动工作树：

- `src/seismoflux/data/contracts.py:221`、`:268`：段和精细迹线的字段契约；
- `src/seismoflux/data/geology.py:432`、`:479`、`:513`：简化几何；`:679`：精细线形；
- `src/seismoflux/data/geology.py:502`：快照离逝率，首轮禁止使用；
- `src/seismoflux/data/pipeline.py:180`、`:258`：权威分表产物，不改这些文件；
- `src/seismoflux/multitask_s1/runner_inputs.py:594`：不读目录的固定空间输入；
- `src/seismoflux/background/grid.py:51`、`:268`：CRS及转换写法。现有多边形转换函数不能直接
  传LineString，只复用`Transformer(always_xy=True)`与`shapely.ops.transform`；
- `src/seismoflux/d1_replay/spatial.py:314`：既有代表点的公里坐标。

当前没有S2脚本、机器协议、训练进程或成绩。下一安全动作是精确登记首个纯几何配对并独立复审，
必要验证/提交推送后才实现和运行；不要把此研究起点当已完成的S2。已完成S1不再打开。

科学价值：本次为新增资料检验的必要准备，尚无S2预测增益；没有偏离多数据目标。避免新的地图
清洗、断层关联和平台建设循环，直接利用可用几何开展有限对照。S3—S6尚未完成，S7并行；
心跳每30分钟继续，保持受控CPU、至少保留2个物理核心。不读取2020—2022留出、2023+审计或
锁定测试，不改冻结P1，不触碰science_first的Stage4草稿。
