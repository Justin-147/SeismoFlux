# SeismoFlux 重启续接交接：Stage 2P 科学 MVP

- 日期：2026-07-31
- 工作树：`D:\AIPred\SeismoFlux\data\interim\worktrees\science_first`
- 分支：`codex/stage2-etas-science-first`
- 当前阶段：Stage 2P-1B 合成科学 MVP 已本地验收；等待提交、推送和代码标签回读
- `stage_id`: `Stage2P-1B`
- 控制协议阶段：`Stage2P-1A2`
- 协议版本：0.2.5
- 当前科学价值：`necessary_enabler`
- 直接预测提升证据：无

## 1. 先用一句话说明正在做什么

已经用三组已知答案检查三张同面积地图会不会正确判断正、零、负结果；下一步才是按时冻结第一张
真实 P0/P1/PP 前瞻图，并等待未来地震检验。

## 2. 当前完成到哪里

Stage 2P-1B 已完成本地验收：

- 三种合成情景均形成 P0/P1/PP 静态图、总对比图、离线交互页和指标；
- 近期有效时 P1 明显赢，无增量时不虚构优势，近期误导时 P1 明显变差；
- 三模型实际报警面积均为 600,000 平方公里；
- 每种情景 36 个目标、12 个震群、4 个区域，2,000 次配对 Bootstrap；
- 审计发现并修复了手工构造窗口注入未来事件的 P0 旁路；
- 异质 horizon 零分母会返回 `evidence_insufficient`，不重抽；
- 精确回归 282 项通过，三路独立复验最终 GO，P0/P1 阻断为 0；
- 当前没有读取真实目录、没有联网发行、没有把合成结果称为真实预测证据；
- 验收提交 `d9d662c2accde861dd4b0bca8796fa870ef4d547` 已推送；
- 协议标签 `v0.2.5-prospective-science-mvp-protocol` 已推送，远端 peeled commit 与验收提交一致；
- Stage 2P-1B 代码和结果尚未提交、推送或打代码标签，因此真实阶段仍未授权。

## 3. 模型和数据

- `P0`：长期 75 km KDE，多发区背景；
- `P1`：0.5×P0 + 0.5×最近 30 天 M4+ KDE；
- `PP`：0.5×P0 + 0.5×再早 30 天 M4+ KDE。

本阶段已经只用人工生成但规则清楚的合成地震目录完成“近期有效、无增量、近期误导”三种情景。
目标按已知地图关系构造，因此 100 个百分点的差只验证方法和评价器，不是现实效果估计。

真实路线以后仍使用每个起报时刻之前可见的地震目录。异常表、断层和长期危险性表不进入本次第一
个最小比较；它们将在基础地震活动挑战者有清楚结果后，以独立挑战者逐个加入并做贡献消融。

## 4. 下一步执行顺序

1. 已完成并远端闭合 v0.2.5 最小科学协议；
2. 已完成 Stage 2P-1B 三种合成情景、静态图、交互页和指标；
3. 已修复科学审计发现的未来事件旁路和三项 P1；
4. 已完成 282 项回归、2,000 次结果包复算、视觉 QA 和三路独立 GO；
5. 当前只允许提交明确的 Stage 2P-1B 文件，禁止暂存 Stage 4 草稿；
6. 推送分支并建立 `v0.2.5-prospective-science-mvp-code` annotated tag；
7. 回读远端分支和标签，更新本交接的远端闭合证据；
8. 只有以上闭合后，才能另立并验收 Stage 2P-1C 真实前瞻起报边界。

## 4.1 Stage 2P-1A2 远端闭合证据

- 分支：`codex/stage2-etas-science-first`
- 远端分支提交：`d9d662c2accde861dd4b0bca8796fa870ef4d547`
- 协议标签对象：`95ef5c7bab5d2192d83055aada70d94ab9681ff6`
- 协议标签 peeled commit：`d9d662c2accde861dd4b0bca8796fa870ef4d547`
- 最终验收：255 项通过，独立审计 GO，P0/P1 阻断为 0
- 本阶段科学价值：仍为 `necessary_enabler`，没有真实预测提升证据

## 4.2 Stage 2P-1B 本地闭合证据

- 实现状态：`accepted/frozen`
- 结果目录：`docs/stage2p_science_mvp/`
- 结果说明：`docs/phase2p1b_science_mvp_results.md`
- 精确回归：282 项通过
- 结果复算：2,000 次 PCG64 种子 147，六个产物逐字节 `--check` 通过
- 独立审计：三路最终 GO，P0=0、P1=0
- 视觉 QA：总对比 SVG 和离线交互 HTML 已用浏览器实际渲染通过
- 真实数据：未读取
- 真实预测证据：无
- 待完成：提交、推送、代码标签和远端回读

## 5. Stage 2P-1B MVP 的验收结果

每种情景已经展示：

- P0、P1、PP 三张相对强度图；
- 相同面积报警区和未来合成目标；
- 严格区域召回与支持域信息增益；
- P1-P0 和 P1-PP；
- 移除最大贡献区域、最大震群后的方向；
- 清楚的“合成演练，不是真实预测证据”标记。

实际行为：

- “近期有效”：P0/P1/PP 震群召回为 0%/100%/0%，两项比较均为 +100.0 pp；
- “无增量”：P0/P1/PP 均为 100%，两项比较均为 0；
- “近期误导”：P0/P1/PP 为 100%/0%/100%，两项比较均为 -100.0 pp。

三种情景的“合成已知答案检查”全部通过。这个“通过”不代表真实确认性门通过。

## 6. 停止条件

- 代码提交、推送或标签远端回读不能闭合：不进入真实数据阶段；
- 真实来源、局部 Mc、支持域、39 区或按时发行不能公平闭合：不发行；
- 未来正式结果不通过预登记门，或第 104 个按时期仍证据不足：停止 P1、保留 P0；
- 工作再次被与科学效果无关的工程前置长期阻断：停止扩张并复审。

不得换窗口、权重、面积或转向大型网络追逐已查看 cohort 的阳性结果。

## 7. 科学价值复审字段

- `science_value_category`: `necessary_enabler`
- `evidence`: 合成正/零/负情景被正确区分，未来旁路封死，静态与交互图可直接查看
- `uncertainty_change`: 实现和评价逻辑不确定性下降；真实预测效果不确定性未下降
- `decision`: `accept_stage2p1b_then_remote_close_before_stage2p1c`
- `next_scientific_test`: 第一张按时冻结的真实 P0/P1/PP 前瞻图，并等待成熟真值
- `stop_condition`: 真实公平性或按时发行不能闭合则不发行；正式门失败则停止 P1、保留 P0

## 8. 禁止触碰

工作树中已有与本阶段无关的 Stage 4 未跟踪草稿。续接时不得删除、修改或提交；暂存必须逐路径，
禁止 `git add .`。
