# SeismoFlux 重启续接交接：Stage 2P 科学 MVP

- 日期：2026-07-31
- 工作树：`D:\AIPred\SeismoFlux\data\interim\worktrees\science_first`
- 分支：`codex/stage2-etas-science-first`
- 当前阶段：Stage 2P-1A2 最小科学合同已完成并远端闭合；下一步为 Stage 2P-1B 合成科学演练
- `stage_id`: `Stage2P-1A2`
- 协议版本：0.2.5
- 当前科学价值：`necessary_enabler`
- 直接预测提升证据：无

## 1. 先用一句话说明正在做什么

比较三张相同报警面积的地图，判断“最近 30 天的地震活动”是否真的能让未来地震区域预测比长期
多发区和旧时窗对照更好。

## 2. 当前完成到哪里

已完成路线纠偏和本地验收：

- 旧 v0.2.4 协议完整保留为历史；
- 新 v0.2.5 只保留防止偷看未来、事后改图、扩大面积和重复调参的科学硬约束；
- RFC3161、TSA、证书链、逐工件注册和硬件收据不再是合成科学演练的前置；
- 下一阶段必须产出 P0/P1/PP 静态图、离线交互页和同面积效果指标；
- 当前没有读取真实新增目录、没有联网发行、没有打开未来目标或效果；
- 新旧协议及背景回归共 255 项通过，独立复验为 GO，P0/P1 阻断为 0；
- 验收提交 `d9d662c2accde861dd4b0bca8796fa870ef4d547` 已推送；
- 协议标签 `v0.2.5-prospective-science-mvp-protocol` 已推送，远端 peeled commit 与验收提交一致；
- Stage 2P-1B 合成演练现可开始；真实目录、联网发行和未来效果读取仍未授权。

## 3. 模型和数据

- `P0`：长期 75 km KDE，多发区背景；
- `P1`：0.5×P0 + 0.5×最近 30 天 M4+ KDE；
- `PP`：0.5×P0 + 0.5×再早 30 天 M4+ KDE。

下一阶段只用人工生成但规则清楚的合成地震目录，构造“近期有效、无增量、近期误导”三种情景。
这一步的目的不是宣称真实地震可以预测，而是先验证方法和评价器会给出符合已知答案的判断。

真实路线以后仍使用每个起报时刻之前可见的地震目录。异常表、断层和长期危险性表不进入本次第一
个最小比较；它们将在基础地震活动挑战者有清楚结果后，以独立挑战者逐个加入并做贡献消融。

## 4. 下一步执行顺序

1. 已完成 v0.2.5 配置、Schema、文档一致性和防泄漏验收；
2. 已完成独立审计，确认科学门完整、过度工程门已取消；
3. 已将同一候选唯一改为 accepted/frozen；
4. 已重跑同一套 255 项验收；
5. 已只提交本阶段 9 个明确文件，并完成推送和远端协议标签核验；
6. 进入 Stage 2P-1B MVP；
7. 生成三种合成情景的静态三联图、离线交互页和指标；
8. 立即复审是否对科学目标有实质推动。若没有直观、可判断的结果，停止工程扩张并调整。

## 4.1 Stage 2P-1A2 远端闭合证据

- 分支：`codex/stage2-etas-science-first`
- 远端分支提交：`d9d662c2accde861dd4b0bca8796fa870ef4d547`
- 协议标签对象：`95ef5c7bab5d2192d83055aada70d94ab9681ff6`
- 协议标签 peeled commit：`d9d662c2accde861dd4b0bca8796fa870ef4d547`
- 最终验收：255 项通过，独立审计 GO，P0/P1 阻断为 0
- 本阶段科学价值：仍为 `necessary_enabler`，没有真实预测提升证据

## 5. Stage 2P-1B MVP 的验收

每种情景都必须展示：

- P0、P1、PP 三张相对强度图；
- 相同面积报警区和未来合成目标；
- 严格区域召回与支持域信息增益；
- P1-P0 和 P1-PP；
- 移除最大贡献区域、最大震群后的方向；
- 清楚的“合成演练，不是真实预测证据”标记。

预期行为：

- “近期有效”情景：P1 应明显优于 P0 和 PP；
- “无增量”情景：不得虚构稳定优势；
- “近期误导”情景：P1 应显示恶化或不通过。

## 6. 停止条件

- 合成三种情景不能被正确区分；
- 图和指标不能让人直观看出模型好坏；
- 工作再次被与科学效果无关的工程前置长期阻断；
- 未来正式结果不通过预登记门，或第 104 个按时期仍证据不足。

前三种情况立即停止堆叠工程并简化方案；最后一种情况停止 P1、保留 P0。不得转向大型网络来补救。

## 7. 科学价值复审字段

- `science_value_category`: `necessary_enabler`
- `evidence`: 当前只有最小合同纠偏，没有真实预测效果
- `decision`: `continue_to_stage2p1b_mvp_synthetic_after_acceptance`
- `next_scientific_test`: 三种合成情景的 P0/P1/PP 图、交互页和同面积指标
- `stop_condition`: 无直观科学结果则停止简化；正式门失败则停止 P1、保留 P0

## 8. 禁止触碰

工作树中已有与本阶段无关的 Stage 4 未跟踪草稿。续接时不得删除、修改或提交；暂存必须逐路径，
禁止 `git add .`。
