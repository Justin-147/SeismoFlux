# S1-C2A审计纠偏与固定参数对照协议验收

日期：2026-09-02。结论：**通过，可在本提交推送后进入最小位置对照实现与运行。**

## 科学问题与决定

本次只回答两个恢复问题：95%全国支持门、机器震级类型空值是否改变已完成结果，是否需要重跑。
审计确认旧预测成绩未因此改变；C1预测从未启动，只有支持诊断。复用其诊断，新增C2A固定参数
输入敏感性即可，不重复入库、样本计数、内层选参、时间/震级模型或ETAS资格。

准确的协议、事实与恢复入口：

- `configs/multitask_s1_c2a_input_sensitivity.yaml`
- `docs/s1_support_threshold_magnitude_audit_2026-09-02.md`
- `data/manifests/catalog_magnitude_semantics_2026-09-02.json`
- `docs/restart_handoff_2026-09-02_s1c2_resumed.md`
- 总蓝图第1.11节。

## 已完成的验证

- 从已有支持摘要重新分类：95%为0/16节点，80%为3/16；外折起点80%为2/4。
- 核对六个父产物的实际SHA-256，包括C0运行合同、内层参数、四折预测、旧评分文件和C1两份诊断。
  验证时对旧评分只计算字节哈希，没有提取新目标或新成绩。
- 四折30天参数与C0内层产物逐项一致：tau为5/1/1/5年，带宽75km，近期权重0.25。
- 只读取类型列核对机器事实：标准目录40,898行类型均空；源表43,785行类型均空，来源数为
  38,375与5,410。两源默认Ms来自用户明确说明，不是算法反推，不更改原数值。
- 元数据源文件SHA与既有source_inventory一致，明确排除ComCat等外部目录。
- 全国面积门关闭、固定参数不重选、holdout/audit/locked test/未来目录等边界明确禁止。
- 既有输入、局地完整性和Mc定向单元验证：24项通过；`git diff --check`通过。
- 两项独立科学复审均为GO：一项检查面积规则与公平对照，另一项检查Ms语义及外部目录边界。
- `science_first`旧工作树只读状态仍为原15个Stage4未跟踪文件；本轮未修改这些文件。

以上验证只确认定义和复用依据，**没有新的模型训练成绩、测试成绩或预测提升**。

## 实现时必须保留的科学细节

1. 三模型在同一种掩膜下使用同一历史输入；L1曝光起点仍为1970，不以过滤后的最早事件重置。
2. 近期窗为`(T−30天,T−24小时]`，不是在数据截止日前再倒推30天。
3. 复用C1正式空间归属函数，含国界/固定格边界处理；CSV布尔字符串必须明确解析。
4. 只改变训练中心，模型仍在完整全国格上归一化并接受完整目标评价。
5. 复制原预测须核对本期输入事件集合确实不变；所有复用都注明来源。
6. 采用C0同样的完整格面积前缀和并列顺序，报告每个模型的实际面积，不把相同预算误写为逐平方米
   完全相同。格网前缀是开发期诊断，不冒充最终有限连通预测区域发布。
7. 六条曲线四折全部保存后才抽取C0相同目标统一评分；空目标期保留，147个首震锚点不增不减。
8. 完整报告A/B正、零、负效应；固定参数敏感性不是重调后的性能上限，不是独立确认。

## 科学价值复审

- 分类：`necessary_scientific_correction_enabling_a_direct_input_ablation`。
- 对最终目标：现在没有新增命中；已解除不合理的全国停止条件，下一项直接检验同面积召回。
- 是否偏离：没有；未扩大目标、面积、模型族或参数，不重做已有结果。
- 是否工程化：只做复用和单因素比较所必需的检查，不修改旧框架以兼容不需要的五时限/时间/震级链。
- 继续决定：本阶段提交推送后实现轻量位置执行器；出结果后立即画图、离线回放、复审并落盘。
- 停止条件：A/B两种输入处理完成即结束此问题，不追加百分比、Mc或网格搜索。

```yaml
acceptance: PASS
new_model_predictions_at_acceptance: 0
new_model_scores_at_acceptance: 0
next_after_this_commit_is_pushed: S1_C2A_LOCATION_ONLY_IMPLEMENTATION_AND_PREDICTION
holdout_authorized: false
audit_authorized: false
locked_test_authorized: false
```
