# S3-A已有预测的本机离线回放

2026-09-03，接线核验及本机真实页面生成完成；归因任务仍在运行，整个S3-A未完成。

目的：让同一时窗、同一面积预算下的新增命中和丢失命中可直接查看，不用界面或代码量代替预测效果。
这属于既有效果的可解释展示，不产生新训练成绩或独立检验证据。

## 范围

- 只读`s3a_fit_v1`完整预测、`s3a_score_v1`已有诊断、`case_ledger_v1`已有事件元数据和冻结静态几何。
- 原`log_alarm_prefixes`恢复五模型全部面积档的报警网格，核对保存的实际报警成本。没有重算命中。
- 两折、四个可评价时限、两震级档、主起报/全部报告、全部六对比、五面积档、严格/70km、
  锚点/后续/全部事件均可浏览。空地震窗口不丢弃，365天明确NA，不转换成零成绩。
- 默认30天、Ms5–6、60万km²、动态对覆盖、主起报、锚点、最早合法日期，不按命中挑默认震例。
- 左右图共享范围，点击事件可共同放大。放大仅改变显示，不以真实震中修改报警区、格点或推理。
- 次数表显示该震级档全部目标事件，不因锚点显示筛选而改变实际次数。页面不声称学会震级分布。
- 完全离线，无CDN、瓦片服务或网络请求；包含事件位置，页面及载荷仅本机，不进入公开白名单。

## 核验与科学边界

16项小型合成核验通过，Ruff及新增入口类型检查通过；独立只读科学接线复审通过。
Node原生执行检查了真实HTML id、所有控制、默认空窗、365天NA、保存命中切换和双图同步放大。
这是逻辑核验，不冒称真实浏览器视觉验收。既有file访问拒绝不绕过、不新建服务平台。

预测与诊断身份记录进回放清单；没有读取新目录目标、未来信息、C成绩、锁定测试或正在运行的
置乱效果，没有修改模型、数据、指标和200+200规模。支持局部进步，同时保持正负结果完整。
归因、震序等权/区域分层和整个S3-A科学复审仍待完成，交互页面完成不代表这些科学任务完成。

## 真实产物核验（14:04）

代码提交`7ea3ad8f1ef6ba7850a1f7482aa0b7969c8c7c60`推送回读一致后，唯一PID45680于13:58:53
启动，14:01:37输出完整页面与清单后正常退出。原归因进程未改变，两个日志均无错误输出。
页面保存在`s3a_score_v1/replay_v1`，324帧、648个分震级窗口、38个不同地震；338个空窗口保留。
这是既有证据的展示数量，不是增加的独立样本或命中数。

页面哈希与清单一致；Node对真实内嵌载荷执行20种折/时限/起报轴视图与两震级日期/事件对应检查，
以及六对比×五预算×两判定切换，通过。仍只称数据/交互逻辑核验，不声称真实浏览器视觉验收。
原预测manifest哈希未变；地图恢复过程中每个实际报警成本与旧诊断相符。没有新的模型或效果分数。
科学价值复审：必要的可视解释已完成，停止本轮展示扩展，回到归因与震序/区域效应解释。
