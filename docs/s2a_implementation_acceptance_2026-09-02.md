# S2-A有限实现验收

日期：2026-09-02 22:55（北京时间）。结论：`PASS`，本实现提交推送后允许启动冻结的真实历史计算。
协议提交`5ffd1013640ccea220a23cc3e47dcb4e163e8f95`已经远端闭合；协议参数与输入身份没有修改。

## 1. 可执行范围

`src/seismoflux/multitask_s2/fault_geometry.py`实现两套白名单几何、公里距离、面积归一和39块粗化。
`fault_predict.py`只读复用旧因果核和外折多尺度目录，按更早I2/I3选六个新模型，逐折逐时限保存。
`fault_score.py`在全部四折396个预测对保存并验证后评分；四参考原样读取，只有六新模型计算新分数，
严格执行11个已登记配对，不运行旧C2B全流程。入口为`scripts/run_multitask_s2a.py`。

预测/评分共用一个操作系统持有的运行锁；完成的产物只能核对/复用，未封口产物只有在重算内容
一致时才能续接，否则停止并保留原件。评分分折保存小检查点，不因中断整组重算。输出限S2新目录。

## 2. 必要验证结果

- 主代理完整运行协议、几何、预测与评分聚焦合成测试：**71项通过，9.51秒**；Ruff及差异检查通过。
- 几何：投影米/公里一致、重复线不改变最近距离、全国质量与逐块质量守恒、真实面积粗化、α0/1
  精确保留端点，以及字段白名单与固定映射验证。
- 预测：I2只有I1时明确K75回退、I3更早块选择、目标结束和可见时间截断、有限候选/平局/空块，
  以及2格合成的四折五时限396对、六模型保存与恢复；旧产物内容和修改时间不变。
- 评分：四折未全时不读取目标，原参考复用、同目标/空期/预算、α0一致、仅11对、未封口恢复及LF JSON。
- 独立只读科学复审分别检查几何、预测时序和评分接线，均未发现阻断试验的实质问题。
- 未运行真实S2预测、评分或图件；合成测试只证明接线可用，不能称断层资料提高了预测效果。

## 3. 科学价值与紧邻工作

本次为**必要使能**，下一动作是运行已经冻结的有限对照，不追加数据清洗平台或参数搜索。
继续保持四外折、五时限、两震级、五面积预算和原独立首锚点，不读2020—2022留出、2023+审计或
锁定测试，不改变P1、旧S1及science_first的Stage4草稿。2026断层快照的历史描述性边界必须伴随成绩。

默认2折线程、数值库1线程；启动器设置BelowNormal。运行前只读确认没有重复实例，机器24物理核，
至少保留2物理核。实际PID、日志和检查点进度写入`docs/restart_handoff_2026-09-02_s2a.md`。
所有真实预测完成后统一评分；形成结果立即给出新增/丢失震区、分时限/震级效果及精粗对照，随后
静态图、离线回放与科学复审一起验收。展示代码可并行准备，但不能反向改变模型或结果。
