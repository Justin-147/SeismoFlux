# S2-C应变有限协议验收

时间：2026-09-03 02:20（北京时间）。结论：`PASS_PROTOCOL_ONLY_NO_NEW_PREDICTION_SCORE`。
精确入口为`configs/multitask_s2_c_strain.yaml`及`docs/s2c_strain_methods_2026-09-03.md`。

已完成原始公开应变文件预检、官方单元/第二不变量定义核对与独立科学复审。145,086原变形单元
按0.25°×0.2°范围面积映射；四新模型、六配对、五alpha，无平滑尺度或底噪。保留真零，空块与
负无穷得分区分。只用更早I2/I3选择混合参数，原目录只读复用，四折全预测保存后才评分。

四项不读取真实数据的协议检查通过；新增检查文件的Ruff及文档差异检查通过。独立复审确认面积
计权、支撑/强弱对照、零质量、时界与2015—2019预定分列无阻断。测试只证明定义与约束一致，
不是新数据有效的证据。本轮尚无S2-C实现、训练、预测或评分产物。

本次价值是直接检验新数据的必要准备；方向没有偏离，不新增GNSS处理平台、不下载87MB替代
数据、不重估完整性或重算旧结果。当前危险性上游时间不明，未混入应变实验；不阻塞应变及S3。
接下来只新增应变空间层、真零局部适配与旧预测/评分复用，必要实现验收同步后立即运行。

协议提交推送后允许最薄实现；尚不允许直接启动真实实验。旧目录/S2-A/S2-B源码与产物不改，
2020—2022留出、2023+审计、锁定测试、冻结P1及Stage4草稿仍不触碰。默认2、最多3折线程，
单数值线程、BelowNormal、至少留2物理核。02:13:32核验无项目Python、两CPU12%/0%（约6%）。

最新运行和同步状态仅见`docs/restart_handoff_2026-09-03_s2c.md`，不得把协议验收当全部S2完成。
