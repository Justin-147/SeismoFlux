# 阶段 2R-0 验收记录

- 阶段：2R-0（局部支持域无评分预登记）
- 协议版本：`0.2.1`
- 门控名称：`G1-LS`
- 验收日期：2026-07-14
- 本地状态：通过
- 远端状态：完成
- 冻结标签：`v0.2.1-background-local-support-protocol`

本验收只冻结局部支持域的构造、输入身份、科学边界和评分前阻断。它不表示任何背景模型已拟合或通过 `G1-LS`。本阶段没有模型分数、信息增益、命中结果、模型选择或 `Score ID`，锁定测试未运行。冻结标签已存在且指向本阶段实现提交，因此允许开始 2R-1。

## 冻结身份

| 项目 | 身份 |
| --- | --- |
| 协议配置 SHA-256 | `85c7c05f353e25d742e1d4d694012205bba8fa3a1b6f4e40722bbd17ad92fd1c` |
| 协议规范指纹 | `5dd31a212f4894625cf9db1bbfb1de3a4861a9bcffaeef9676c06707c03eceb8` |
| 支持域清单 SHA-256 | `632278416dfc717dbcb9d2eae048a4f13cdf7737a31e6e5e704a9dd17d7cef8d` |
| 支持域清单内容地址 | `local-support-bundle-69cecbee9093a21d` |
| 折清单 SHA-256 | `d7ae5266c9143ed0a67a9954da52039b2753f108698fb05477466a6d5b934e38` |
| 诊断 SVG SHA-256 | `7ba347818692eb022c605df1d8b2f10b2e8b6b006fd32094cd17920f5c93a2b2` |
| 诊断 PNG SHA-256 | `777f316425470ad984e9e89bd9a9c0f2ba22c106458f73a2911efe1566e94286` |

五个因果快照的 `support_id` 依次为：

1. `fold_1`：`local-support-f06e7c7496ea2357`
2. `fold_2`：`local-support-eaee903b28c55ace`
3. `fold_3`：`local-support-f86126dbec5bb79b`
4. `fold_4`：`local-support-788851371baf0e3b`
5. `final_validation`：`local-support-f6816ab6c6581306`

## 科学与治理验收

| 检查 | 结果 |
| --- | --- |
| 因果时间 | 每个快照同时要求 `origin_time_utc <= fit_end_utc` 和 `available_at <= fit_end_utc`；后期目录不得回填早期快照 |
| 空间局部性 | 空间原始 Mc 高于 4.0 只排除对应固定 500 km 基础格；其余区域继续评估 |
| 全局停止 | 时间原始 Mc 高于 4.0或没有合格时间层均在任何模型拟合和评分前停止 |
| 稀疏格 | 不足 200 个事件的基础格只继承固定 1000 km 父格；父格仍稀疏时保留为 `indeterminate` 并使用公共 Mc |
| 面积门槛 | 五个快照分别保留 `97.344749%`、`100%`、`99.720586%`、`100%`、`100%`，均通过包含边界值的 95% 门槛 |
| 共同支持 | 同一快照三模型必须共享 `support_id`、公共 Mc、目标集合、有效面积和补偿积分域 |
| 全区域指标 | 后续全区域召回、Molchan 和报警面积主表仍包含无支持格目标，并将其计为未覆盖 |
| 公开数据边界 | 清单不含研究区边界、格网裁剪几何、坐标或 WKB；执行时从封印的本地研究区重建并核对固定格身份 |
| 内容寻址 | `0.2.0` 严格保持原 7 个输入；`0.2.1` 严格要求第 8 个支持域清单输入 |
| 评分阻断 | 主流水线、Poisson/KDE、ETAS、时窗回测、生产运行器、评分交付物构建和评分产物发布入口均在读取评分输入前拒绝 2R-0 |
| 父结果不可变 | 原 `0.2.0` 配置、注册表和报告 SHA-256 分别保持 `d845f7...5e24b`、`34ff1f...81be3`、`358a82...e1f6c`，执行指纹保持 `f386f0...f9032` |

## 本地验收证据

| 项目 | 结果 |
| --- | --- |
| 全量非锁定测试 | `531 passed` |
| 代码检查 | `ruff check .` 通过 |
| 格式检查 | `ruff format --check .` 通过，95 个文件已格式化 |
| 类型检查 | 项目配置的严格 `mypy` 通过（`src`、`tests` 共 93 个文件）；扩展 `mypy .` 也通过（95 个文件） |
| 支持清单确定性 | 由封印原始输入逐字节重建并通过 `--check`；文件 SHA-256、bundle ID 和五个 `support_id` 不变 |
| 图件确定性 | 重新渲染后的 SVG 和 PNG 与验收前逐字节相同 |
| 公开内容扫描 | 未发现 WKB、边界几何、坐标、原始行级数据、密钥或大文件 |
| Git 差异检查 | `git diff --check` 通过 |
| 锁定测试 | 未运行 |

## 独立审计关闭项

- 已从公开清单和图件移除可还原受限研究区的派生几何，并新增本地几何身份重建校验。
- 已封锁 CLI 第八输入覆盖、时窗评分入口、生产运行器及评分交付物构建/发布绕过路径。
- 已统一“无合格时间层即全局失败”的代码、配置、协议和运行时测试。
- 已补强严格清单的事件数、来源、状态、父格、时间块、有限 Mc 和候选映射校验。
- 已处理研究区闭合外边界恰与固定格线重合的测度零事件；内部格线仍保持高侧优先。
- 已将生产运行器调整为先做无数据协议授权预检，再绑定受限研究区和完整输入，并在完整加载后重复授权校验。
- 公开 CI 的清单测试只读取公开内容；生产加载器仍对受限研究区文件 SHA、重建几何和固定格身份执行硬校验。

## 远端完成证据

| 项目 | 远端回读结果 |
| --- | --- |
| 冻结实现提交 | `966fb4e84c36aba373d90b81fe9e1350ffe349b6` |
| 跨平台 CI 修复提交 | `32744fdae7daedf73c2a1e57d78ca1aa9b08c776`；不改变协议、科学阈值、支持清单或冻结标签 |
| 阶段分支 | `origin/codex/stage2-local-support` 包含冻结实现、验收记录和 CI 修复 |
| 带注释标签对象 | `06136e22bb8c6e2606a9debd5e00d53b500f758d` |
| 标签剥离结果 | `v0.2.1-background-local-support-protocol^{}` 指向实现提交 |
| 草稿 PR | <https://github.com/Justin-147/SeismoFlux/pull/6> |
| 首轮 CI 诊断 | [运行 29336207596](https://github.com/Justin-147/SeismoFlux/actions/runs/29336207596) 仅因公开检出缺少被忽略的受限研究区文件而有 2 个测试失败；其余 529 项通过 |
| 修复后 CI | [运行 29336935742](https://github.com/Justin-147/SeismoFlux/actions/runs/29336935742) 的 Ubuntu 与 Windows 质量检查均通过 |
| 冻结前运行状态 | 未运行 2R-1 模型拟合、评分或锁定测试；`Score ID` 数量为 0 |
