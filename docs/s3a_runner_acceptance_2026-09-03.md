# S3-A真实训练接线与评分原语

日期：2026-09-03。当前为运行前接线验收记录，不是新模型效果报告。

## 科学任务

使用已准备的153期历史异常报告与各起报时刻可用的地震目录，比较目录参考、报告覆盖控制、
异常现状和异常变化趋势。地点模型调整网格相对强度；次数模型调整完整时窗的预期地震次数。
两个正式震级带共享次数乘子，本轮不宣称单独学会震级分布。

## 本次接线

- `multitask_s3.runner`读取准备缓存，不重新计算目录核或重新处理4.09GB异常库。
- 保持两A开发折、7/30/90/180/365天、既定特征/模型/参数候选。只在训练和内验证期构造标签，
  各自使用正确的可用时间截止；预测不评价外层目标。
- 每折依次保存五个时限，两个折最多并行两线程。每个时限保存后更新可恢复检查点。
  所有十个块完整保存并核对后，另行评分；不看一折效果再修改另一折。
- 365天在本轮两年度外折没有完整评价窗，保存明确NA和空预测数组，不虚构零成绩，
  不为无法评价的窗口额外拟合。后续长时限研究另用预先规定的合法长分段。
- 纯评分组件提供固定报警面积下严格及70km辅助召回、空间对数评分、包含零震窗的次数评分、
  Brier和计数偏差，以及同起报配对的新增/丢失命中。没有效果采用硬门槛。
- 当前纯评分组件不等于完整评分装配：震序等权、区域细分、完整不确定性/置乱及图件仍待接入。
  逐事件诊断只留本机，不能直接把含`_local`的评分字典上传公开仓库。

## 验收状态

178项S3相关合成/配置检查通过，其中本轮34项纯评分、12项接线检查通过；Ruff及四个新增文件类型
检查通过。独立只读科学接线审查通过；真实外层成绩尚未读取。代码已于11:55:55推送回读一致，
提交`4b69df56cc959f0d162b31b01f7d9308ed8d6852`。准备11:58完成，随后唯一真实拟合已启动；
当前进度见最新交接，尚不构成S3效果验收。
重点核对训练时间截止、不得读取外层目标、两个震级共乘子、空评价NA、完整预测前不得评分、
中断恢复不混合不同缓存。不因本次检查增加模型候选、数据范围或评价标准。

## 科学价值复审

这是必要使能，尚无预测提升证据。它直接通向异常是否有用的实际比较，没有转向通用工程平台。
本轮不重新讨论已关闭的问题；下一项是取得并解释训练/较晚开发表现，再完成原定归因对照。
对可复核的小幅或局部改善予以肯定，按适用任务保留，同时报告新增命中和丢失命中。

运行目录、检查点、进程和下一恢复动作以 `restart_handoff_2026-09-03_s3.md` 为准。

## 追加：初步真实评分接线（12:10）

全部真实预测已于12:01完成并读回核验；24个可拟合组合均完成拟合，无失败。
新增`score_runner.py`直接读取不可变预测，不再训练。先验证全部十块才读取外层目标，
分别统计主间隔起报与全部报告描述回放；按折、时限、震级汇总和配对，保留零震窗口及NA。
70km邻域沿用冻结等面积投影与格多边形距离，报警成本不扩大。

185项S3相关合成/配置检查通过，Ruff及新增评分入口/测试类型检查通过；第二次独立只读科学接线
审查通过。没有读取真实外层成绩。提交推送后可运行单进程初步评分，不需要重复拟合。
新增两张汇总静态图脚本固定展示四个可评价时限及两个震级带，尚待实际结果生成后可视核对。

这次仍是必要接线，不是效果提升。真实初步得失出来立即复审，剩余震序等权、区域细分、归因对照
与交互展示不冒称已完成。初步科学结论可明确标注范围后报告，不必等完整大包才告知用户。

## 追加：真实初步效果与静态图验收（12:28）

全部主评分于12:17:13完成；保留主间隔起报与全报告描述两套汇总。评分前后预测manifest哈希一致。
独立只读科学复审已完成，详细结果与增量采用决定保存在本机`outputs/multitask_s3/s3a_score_v1/SCIENCE_REVIEW.md`。
三张PNG/SVG已生成并可视核对，包含全部预定面积的正负净命中，不只展示有利结果。
新增绘图代码Ruff通过；只是读取汇总作图，没有重训或修改预测。机器12:28 CPU4%，无相关后台任务。

这次已形成直接的局部科学效果证据和需保留的局限，不再只是实现使能；没有偏离多数据/三任务目标。
后续科学动作是完成冻结归因与震例回放，不继续重复工程装配。当前完成仅限初步效果，
交互页面、震序均衡/区域分层、200+200归因和完整S3验收仍未完成。
新派生成绩、结果说明、图和逐事件诊断留本机，本次未作为公开载荷暂存。

## 追加：震例追溯与时间归因组件（12:46）

本次不改变冻结模型或主评分，新增纯事件对比账本、受限元数据装配、时间反事实重建和两基础列
读取。已有六种空间对比的所有预算、严格/70km、首震/后续、主起报/描述起报均保留；分类包括
新增、丢失、共同命中、共同漏报。唯一地震数、重复曝光数、展开对比行数分别记录。

时间置乱保持覆盖及其NaN在接收方，九快照与年龄/熵缺失随来源同移，两radius基础列同移后
沿完整伪时间轴重算三动态与动态缺失。原有实际日历、分段边界、3观测与13/4周公式均不变。
小格分块仅控制内存，不改变方法。两基础列仅读精确请求的合法row groups，禁止先全读再筛选。

221项S3相关合成检查、Ruff通过；时间重建经独立合成数值复核，与原trajectory逐值一致。
这属于解释现有局部改善、完成原定归因的必要使能，不声称产生新提升或已完成置乱复本。
不引入显著性/最小改善/全部折一致门，不再讨论已解决审计。完整S3验收仍未完成。

本次真实case_runner已在提交推送后于12:48完成，记录原预测/评分/诊断身份，没有重训或重算命中。
本机CASE_REVIEW及全部改变结局震例静态PNG/SVG已生成，图经合成矩阵核验和实际视觉核对。
图保留全部预设面积以及新增、丢失、共同命中、共同漏报和不在该主轴五种情况；不以展开网格
数、多个预算或多个时限夸大事件数量。科学复审发现的局部支持与解释限制只在本机结果说明中。
此次推进为已有效果的具体震例证据与解释，不是新增独立验证；归因复本、分层和交互仍待完成。

## 追加：冻结归因运行接线（13:17）

已补齐空间九快照/两基础序列/三动态及缺失控制重建，直接复用既有200km聚合与时间公式；
同一期、同静态区及inside/outside层双射坐标，覆盖不动。只读显式授权的状态row groups及分区映射。
状态支持一期多个row groups，无坐标实体保持在原状态中但不参加坐标双射。

薄执行器固定两折×时间/空间各200×五时限，保存4000块，365天明确NA。空间同复本跨h复用
同一场；时间按h的冻结分段分别重建。每次直接调用冻结S3拟合，重新训练内填补/标准化/内选择，
不重新计算目录背景，也不访问外层评分。所有置乱预测保存/失败登记后才接统一评分。

259项S3相关合成检查、Ruff与独立只读科学接线审查通过。已存块不覆盖；中断恢复身份相同；
数值失败如实登记不删/补抽，文件/内存/检查点问题按中断处理，不虚构科学失败。
启动前做恒等输入重建对比以核实新装配没有改变原特征。这不是新模型或新的科学采用门。
科学价值是解释已有局部进步来源的必要使能；实际归因结果、完整S3-A验收仍待运行完成。
