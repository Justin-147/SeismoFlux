# S3-A训练与目标组件验收

日期：2026-09-03。状态：纯内存训练与标签组件已完成，真实异常模型尚未拟合，外层效果未评分。
最高科研采用原则为蓝图1.22；本次仅为紧接真实科学比较的必要实现，不是预测效果提升。

## 本次实现

- `multitask_s3/training.py`：覆盖、快照、动态三个冻结设计，按内层训练拟合缺失处理与缩放；
  空间Ms4条件ridge及次数Ms5+ offset-Poisson分别选λ，候选仍为0.1/1/10；
  有效内块不足两个时按原协议用10。这个样本规则仅控制选参，不是效果采用门槛。
- 空间按事件池化内验证损失，次数按起报池化、保留零震窗；各内块独立拟合预处理。
  无可训练样本或数值不可评价按预定参照回退，并明确标记，不能称科学负结果。
- `multitask_s3/targets.py`：只在调用者给定成熟截止内形成(T,T+h]目标，分别给出Ms4空间计数、
  Ms5+次数与两正式震级带的首震/后续标识。采用现有S0固定首震算法，不重新设计震序定义。
- `preparation.py`为全部真实A报告期生成因果目录参考与20列输入缓存，模型间复用；
  本轮准备代码已推送（a4165f8），11:32开始单进程2工作线程运行，逐期数组只留本机。

## 核对与边界

132项S3相关合成/配置检查通过，包含52项新增训练和标签检查；Ruff通过，两个执行者的新增纯组件
Mypy通过。核对了时间边界、可用时间、真实空窗、不因格号缺失减少分母、训练期预处理、内层池化
选参、仅截距参照及小增益不作硬判失败。这些检查仅证明实现符合当前试验，不替代真实效果。

模型、源输入、A开发折、五时限、震级档、面积预算、指标及200+200置乱规模均未改变。
没有读取A审计、C留出/审计分数或锁定测试；没有修改旧结果与Stage4草稿。

## 科学复审与下一步

这是必要使能，尚无直接效果证据；没有偏离多数据与三任务路线；复用已验收数学和因果输入，
不新增通用平台。下一步把准备缓存、目标和训练组件接成薄执行器，先保存全部外折预测，再统一
评分与归因对照。评价肯定可复核小幅/局部进步，按适用任务采纳，不引入统一显著性否决。

精确恢复入口、PID和日志见 `restart_handoff_2026-09-03_s3.md`，运行结束只续接下一环节，不重跑准备。
