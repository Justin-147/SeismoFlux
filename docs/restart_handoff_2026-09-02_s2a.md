# 最新交接：S2-A断层几何增量

更新时间：2026-09-02 23:38（北京时间）。当前：`S2A_SCIENCE_COMPLETE_REMOTE_CONFIRMED_NEXT_STATIC_PHYSICS`。
本文件接替`docs/restart_handoff_2026-09-02_s1c2b.md`；旧文档与结果保留。

## 1. 一句话状态

项目尚未全部完成。S1目录比较已验收推送，现有小幅地点提升保留；现在检验加入断层图能否少漏震区。
首个有限S2-A协议已经通过配置检查和独立科学复审，并完成提交推送。新断层层与只读复用
目录的预测/评分接线也已完成验收推送。22:59真实启动在几何读取时停止，尚未训练或生成预测。
已最小修正已标记不可用迹线的空几何读取顺序并通过回归、推送；同协议attempt2的全部预测已于
23:08完成（20/20检查点、396/396对），评分于23:17:37完成（4/4折），PID36948已退出。
主条件目录与两种细位置混合都是58/147；简化混合30天45万km²净增3、90天60万km²净增3，
但Ms≥6部分时限有退步。最终图/本机离线回放与科学验收均已完成，科学提交
`f2f1bf48e8cdf036a3f9b664430f26ddf48cd22c`已推送，23:36:48远端回读一致。
不再启动任何预测/评分进程；详见第12节和`docs/s2a_scientific_results_2026-09-02.md`。
验收见`docs/s2a_protocol_acceptance_2026-09-02.md`。

S0数据/任务已完成；S1有限目录基准已完成；S2断层/危险性/应变正在做首轮几何；S3异常、S4有限
机器学习、S5组合消融、S6综合稳健性和论文展示尚未完成；S7外部检验并行，不能阻塞历史研究。

## 2. 已完成证据和不再重做的事

- S1科学提交`544360e34b139581508deba5c62d14bccf46d535`，闭合`7164e3b8cd2b8af939f18e40337d6c89ac476295`，
  已推送并远端回读一致。主任务30天Ms5–6、60万km²，L3为54/147，多尺度58/147，年龄57/147。
  多尺度新增14/丢失10，差值区间跨零；这是开发期小增益，不能称最终确认。
- 95%/Ms审计已结束。两份本地目录原数值无转换、无因类型空缺删事件，不需全部重跑。C1被过严
  百分比门挡在预测前；已用C2A做实际固定参数敏感性，主结果54→52/53，没有稳定收益，分支结束。
- S1最终图和离线回放位于`outputs/multitask_s1/s1c2b_catalog_models_v1/rendered_v2`，不要重新运行。
- 不重开95%/80%比例、旧ETAS/负二项、C2A掩膜或C2B参数搜索。不可把未完成的S2写成已提升。

## 3. S2-A精确入口

工作树：`D:\AIPred\SeismoFlux\data\interim\worktrees\p2r_multitask_multidata`；
分支：`codex/p2r-multitask-multidata`；Python：`D:\AIPred\SeismoFlux\.venv\Scripts\python.exe`；
数据根：`D:\AIPred\SeismoFlux\data`。

续接先读总蓝图第1.9—1.14节、本文件、以下两个协议及研究起点：

1. `configs/multitask_s2_a_fault_geometry.yaml`；
2. `docs/s2a_fault_geometry_methods_2026-09-02.md`；
3. `docs/s2_static_data_research_start_2026-09-02.md`。

已有519简化段、7,215可用精细迹线，只取几何；2026收集快照是历史描述性研究，不改原历史不可用
标记，不混入上次强震年、当前离逝率、危险性或速率。复用15,697格和39个目标盲固定块。
六新模型：每源纯断层、目录+精细断层、目录+粗化断层；三个距离尺度、每混合13候选，不扩张。
内层I2目录明确按原规则回退K75，I3只用更早数据选择；混合只看I2/I3且标签在外折前成熟。
旧413份核和外折目录预测只读复用，有效输出目录为
`outputs/multitask_s2/s2a_fault_geometry_v1_attempt2`；无后缀v1仅保留预测前读入故障记录。

## 4. 最高优先级安全动作

先核对第13节远端闭合记录和当前git。S2-A已完成，不重新计算。现在转向
S2尚未用的断层物理属性及应变：先读既有研究起点和原始论文，检查现成属性单位/缺测，登记有限
单组件与目录混合对照。不继续扩搜断层几何；不新建地图清洗系统、PSHA平台或反复溯源工程。
不得启动旧预测/评分；旧完成产物、冻结P1、根目录旧修改和science_first的Stage4草稿不覆盖。

目前没有S2实现/运行PID。后续恢复必须先确认唯一进程和检查点，不按旧S1 PID重启。
2折线程默认、最多3折，BelowNormal、数值库单线程、至少保留2物理核。日常检查单进程。
不读2020—2022留出、2023+审计、锁定测试；不取未来目录，不改根库、冻结P1、science_first
的Stage4未跟踪草稿。代码/文档/聚合证据可公开，逐事件数组/案例地图/离线HTML仅本机。

科学复审：本次是新增资料有限检验的必要准备，尚无S2效果；方向仍是充分用数据提升预测，没有
偏离目标。今后的最小证据是相同面积下多命中/多漏几个独立震区，而不是新增多少代码或通过多少测试。
心跳保持每30分钟，阶段变化后同步更新其恢复入口，不能继续指向旧S1运行。

## 5. 22:36协议验收

六项配置检查通过、独立科学复审无阻断问题。协议SHA-256为
`d6e19dca67063030e8eafdfd766f13f2310a5e52790cc4b2b7fe8707cf58b5c9`。
22:33资源核验无SeismoFlux Python任务，两CPU16%/6%，总约11%，没有占满核心。
本轮先同步协议/方法/配置测试/验收/交接及蓝图，再分别实现几何层和预测/评分接线。
当前尚无S2预测器和成绩；不得把本节误当已可恢复真实计算。

## 6. 22:42已进入有限实现

协议提交`5ffd1013640ccea220a23cc3e47dcb4e163e8f95`已推送至
`origin/codex/p2r-multitask-multidata`；22:36:54远端回读一致。上节“待同步”由本节取代。

新模块位于`src/seismoflux/multitask_s2/`。几何模块已完成合成验证，预测与评分模块仍在实现，
不得因文件存在便启动真实计算。命令入口`scripts/run_multitask_s2a.py`分别支持predict/score，
输出仅限`outputs/multitask_s2/`；旧目录结果只读。必要实现测试/科学复核/验收提交推送后才能运行。
评分将按折留检查点，防止中断后整组重做。代码和测试开发不构成新的预测证据。

## 7. 22:55实现验收，待同步后立即计算

最小几何/预测/评分与CLI已完成，见`docs/s2a_implementation_acceptance_2026-09-02.md`。
主代理集成验证71项通过（9.51秒）、Ruff通过；独立科学复核未发现阻断问题。没有改变协议参数、
数据、原目录结果或目标口径。实现提交远端确认后，不再扩展实现，立即启动真实有限计算。

预测入口（从活动工作树、设置PYTHONPATH=src及数值库单线程后，由隐藏BelowNormal进程运行）：

```powershell
& 'D:\AIPred\SeismoFlux\.venv\Scripts\python.exe' scripts/run_multitask_s2a.py --phase predict --project-root 'D:\AIPred\SeismoFlux\data\interim\worktrees\p2r_multitask_multidata' --data-root 'D:\AIPred\SeismoFlux\data' --workers 2
```

只有`outputs/multitask_s2/s2a_fault_geometry_v1/prediction_manifest.json`完整并核验后，才将phase改为
`score`，逐折评分；两阶段共用同一run.lock，不能同时启动。还没有运行PID或真实检查点。
图件/交互薄渲染器正在另行准备，只用合成数据，不阻塞预测，不将草稿当最终交付。

## 8. 23:01首次真实读入停止：没有预测成绩

实现提交`c84f76324541d23b2028d5d15deb20f8a1ffa93e`已推送，22:58:21远端回读一致。
22:59:30隐藏BelowNormal进程PID36172启动，2折线程/数值库1线程；23:00:18检查时已退出，
未进入任何折训练，没有时限/预测manifest。日志：

- `data/interim/s2a/20260902_225930_predict.stdout.log`；
- `data/interim/s2a/20260902_225930_predict.stderr.log`。

原因是`fault_geometry._source_lines`在筛选`usable_for_geometry`之前，要求全部TRACE记录的WKB
非空；原协议已经明确7,216条中只使用7,215条，但不可用记录仍被错误检查。当前为实现读入错误
`invalid_run_before_prediction`，**不是断层模型负结果，不改变任何科学候选或数据**。

最高安全动作：只修“不可用记录先排除，再检查参与计算的几何”的顺序，新增最小回归并验收推送；
保留`outputs/multitask_s2/s2a_fault_geometry_v1`的身份/缓存目录记录和旧日志，不覆写。
修复后的同一冻结协议使用新输出`outputs/multitask_s2/s2a_fault_geometry_v1_attempt2`，不改协议
参数、输入哈希或范围。启动前再确认无重复进程；不能按PID36172盲目重启。
23:00资源核验无相关进程，两CPU3%/9%，总约6%。心跳继续；旧S1仍不重算。

23:03修正验收：真实白名单核实只有1条不可用记录WKB为空；代码只移动非空几何检查到可用行，
原7,216/7,215计数不变。73项聚焦回归通过（9.14秒），Ruff通过，主代理已审最小差异。
修复推送后用相同命令加`--output-root outputs/multitask_s2/s2a_fault_geometry_v1_attempt2`启动；
不要再使用无预测的旧v1。修复提交与新PID由下节更新，当前仍未启动attempt2。

## 9. 23:05修复远端闭合，同协议attempt2启动

修复提交`96c7cc1fe1f8f5200d9c1d8a8ed7cad381ad43dc`已于23:04:19推送并远端回读一致。
23:05:07确认无重复实例后启动PID36060，隐藏窗口、BelowNormal、2折线程/数值库1线程；机器仍为
24物理核。旧PID36172已退出，不能再使用它；旧v1三份记录原样保留。

当前唯一运行目录：`outputs/multitask_s2/s2a_fault_geometry_v1_attempt2`；日志：

- `data/interim/s2a/20260902_230507_attempt2_predict.stdout.log`；
- `data/interim/s2a/20260902_230507_attempt2_predict.stderr.log`。

恢复时先确认当前进程及日志/检查点。输入核验阶段暂时还没有折日志是正常的，不能据此重复启动；
只有进程已退出且预测未完成时才按原协议和attempt2路径安全续接。20个fold×horizon全部完成，
再核验`prediction_manifest.json`，运行score时也必须显式使用相同attempt2输出路径。
当前还没有统一评价，因此不得报告断层模型已经有效。最终科学证据由同面积新增/丢失震区决定。

薄展示器`scripts/render_multitask_s2a.py`和合成测试已完成，根代理6项复测通过（14.94秒），
含1,000组合的离线交互；目前仅合成演示，没有真实S2效果图，尚待结果阶段提交。

## 10. 23:08预测全部完成，23:09统一评分启动

四折各五时限、99个起报—时限对已完成：20/20检查点、396/396对，预测进度100%。
预测进程PID36060已正常退出；最终manifest于23:08:00写入，SHA-256为
`5744cefc88dd83f6d71b981f9bf96faff0dbcea2b09ea1184ac934e45f15bef4`。
运行自身已核验全部四折预测，未在保存前读取外折评分或目标。真实几何audit确认519段和7,215迹线，
原字段/快照标记和39块映射未改。不要重新训练或回写预测。

23:09:36确认无相关进程后，唯一评分PID36948启动，隐藏BelowNormal、单进程数值库单线程。
运行路径仍是`outputs/multitask_s2/s2a_fault_geometry_v1_attempt2`。日志：

- `data/interim/s2a/20260902_230936_attempt2_score.stdout.log`；
- `data/interim/s2a/20260902_230936_attempt2_score.stderr.log`。

评分先核验全预测和旧参考，再读取同一C0目标；参考不重算。各折完成后保存
`score_phase/folds/<fold_id>/fold_score_manifest.json`，最终是
`score_phase/score_manifest.json`。整个折计算过程中日志可能暂时不变，必须结合进程CPU和
新检查点判断是否活跃。当前无最终评分；不能把20/20预测完成当已证实断层增益。
恢复时若评分仍活跃只监测；若已退出且最终评分未完，先核对无重复实例，再在同一个attempt2路径
执行`--phase score`续接，不改源码/协议/输入、不重复预测。

23:06:58的预测阶段资源样本：唯一进程CPU占整机3.44%、内存538.5MiB、BelowNormal；两CPU5%/12%，
总约8.5%。这不是评分当前值，后续以新采样为准。

## 11. 23:12真实内层训练证据（不是较晚评价）

独立只读核对20份训练记录：I2目录均明确K75回退20/20；I3正常选择balanced 10次、fine 10次。
I2更早训练50—384个Ms≥4事件/4—43起报，I3更早训练103—1028事件/8—87起报；断层选择的
两验证块合计103—1132事件、8—87起报。范围随时限/折变化，不能相加当独立样本。

| 新混合模型 | α=0（不加） | α=0.25 | α=0.5 | 内层相对原目录的平均对数密度差 |
| --- | ---: | ---: | ---: | ---: |
| 简化断层精细＋目录 | 1 | 18 | 1 | +0.02985 |
| 简化断层粗化＋目录 | 7 | 13 | 0 | +0.01181 |
| 详细迹线精细＋目录 | 15 | 5 | 0 | +0.00467 |
| 详细迹线粗化＋目录 | 17 | 3 | 0 | +0.00331 |

单位为每事件自然对数空间密度；先非空块等权，再概括20次。六模型120次选择都有标签，未使用
空标签默认。包含α0的候选选择本就偏向非负内层差，因此不能拿此表当召回提升或独立确认。
通俗看，较早资料更常支持加入简化断层，详细迹线多数保留原目录；最终有无帮助要看正在计算的
较晚评价。现代快照只能支持历史描述性增量。没有偏离科学目标，不因训练好看追加新参数。

## 12. 23:34最终结果、图件与科学验收完成，待远端闭合

评分于23:17:37完成，唯一评分PID36948正常退出；四折检查点分别为23:12:02、23:13:48、
23:15:32、23:17:15。396/396预测与4/4评分均100%，没有需要续算的后台任务。
独立复核六评分产物SHA、1,000条件、1,100配对、原分母/空期与实际面积全部通过。
结果全文：`docs/s2a_scientific_results_2026-09-02.md`。

关键科学结果：主条件30天Ms5–6、60万km²，目录与简化/详细细位置混合均58/147；简化和详细
各新增1/丢失1，简化粗化57、详细粗化58；纯断层仅16/15，不替代目录。简化混合30天45万km²
49→52/147（新增3/丢失0、四折+1/0/+1/+1）；90天60万km²76→79/202（新增4/丢失1）。
Ms≥6的180天60万km²19→17/52，正负结果都保留；不同条件不相加冒充独立成功。

科学决定：有任务局部的小幅描述性增益，主指标没有提升。多尺度目录仍为主要基底；保留简化
混合及粗化，停止本轮纯几何扩搜，转向其他物理信息。没有偏离目标，也不做无关工程。
这一结果不是新的独立测试，不证明时间、震级概率或真正未来效果。

最终展示位于有效运行的`rendered/`，23:19:52完成，四张真实PNG已由主代理目视确认；主图、
面积曲线、净增拆分可公开；正反案例地图与离线HTML含事件，仅本机。两个案例是2010-11-18
起报的新增和2018-08-16起报的丢失，按固定主条件选，展示不改变模型或保存命中。
最终全S2-A聚焦复测79项通过（23.47秒），Ruff通过。真实交互独立只读QA通过：九产物SHA、
1,000组合、133,300条命中与评分逐条一致；792个起报—时限—震级键及466空键保留。
日期微秒/北京时间正确、无远程资源；Node模拟DOM/Canvas而非浏览器像素检查，边界已披露。
科学验收为`docs/s2a_scientific_acceptance_2026-09-02.md`，已允许显式白名单提交推送。
只公开summary/score_manifest及前三组聚合PNG/SVG、代码和文档；事件图/HTML/数组仅本机。

评分manifest SHA：`cc14d9d3d820bbf8989a5e3a1dec22edba19e91676559a765545d20ad91354e6`；
summary SHA：`ef2a3c3d0a822a7e328e5aa95aedeac24fbdc3c941a80216941756e6a40b8e8a`；
展示manifest SHA：`5f1563ac0c6ca6bb8527f0dfe697bc1b0a8fa206c7ccd665ae2a8fb1a9915ce6`。

23:22资源核验：无SeismoFlux训练/评分/渲染后台进程，两CPU6%/17%，总约11.5%；24物理核仍保留。
随后仅单进程聚焦测试，数值库1线程。根工作树旧修改和science_first的Stage4未跟踪测试仍在，
本轮未编辑或暂存。心跳保持30分钟，最终验收后更新为“下一S2数据”，不能误重启已完成S2-A。

## 13. 23:38科学阶段远端闭合，下一安全点是物理属性研究

科学结果提交`f2f1bf48e8cdf036a3f9b664430f26ddf48cd22c`已推送至
`origin/codex/p2r-multitask-multidata`，23:36:48远端回读一致。白名单仅14项代码/测试/文档/
聚合JSON/前三组聚合图；含事件地图、离线HTML和数组保持本机。公开JSON暂存字节与磁盘完全一致。
本阶段完成了必要测试、独立科学复核、最终图件/交互、验收、提交推送，不再是等待评分或同步。

续接顺序：

1. 不重启S1/C2A/S2-A，先读本文件、总蓝图1.15及S2-A科学结果第6节。
2. 以`docs/s2_static_data_research_start_2026-09-02.md`中的国内滑速/GPS与国际混合研究为起点，
   先核对经典方法与现成断层属性；有具体新公式时阅读其原始来源，不凭空造规则。
3. 只读核对已处理属性的单位、缺测和空间覆盖，区分滑动信息、综合危险性、上次事件/2026离逝率。
   不以95%/80%全国覆盖一票否决，不因用户目录震级类型重新跑旧结果；两份目录默认Ms。
4. 写下一份有限物理属性/应变比较方案，单组件与同一目录混合分别检验，保留任务专用增益和负结果。
   若现成属性不适用则明确原因转向可用应变资料，不陷入自动断层关联或字段补全工程。
5. 下一有限协议先必要检查、科学复审、验收、提交推送后才计算；任何小增益都按同预算震区得失判断，
   不等待某个未来日期，不读2020—2022留出、2023+审计、锁定测试，不改变冻结P1。

全部S0—S7未完成：S0数据/任务、S1目录与S2-A几何已完成；S2其余静态物理信息为下一项，S3异常、
S4有限学习、S5组合消融、S6稳健性与论文/PPT/可编辑poster仍需研究；S7外部检验并行。
心跳继续每30分钟，必须报告真实效果或明确无新效果、全工作线、偏差纠正和资源；下一次不是重跑本轮。
