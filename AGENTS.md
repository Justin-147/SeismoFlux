# SeismoFlux development rules

## 当前最高科研与汇报准则（用户于2026-09-03明确）

这是逐步探索地震预测的科学项目，不以其它领域的高准确率或工程成熟度作为采用门槛。
在相同样本和报警成本下，相对合理随机、合法基线或前一版本的可复核小幅进步，必须明确肯定，
按适用的时间、地点、震级或地区任务采纳为当前工作模型、组合成分或后续候选；不得因为未达
统一显著性、跨全部折一致或预设最小增益而直接否定。单折/局部收益可以保留，注明证据范围。
不同参照的意义要说清：胜过随机是基础能力，胜过当前基线才是相应任务的进一步提升。
采用探索结果不等于宣称普遍有效或独立确认；如实保留负变化、不确定性、时间因果和测试隔离。
实施以唯一蓝图第1.22节为准，旧的泛化硬门若冲突，不再作为当前停止依据。

心跳只报新增进展、正在做的事、下一步及当前阻碍；S0—S7用一行定位。
先读 `docs/SCIENCE_DECISIONS_AND_REPORTING.md`，已解决并已汇报的事项不再每次复述，
除非有新证据、问题复发或用户询问。没有变化时简短说明，不重放历史成绩和旧问题清单。

1. 始终使用中文向用户汇报，代码标识符使用英文。
2. 首要目标是充分利用已有与可合法获取的数据，提高未来地震时间、地点和震级的外推预测效果；受控报警面积下的独立物理地震区域召回是地点任务主指标之一，不是唯一任务。
3. 任何起报日不得使用其后的异常报告、地震或人工结果。
4. 不得使用真实震中生成推理候选、空间加密位置或区域边界。
5. 人工预测地点、震级和时间默认禁止作为训练特征或标签。
6. 动态异常必须由全部历史报告期重建，不能只读取最新一期。
7. 所有异常模型必须与可评价的 ETAS 背景比较；ETAS 不可评价时，与当前最佳合法背景模型比较，并始终做异常置乱对照。
8. 锁定测试只运行一次，失败后不得针对测试集继续调参。
9. 每一阶段必须先完成测试、验收、提交和推送，再进入下一阶段。
10. 不覆盖历史前瞻预测，不根据后来是否命中选择保留版本。
11. 相对强度、评分和顺位不得称为绝对发震概率。
12. 先建立简单模型参照，再按现行S4方案公平试验有限代表模型；不用简单模型尚未获胜否决一切非线性探索，不开展无界大网络搜索。
13. 大型任务使用受控多核，默认至少保留 2 个物理核心。
14. 每次汇报简述完整工作线和当前阶段，重点展示本次科学变化与下一步；已关闭事项按汇报记忆去重。
15. 不复制 LocationPred 的旧模型和脚本，只迁移原始输入和只读基准。
16. 每个阶段性结果形成后必须立即完成科学价值复审：说明它对最终预测目标是直接提升、必要使能还是没有实质推动，并给出证据；没有实质推动时停止继续堆叠工程，先复审和调整方案。
17. 不同数据覆盖期分别使用其最大合法样本；评价新增数据贡献时，必须在共同覆盖期对同一批起报、目标和成本做配对比较，不得直接比较不同覆盖期的原始分数。
18. 长历史目录不得因短覆盖异常或形变数据而被整体截短；短覆盖数据应在长历史目录的因果背景预测上学习增量或残差。
19. 地点、时间、震级分别训练和评价，再检验联合模型；每类数据和模型都要做适合任务的消融，肯定并采纳可复核的小幅、局部或任务专用提升，不以跨全部折稳定为前提。
17. 合成数据、代码测试、运行稳定性、封印和界面完成度只能证明实现可用，不能称为真实预测进展。
18. 历史开发采用分级证据：可复核的正提升进入后续积累，不因样本少、单折或未达到单一硬阈值自动判零；报告效应大小、适用条件、正负变化、不确定性和置乱对照，后两者不是一票否决。
19. 每次阶段汇报必须通俗说明使用的数据、处理和时间边界、特征、模型、训练效果、时间外推/测试效果、未用数据及下一项直接科学检验。
20. 工程工作只允许直接解除最近一次真实科学检验的阻断；在形成预测成绩前的实现故障记为无效运行，不得误称为模型负结果。
21. 每个阶段和长任务都要及时更新当前状态、后续计划、数据/产物位置和精确恢复入口，保证重启或网络中断后可以继续。
22. 凡涉及震序划分、去丛集、目录完整性、时空窗、标签、特征、模型、统计检验或评价等科学处理，必须先调研并记录国内经典、国际经典和当前主流方法的来源、适用问题、局限及与本项目任务的差异；优先采用已有可靠方法。确需自定义时，只能把它登记为待检验假设，并在相同样本、时间边界和评价成本下与至少一个经典基线比较，不得把临时自创规则直接当成权威标准。

本文件受 `SEISMOFLUX_IMPLEMENTATION_HANDOFF.md` 约束；发生冲突时，以该唯一实施蓝图为准。
