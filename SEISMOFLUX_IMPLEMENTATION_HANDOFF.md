# SeismoFlux 从零搭建交接实施文档

| 项目项 | 约定 |
| --- | --- |
| 项目英文名 | **SeismoFlux** |
| 项目文件夹 | `SeismoFlux` |
| 建议本机路径 | `D:\AIPred\SeismoFlux` |
| GitHub仓库名 | `SeismoFlux` |
| Python包名 | `seismoflux` |
| 命令行入口 | `seismoflux` |
| 文档版本 | 1.0 |
| 交接日期 | 2026-07-13 |
| 建设方式 | 新仓库、新环境、从零实现，不依赖旧项目代码 |

## 1. 文档用途

本文件是新项目的唯一实施蓝图。新项目应从空目录开始，按本文顺序完成数据契约、基线模型、异常增量模型、危险区生成、验证、展示和前瞻运行，不应继续复制旧项目的版本链。

本文既是需求说明，也是技术设计、验收标准和实施顺序。任何模型升级只有通过对应阶段门控后才能进入下一阶段。没有通过门控的实验应登记为负结果并结束，不得通过反复查看锁定测试集来调参。

每个阶段性结果一旦形成，必须立即进行“科学价值复审”，并把结论写入该阶段验收和当前交接文档。复审不是一般性的工程总结，而必须回答：

1. 该结果对“同等报警面积下提高未来独立地震区域召回”属于直接提升、必要使能还是没有实质推动；
2. 支持该判断的前瞻或滚动验证证据是什么，尚未获得直接效果证据时不得把工程可运行误报为预测有效；
3. 当前路线与最有希望的下一条科学路线相比，继续投入的机会成本是否合理；
4. 下一步应继续、调整还是停止，以及能够推翻当前判断的最小新证据是什么。

科学价值复审是阶段验收的一部分。若结果没有实质推动，或只产生无法在紧邻下一阶段转化为科学检验的工程资产，则不得继续无期限优化实现；必须暂停原路线，复审数据、假设、模型组合与评价设计，形成明确调整决定后再继续。必要使能工作可以保留，但必须标明它尚未证明预测效果提升。

## 2. 项目名称

推荐名称：**SeismoFlux**。

名称含义：

- `Seismo` 表示地震活动与地震学背景。
- `Flux` 表示地震发生强度、异常证据和空间关注区域随时间连续变化。
- 名称强调概率强度场和动态演化，不承诺确定性地震预测。

截至2026-07-13，公开搜索未发现明显的同名GitHub项目或PyPI包。创建仓库及发布Python包前仍需重新检查名称占用情况。

推荐项目说明：

> Anomaly-informed spatiotemporal seismicity forecasting under alarm-area constraints.

推荐中文说明：

> 融合动态异常、地震活动背景和活动断层先验的时空地震活动预测与有限面积危险区研究系统。

## 3. 核心科学问题

项目只回答一个主问题：

> 在严格控制地震活动自身聚集效应、长期构造背景和报警面积的前提下，动态地球物理异常是否能为未来指定震级范围、指定时间窗的地震地点提供稳定、可重复、可前瞻验证的增量信息？

这比“训练一个分数更高的分类模型”更严格。异常模型必须超过不使用异常的地震活动背景模型，才能说明异常本身有价值。

## 4. 目标与非目标

### 4.1 一级目标

在可接受的候选区数量和总报警面积下，尽可能提高未来目标地震的严格区域召回率。

严格覆盖定义：真实震中位于预测区多边形内部或边界上。同一地震即使被多个预测区覆盖也只计一次。

### 4.2 二级目标

在区域召回稳定后，提高预测强度峰值或区内定位点与真实震中的接近程度，并报告50 km和100 km命中率。

### 4.3 输出目标

每个起报日期应输出：

- 未来7、30、90、180、365天的连续条件强度面；
- 5-6级和6级以上两个震级档的期望事件强度；
- 在预先规定预算下生成的有限候选危险区；
- 候选区顺位、覆盖的期望事件质量、面积和证据构成；
- 数据水位、样本充分性、模型版本和不确定性；
- 与上一期的变化及完整历史版本。

### 4.4 明确不做

- 不声称确定性地震预报。
- 不把相对强度、模型分数或颜色直接称为绝对发震概率。
- 不把50 km精定位作为第一阶段的成败标准。
- 不使用未来地震位置生成候选点或候选区。
- 不使用异常表中人工填写的预测地点、震级和时间作为可靠标签。
- 不每周重新训练模型。
- 不通过扩大区域到几乎覆盖全国来制造高召回率。

## 5. 旧项目留下的基准认识

新项目不复用旧代码，但必须继承以下经过验证的认识：

1. 候选点生成后再做Top-K排序的路线已进入平台期。
2. 5-6级后期压力段严格区域召回约19%-25%，每区一个定位点的50 km召回约0%-6.25%。
3. 6级以上独立测试震例只有2-3个，无法支持稳定结论。
4. 局部候选池偶尔包含真实震中附近的位置，但排序无法稳定选中。
5. 动态异常必须综合多期表格，单期快照会丢失结束、取消和演化信息。
6. 地震活动聚集本身可以产生较强的空间可预测性，异常是否有效必须在ETAS等背景模型之上检验。
7. 旧项目的v75和v83结果只能作为外部对照，不能成为新项目的代码依赖或训练输入。

## 6. 预登记科学假设

在第一次查看锁定测试结果前，将以下假设、指标和通过条件写入 `docs/research_protocol.md` 并打标签。

| 编号 | 假设 | 对照 | 通过条件 |
| --- | --- | --- | --- |
| H0 | 地震目录自身可以形成有效背景强度 | 空间均匀Poisson | 空间变化Poisson或ETAS在验证段信息增益为正 |
| H1 | 动态异常提供增量信息 | ETAS或最佳背景模型 | 时间置乱检验后仍有正信息增益，并改善同面积召回 |
| H2 | 多期演化优于单期快照 | 仅当期异常特征 | 多数滚动验证折同时改善，不只改善一次切分 |
| H3 | 活动断层和长期危险性改善中长窗 | 不使用断层先验 | 90/180/365天验证指标改善且短窗不明显恶化 |
| H4 | 共享的标记模型优于两个完全独立模型 | 分别训练M5-6和M6+ | 稀疏震级档的对数评分改善且不确定性合理 |
| H5 | 面积预算下直接选区优于候选点排序 | 固定Top-K点和圆 | 相同并集面积下严格召回提高，或相同召回下面积下降 |

任何假设未通过时应保留其对照模型，不得因为模型更复杂而默认采用。

## 7. 数据资产与可信度分级

### 7.1 动态异常表

旧项目盘点时约有205份异常表、212个报告日期、59,904条标准化记录，时间约为2022-07-18至2026-07-01。迁移时必须重新统计，不能把这些数量写死为事实。

高可信字段：

- 经度、纬度；
- 学科；
- 测项名；
- 台站或观测点名称；
- 异常开始时间；
- 明确填写的异常结束时间；
- 报告日期；
- 原始来源文件和原始行号。

需要重新计算或谨慎使用的字段：

- 异常持续时间必须自行计算；
- 结束时间为空表示截至该报告日期尚未明确结束，应视为右删失或仍在报告，不能随意补结束日期；
- 某条异常在下一期表中消失不等于异常实际结束，应记录为“未继续列报”；
- 效能、异常等级等字段可以作为有噪声特征，但必须保留缺失和来源差异；
- 人工建议的预测区域、地点、震级和时间只能作为单独的弱信息实验，默认模型不得使用；
- 人工填写的异常持续时间不得进入训练。

### 7.2 地震目录

可用目录：

- `china3new.eqt`：1970年以来3级以上地震，格式为年月日、时分秒、纬度、经度、震级、深度、固定字段和地点；
- `1900年以来5级以上地震目录_simple.xlsx`：1900年以来5级以上地震，包含部分中国大陆以外事件。

使用原则：

- M3+目录用于建立地震活动背景、ETAS和小震时空演化；
- M5+目录用于目标事件定义、长期背景和震级标记；
- 两个目录重叠事件必须去重并保留来源映射；
- 所有原始时间、时区、震级类型和坐标必须原样保存；
- 模型使用前统一到UTC和WGS84，同时保留北京时间字段供展示；
- 中国大陆研究区必须使用明确的研究区多边形过滤，不能只用经纬度矩形；
- 区外事件可用于边界附近的触发历史，但必须通过配置控制，不能混入大陆目标事件指标。

### 7.3 活动断层和长期危险性资料

- `FaultCord.xlsx`：简化活动断层段坐标，适合模型计算和属性连接；
- `FaultAttri_ALLV4_修改.xlsx`：断层属性、滑动速率、强震复发周期、上次强震时间等；
- `weight_analysis-修改newV4_修改.xlsx`：破裂空段、闭锁段、中小地震稀疏段、库仑应力增强段和综合长期危险性得分；
- PlotBD断层迹线：更接近真实迹线，优先用于绘图和几何距离辅助，但属性不完整。

使用原则：

- 简化断层和真实迹线是两套不同几何，必须分别建表；
- 建立断层交叉映射表并给出匹配置信度，禁止只按行号拼接；
- 离逝率等长期指标只能作为缓慢变化的背景先验；
- 综合长期危险性得分不能直接解释为短期发震概率；
- 缺失复发周期或上次强震时间时必须显式标记，不能用0替代；
- 距离、线密度和方位关系必须在等面积投影或测地线算法下计算。

### 7.4 底图资料

PlotBD中的国界、断层、一级地块边界、推导边界、二级地块边界、周边板块边界和海洋资料主要用于可视化及区域归属。边界数据不得替代活动断层属性表。

## 8. 数据迁移规则

新项目只迁移原始输入，不复制旧项目的脚本、模型、缓存和输出。

建议从以下位置建立迁移清单，实际复制前逐项确认存在性：

| 数据 | 旧项目候选位置 |
| --- | --- |
| 动态异常表 | `D:\AIPred\LocationPred\anomaly\` |
| 1970年以来M3+目录 | `D:\AIPred\LocationPred\china3new.eqt` |
| 1900年以来M5+目录 | `D:\AIPred\LocationPred\1900年以来5级以上地震目录_simple.xlsx` |
| 活动断层简化坐标 | `D:\AIPred\LocationPred\FaultCord.xlsx` |
| 活动断层属性 | `D:\AIPred\LocationPred\FaultAttri_ALLV4_修改.xlsx` |
| 长期危险性结果 | `D:\AIPred\LocationPred\weight_analysis-修改newV4_修改.xlsx` |
| 底图和真实断层迹线 | `D:\AIPred\LocationPred\PlotBD\` |

绝对路径只允许出现在迁移配置中，项目代码、测试和模型清单必须使用项目内相对路径或环境变量。

迁移步骤：

1. 在新仓库外建立只读原始数据目录。
2. 为每个文件记录相对路径、字节数、修改时间和SHA-256。
3. 生成 `data/manifests/source_inventory.csv`。
4. 原始文件不得在清洗过程中被修改。
5. 标准化结果写入Parquet或GeoParquet。
6. 旧项目v75、v83的机器可读指标如需对比，只能复制到 `benchmarks/legacy/`，并标记为只读外部基准。
7. 新代码中不得出现指向旧项目源代码的导入路径。

## 9. 核心数据契约

### 9.1 `anomaly_observation`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `anomaly_id` | string | 稳定异常实体ID |
| `observation_id` | string | 某报告期中的记录ID |
| `report_date` | date | 报告日期 |
| `available_at` | datetime | 最早可用于预测的时间 |
| `station_id` | string | 标准化台站ID |
| `longitude`、`latitude` | float | WGS84坐标 |
| `discipline` | category | 学科 |
| `measurement` | string | 测项名 |
| `start_time` | datetime | 异常开始时间 |
| `reported_end_time` | datetime/null | 明确填写的结束时间 |
| `is_listed` | bool | 当期是否列报 |
| `right_censored` | bool | 截至当期是否无明确结束 |
| `reliability_flags` | list | 解析和可信度标记 |
| `source_file`、`source_row` | string/int | 原始追溯信息 |

异常实体匹配应综合台站、坐标、学科、测项和开始时间。模糊匹配必须输出审计表，不能静默合并。

### 9.2 `earthquake_event`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `event_id` | string | 去重后的物理事件ID |
| `origin_time_utc` | datetime | UTC发震时间 |
| `origin_time_local` | datetime | 北京时间或原始本地时间 |
| `longitude`、`latitude` | float | WGS84震中 |
| `depth_km` | float/null | 深度 |
| `magnitude` | float | 统一数值 |
| `magnitude_type` | string/null | 震级类型 |
| `catalog_sources` | list | 来源目录和原始ID |
| `inside_study_area` | bool | 是否位于研究区 |
| `dedup_confidence` | float/category | 去重可信度 |

去重阈值必须配置化，并输出候选重复事件供人工复核。初始候选规则可使用时间差、测地距离和震级差联合判断，但不能把阈值隐藏在代码中。

### 9.3 `fault_segment`

包括断层段ID、名称、简化几何、真实迹线映射、属性来源、滑动速率、复发周期、上次强震时间、离逝率、四类长期危险性判定、综合得分、缺失标记和匹配置信度。

### 9.4 `forecast_issue`

每个预测版本必须记录：

- `issue_id`；
- 起报时间和数据截止时间；
- 输入清单哈希；
- 模型版本和配置哈希；
- 代码提交哈希；
- 预测窗口和震级档；
- 是否历史回放、影子运行或真正前瞻；
- 生成时间、验收状态和不可变归档路径。

### 9.5 `forecast_region`

包括起报ID、震级档、预测窗口、区域ID、顺位、几何、面积、捕获的期望事件质量、强度峰值、证据构成、稳定性、数据支持等级和生成算法版本。

## 10. 时间语义和防泄漏规则

这是全项目最高优先级约束。

1. 起报日为 `T` 时，只能使用 `available_at <= T` 的数据。
2. 若没有真实发布时间，只知道报告日期，则采用保守可用时间，例如北京时间报告日期次日00:00，并在配置和数据清单中明确记录。
3. 异常真实开始时间早于T，但在T之后才首次出现在报告中时，T期预测不能使用。
4. 结束时间在未来才被填写时，早期起报版本只能把异常视为右删失，不能提前知道结束。
5. 未来报告中异常是否消失不能回填到历史特征。
6. 未来地震不能参与候选生成、空间加密、阈值选择和参数调整。
7. 同一物理地震在多个起报日和窗口中出现时，训练、验证和测试归属必须按物理事件统一管理。
8. 不得随机拆分网格行、起报行或异常行。
9. 最终测试段一经锁定，只允许运行一次正式验收。
10. 每次运行自动执行泄漏检查，并在发现违规时失败退出。

## 11. 总体模型架构

推荐主模型为“背景地震活动强度 + 长期构造先验 + 动态异常增量”的标记时空点过程：

\[
\lambda(s,t,m \mid \mathcal H_t,A_t,F)
=\lambda_0(s,t,m \mid \mathcal H_t)
\exp\{g_A(s,t,m)+g_F(s,m)\}
\]

其中：

- `s`为空间位置；
- `t`为时间；
- `m`为震级标记；
- `H_t`为起报时已经发生的地震历史；
- `A_t`为起报时已经知道的异常演化；
- `F`为断层和长期构造背景；
- `λ0`为不依赖异常的背景模型；
- `g_A`为动态异常增量；
- `g_F`为长期构造修正。

模型通过点过程对数似然训练：

\[
\mathcal L
=\sum_i\log\lambda(s_i,t_i,m_i)
-\int\lambda(s,t,m)\,ds\,dt\,dm
\]

该形式同时惩罚漏掉真实事件和在过大空间时间范围内给出过高强度。

## 12. 背景地震活动模型

必须按由简到繁顺序建立三个基线：

1. 空间均匀Poisson，仅用于最低基准。
2. 空间变化Poisson或历史地震核密度模型，表示长期地区差异。
3. 时空ETAS或Hawkes模型，表示背景率、余震触发、时间衰减和空间扩散。

ETAS是优先主对照，不应一开始直接用神经网络替代。优先使用经过同行评议和验证的实现；如需自研，必须用模拟目录和公开参考结果做数值回归测试。

如果目标盲、预登记的 ETAS 数值资格已经形成可信 `not_evaluable` 结果，则不得为了维持模型名称而无限修补或阻断整个科学问题。此时按 H1 和 G1 使用当前最佳合法、可评价的非异常背景作为确认性主对照，并把 ETAS 明确列为“不可评价、未参与采用判断”。任何新的 ETAS/Hawkes 实现必须另立目标盲协议和独立数值证据，不能针对已经查看的异常增量结果调参。

背景模型可使用1970年以来M3+目录训练，因此样本量远大于异常时期目标地震数。模型完整度震级、目录制度变化和区域检测能力变化必须单独诊断。

背景模型输出至少包括：

- 条件强度；
- 未来窗口内期望事件数；
- 背景项与触发项分解；
- 参数不确定性；
- 目录完整度敏感性。

## 13. 动态异常状态重建

动态异常模块必须使用全部历史报告期，不能只读取最新一张表。

每个起报日、查询位置和空间尺度应构造：

- 当前列报异常数；
- 新增异常数；
- 首次出现率；
- 明确结束数；
- 未继续列报数；
- 4、8、13、26、52周增长斜率；
- 增长加速度和突增变点；
- 峰值后的回落幅度；
- 持续时间和时间自开始以来的年龄；
- 学科数量、测项数量和多学科协同度；
- 空间集中度、扩散半径和主方向；
- 到断层的距离、沿断层方向投影和断层段聚合；
- 报告覆盖率、有效台站数和缺报指标；
- 不同可靠性等级下的加权统计。

空间特征至少比较50、100、200、300、500 km尺度以及连续距离核。尺度选择只能在训练和验证段完成。

异常数量变化必须除以或控制同期可观测台站和测项数量，避免把报告范围扩大误认为异常增加。

## 14. 动态异常增量模型

按以下顺序实施：

### 14.1 可解释增量模型

首先使用正则化Poisson回归、GAM或广义加性点过程模型。它应明确展示：

- 距离衰减；
- 异常年龄效应；
- 数量增长和回落效应；
- 学科多样性；
- 与断层背景的交互；
- 不同预测窗下的效应衰减。

只有物理上明确的关系才使用单调约束，例如距离增大时影响不应无理由增强。异常回落可能有正负两种含义，不应强制单调。

### 14.2 非线性表格模型

在可解释模型通过后，使用梯度提升树作为非线性对照。必须使用相同数据切分、同一积分曝光和同一评估指标。

### 14.3 多实例弱监督模型

将某个空间查询位置周边的一组异常视为一个“包”，模型只知道未来窗口中是否发生目标事件，不假定每条异常都相关。注意力权重用于估计异常实例贡献。

训练时可以围绕真实事件构造正包，但推理和正式评估必须在完整研究区或预先定义的曝光采样点上运行。禁止根据真实震中生成推理候选。

### 14.4 自监督时间编码

只有在前述简单模型显示动态演化确有增量后，才进入自监督编码：

- 遮挡报告期或测项并重建；
- 预测下一期异常状态；
- 对同一区域相邻时间片做对比学习；
- 处理不规则采样和缺失通道；
- 用自监督表示微调点过程增量头。

不要直接在约212个报告日期上训练大型Transformer并把高训练分数当作进展。

## 15. 异常有效性的安慰剂检验

异常模块必须至少通过三类检验：

1. 在保持异常空间分布和数量不变的条件下随机置乱异常时间。
2. 在同一构造区内置换异常位置，保留区域密度差异。
3. 使用报告覆盖率、台站数量等观测条件作为伪特征，检验模型是否只学到报告制度变化。

真实异常模型必须稳定超过置乱分布，才允许声称异常具有增量信息。只超过某一次随机种子不算通过。

## 16. 长期断层先验

长期断层信息应形成独立、缓慢变化的背景层：

\[
g_F(s)=f(d_{fault},\ density_{fault},\ elapsed\ ratio,\ hazard\ score,\ slip\ rate)
\]

实施要求：

- 离逝率设置合理截断并保留缺失标记；
- 长期危险性得分与短期异常特征分开报告；
- 对断层距离、线密度、端点和交汇区特征做消融；
- 使用真实迹线与简化断层分别计算敏感性；
- 不允许长期先验压倒当期动态证据而形成永远不变的热点；
- 断层模块主要期待改善90、180和365天窗口。

## 17. 震级建模

不要为M5-6和M6+从头训练两个完全独立模型。推荐使用标记点过程或分层模型：

1. 先预测未来M5以上事件强度。
2. 再建模条件震级分布 `p(m | s,t,history,anomaly,fault)`。
3. 由同一模型积分得到M5-6和M6+结果。
4. 区域、预测窗和震级档之间使用分层部分汇聚。

M6+结果必须显示样本数和宽不确定区间。锁定测试物理事件不足10个时，只能称为探索性结果；达到10个也只是最低分析水位，不代表结论充分。

## 18. 不同预测窗口的统一处理

优先使用同一个连续时间强度模型对不同窗口积分，而不是分别训练五套互不一致的模型：

\[
\mu_{h,M}(R,T)
=\int_T^{T+h}\int_R\int_M\lambda(s,t,m)\,dm\,ds\,dt
\]

起报后未知的未来异常不能使用。当前异常对未来的贡献通过训练期学习到的时间衰减核向前传播，地震背景则由点过程自然演化。

这样，7、30、90、180、365天是同一条件强度的不同积分范围，结果可以不同，但逻辑应保持一致。

## 19. 空间表达与计算网格

点过程在理论上是连续空间模型，网格只用于数值积分和展示，不应决定预测能力上限。

推荐方案：

- 使用适合中国大陆的等面积投影；
- 初始积分单元约25 km；
- 在高强度梯度区、复杂断层区和候选区边界局部细化到12.5 km；
- 做50、25、12.5 km收敛性审计；
- 若结果随网格明显变化，禁止发布；
- 所有距离使用测地线或投影米制距离；
- 输出GeoParquet和GeoJSON时转换回WGS84。

不要从一开始铺设全国0.1度超密网格。先验证积分收敛，再决定是否进一步加密。

## 20. 面积预算下的危险区生成

候选区不是对散点画任意包络线，而是在强度面上求解受约束的集合优化：

\[
R^*=\arg\max_R
\left[\int_R\mu(s)ds
-\alpha Area(R)
-\beta Fragmentation(R)
-\gamma Instability(R)\right]
\]

约束至少包括：

- 区域数量上限；
- 总并集面积上限；
- 单区面积上限；
- 最小连通面积；
- 碎片度和紧凑度；
- 重叠区域合并；
- 中国大陆研究区裁剪。

开发阶段绘制完整Pareto前沿：

- 区域数量：5、8、10、12；
- 总面积：30、45、60、75、96万平方公里；
- 严格覆盖和70 km容差覆盖分别统计。

最终业务输出原则上最多10个区域、总并集面积不超过96万平方公里。具体操作点只能依据验证段预先选择，锁定测试后不得改变。

单区面积初始参考：

- 5-6级不超过半径100 km圆的等效面积；
- 6级以上不超过半径150 km圆的等效面积。

允许不规则区域，但必须满足等效面积和紧凑度约束。需要放宽时必须形成新的预登记实验，不得在看完测试地震后修改。

## 21. 区域顺位与信度

每个候选区输出两个独立概念：

1. `rank`：同一期结果中的关注顺位。
2. `evidence_grade`：根据数据充分性、模型一致性、历史验证水位和不确定性形成的证据等级。

不要让A/B/C/D同时承担顺位和信度。默认界面直接显示1至K顺位，并另外显示“充分、有限、不足”证据等级。

区域顺位应根据捕获的期望事件质量、峰值、面积效率和冗余度计算，不能只看中心点分数。

## 22. 数据切分与回测

### 22.1 基本规则

- 所有切分按时间进行。
- 背景模型可使用异常时期之前的M3+目录预训练。
- 异常增量模型只能使用有异常报告覆盖的时期训练。
- 最大预测窗口为365天，验证结果必须等待窗口完整成熟。
- 验证与最终测试的目标窗口不得重叠。
- 同一物理地震不得同时进入训练、验证和测试。

### 22.2 推荐流程

1. 先按日历时间定义开发期、验证期和锁定测试期，不看地点预测成绩。
2. 验证期最后一个起报日的365天窗口完全成熟后，才冻结模型和操作点。
3. 测试期从验证数据全部成熟之后开始，避免标签重叠。
4. 开发期内部使用滚动起点验证，报告每个折而不是只报告平均值。
5. 锁定测试只运行一次，结果无论好坏永久保留。

如果现有异常时期过短而无法同时提供成熟验证和独立测试，应明确标记“证据不足”，使用滚动开发验证和今后的真正前瞻积累，不能退回随机切分。

## 23. 评估指标

### 23.1 一级指标

- 相对最佳背景模型的信息增益，按每个物理地震报告；
- 固定总面积下的严格地震召回率；
- 达到固定召回率所需的报警面积；
- Molchan漏报率与报警时空体积曲线；
- 候选区并集面积和面积时间暴露；
- 真正前瞻期的相同指标。

### 23.2 二级指标

- 70 km容差区域召回，必须与严格召回分开；
- 最近强度峰值到震中的50 km和100 km命中率；
- 区域级精确率和每命中事件的报警面积；
- 区域数量、碎片度、紧凑度和重叠率；
- 相邻起报期的Jaccard稳定性和峰值移动距离；
- 期望事件数校准、Brier分数和可靠性图；
- 按东部、西部、构造区、震级档和窗口分层的样本水位。

### 23.3 统计检验

- 以物理地震为单位做块Bootstrap；
- 对异常时间和空间做置换检验；
- 报告置信区间，不只报告点估计；
- 样本不足时不做显著性结论；
- 多模型、多窗口比较需要控制重复试验产生的偶然最优。

## 24. 必须保留的基线

任何新模型都要与以下基线在同一数据、同一切分和同一预算下比较：

1. 空间均匀Poisson；
2. 历史地震核密度或空间变化Poisson；
3. 数值资格通过时的标准时空ETAS；不可评价时如实保留缺席状态，不得伪造分数；
4. ETAS可评价时的ETAS加长期断层先验，否则使用当前最佳合法背景加长期先验；
5. 当前最佳合法背景加线性或GAM异常增量；ETAS可评价时同时报告ETAS加异常；
6. 上一期区域直接延续的持久性基线；
7. 旧项目v75和v83的只读外部结果。

复杂模型只有超过第5项才说明它比可解释异常模型更有价值。

## 25. 阶段采用门控

| 门控 | 最低要求 | 未通过时 |
| --- | --- | --- |
| G0 数据 | 解析、时区、去重、异常状态和泄漏测试全部通过 | 停止建模 |
| G1 背景 | ETAS或空间变化模型超过均匀基线 | 修复背景，不接异常模型 |
| G2 异常 | 相对背景信息增益为正，且真实异常超过置乱异常 | 结论为未发现增量，不进入深度模型 |
| G3 动态 | 多期轨迹稳定超过单期快照 | 保留简单异常模型 |
| G4 构造 | 中长窗改善且没有明显扩大报警面积 | 不采用长期修正 |
| G5 复杂模型 | 多折稳定超过GAM和梯度提升基线 | 不采用神经模型 |
| G6 区域 | 相同面积召回更高或相同召回面积更低 | 保留简单阈值区域 |
| G7 测试 | 锁定测试和完整审计通过 | 不晋级正式影子 |
| G8 前瞻 | 足够成熟的真正前瞻期保持增益 | 不升级为业务参考 |

建议把具有实际意义的最小改善在首次实验前登记。例如同面积严格召回提高至少5个百分点，或在召回相同的情况下并集面积下降至少10%。若统计不确定区间很宽，应继续积累数据而不是宣布成功。

## 26. 从零实施阶段

每个阶段按以下闭环执行：

`技术与防泄漏验收 → 科学价值复审 → 继续/调整/停止决定 → 文档落盘 → 提交并推送 → 下一阶段`

其中“科学价值复审”至少记录：与当前最好合法基线在同一支持域和同一报警面积下的关系；属于直接效果证据还是必要使能证据；组件在后续消融中的可归因角色；继续投入前必须取得的最近一项科学证据。不得用测试数量、代码规模、界面完成度或运行稳定性代替预测效果证据。

三类结论的语义固定如下：

- `direct_improvement`：未来隔离的滚动或独立验证已经显示，相对当前最好合法基线在同一支持域和固定报警面积下提高严格召回，或在同召回下减少面积，并满足该阶段预登记的不确定性和安慰剂门；
- `necessary_enabler`：尚无预测效果证据，但该产物直接解除紧邻、有限且预登记的科学检验阻断；必须同时写明最近检验、最长允许投入范围和停止条件；
- `no_material_progress`：既没有直接效果证据，也不能在紧邻下一阶段转化为受控科学检验；结论形成即停止当前工程扩张，先复审数据、假设、模型组合与评价设计。

每份阶段验收和当前重启交接都必须保存机器可检索的 `science_value_category`、`evidence`、`decision`、`next_scientific_test` 和 `stop_condition`。缺少任一项时，该阶段不得提交为完成状态，也不得进入下一阶段。

### 阶段0：仓库和科学契约

任务：

- 创建空仓库和Python 3.11环境；
- 建立目录、配置、测试和持续集成；
- 写入研究问题、非目标、切分、指标和门控；
- 生成数据源清单和许可证说明；
- 保护 `main` 分支。

交付：`pyproject.toml`、锁文件、`AGENTS.md`、`docs/research_protocol.md`、基础CI。

验收：空项目安装成功，测试命令和代码质量检查成功。

### 阶段1：数据接入和质量审计

任务：

- 解析异常表、M3+目录、M5+目录、断层和底图；
- 生成标准化Parquet；
- 建立异常实体和物理地震ID；
- 输出字段质量、空间范围、时间覆盖、重复和缺失报告；
- 完成时间可用性规则。

交付：`data_catalog.json`、标准化数据、质量报告、数据契约测试。

验收：同一输入重复运行得到相同哈希，未来信息泄漏测试为0。

### 阶段2：不含异常的背景基线

任务：

- 空间均匀、空间变化和ETAS三类基线；
- 目录完整度和参数稳定性分析；
- 未来窗口积分；
- 模拟目录和数值回归测试。

交付：背景模型注册表、回测指标、条件强度图。

验收：G1通过，或明确记录背景模型无法稳定拟合并停止。

### 阶段2R：局部支持域修订（2026-07-14）

阶段2的 `v0.2.0` 全域协议及其可信负结果保持不可变。该协议在任何合格空间层完整度估计高于4.0时全阶段停止；正式执行因此在模型拟合和评分前终止，背景 `Score ID` 数量为0，锁定测试未运行。用户随后明确指出局部目录不完整不应使其他区域同时失去可评估性，并授权继续。以下修订并不把原结果改判为通过，也不允许把候选震级上限事后放宽到4.5。

新实验版本为 `0.2.1`，门控名称为 `G1-LS`（local support）。它只表示固定局部支持域内的背景模型比较，不得称为原全域G1。必须遵守：

- 每个历史折和最终验证快照分别使用该快照 `fit_end_utc` 以前且届时已经 `available_at` 的目录生成支持域，后期快照不得回填早期快照；
- 支持域使用原点固定、目标无关的500 km等面积基础格；不少于200个历史事件的格使用本格MAXC加0.2，稀疏格继承固定1000 km父格，父格仍稀疏时标为不可判定并沿用公共Mc；
- 只有空间层原始修正Mc高于4.0时，才把对应的既有500 km基础格标为 `unsupported`；格网边界不得根据地震位置、模型分数或验证结果移动、加密或重画；
- 时间层原始修正Mc高于4.0仍是全实验硬失败，不能用空间屏蔽绕过；
- 任一快照不存在合格时间完整度层时仍是全实验硬失败，不得只依赖空间层继续运行；
- 每个快照的有效支持面积必须至少为原研究区精确等面积的95%，否则在任何模型拟合或评分前停止，且该阈值不得在看到背景分数后放宽；
- 同一快照的均匀Poisson、空间Poisson/KDE和ETAS必须共享同一 `support_id`、公共Mc、目标事件集合、有效面积和补偿积分域；KDE只用支持域事件拟合并在支持域归一；
- `unsupported` 格事件不得作为评分目标或背景KDE训练事件。达到该格冻结局部Mc的已观测事件只可作为ETAS条件父历史，并必须同时报告完全排除这些父事件的敏感性；
- 三模型仍按同事件、同曝光和同补偿域计算配对信息增益，原G1的开发折、验证段和数值稳定性要求不降低；
- 后续任何全研究区严格召回、Molchan或报警面积主表仍以原研究区全部目标为分母，`unsupported` 格目标计为未覆盖；支持域条件召回只能作为诊断，禁止用缩小分母制造提升；
- 公开支持清单只保留固定格编号、行列号、精确裁剪面积和诊断决策，不得携带研究区边界、格网裁剪几何、坐标或WKB；执行时必须从封印的本地研究区重建固定格并核对身份；公开诊断图同样不得绘制受限派生边界；
- 原 `configs/background.yaml`、原注册表、原报告、原内容寻址产物和原标签不得覆盖或改写。新协议、支持域清单、输出根、注册表、报告和标签必须独立。

阶段2R分两步执行：

1. **2R-0无评分预登记**：实现支持域诊断和防泄漏测试，生成不含评估目标、信息增益、命中结果、模型选择或 `Score ID` 的支持域清单与诊断图；完成测试、验收、提交、推送并冻结 `v0.2.1-background-local-support-protocol`。
2. **2R-1背景比较**：只能在上述标签后实现并执行支持域一致的三类模型。完成全部测试、独立审计、提交和推送后判定G1-LS；通过才允许讨论阶段3，失败则再次登记可信负结果并停止。

### 阶段2S：因果近期地震活动增量（2026-07-30）

阶段 4A `0.4.4` 异常路线在任何真实目标读取前再次发现 foundational P0，已经硬停止并保留
G1-LS 通过的 75 km KDE。阶段 2S 是独立的背景动力学筛查，不重开异常 attempt，也不改变 ETAS
`not_evaluable` 结论。

只比较三个模型：

- `S0`：精确重物化的 `fold_4` 长期 75 km KDE；
- `S1`：`S0` 与最近 30 天、严格因果、等权 M4+、75 km KDE 的凸组合；
- `SP`：`S0` 与紧邻的再前一个 30 天同结构 KDE 的确定性过去对照。

2022–2023 已用于阶段 2R 的背景和 75 km 带宽选择。因此本阶段只能称为“Stage 2S 新增 alpha
和候选图按时间因果隔离的复用开发筛查”，不能称全局目标盲、独立模型选择或独立验证。

每折 `S1/SP` 各一个 `[0,1]` 权重，只用 expanding、非重叠 h007 fit exposures 按固定凹似然和
64 次二分拟合，并共享该折 7/30/90 天。三模型共享该折 M5–6 fit 日率、同一支持域和格网；空间
密度在起报时冻结并归一到 1，补偿差必须独立复算为 0。禁止第二套窗口、衰减、带宽、震级权重、
候选模型或完整 Hawkes。

所有 KDE 都是在冻结等面积投影中的连续二维 Gaussian 密度。12.5 km 支持域裁剪格只用于唯一
归一常数和 operational mass；assessment 事件坐标只在三折 master prediction seal 后用于连续
密度评分，禁止 25 km 查格/插值代替事件密度。12.5 km 质量对齐聚合到 25 km 父格用于报警和区域
补偿；50/25/12.5 km 直接求积只用于 2% 总量、5% L1 收敛门。密度 floor、目标坐标生成格网/
区域/候选一律禁止。

阶段 2S 沿用目标盲冻结的三个滚动折、`M5_6=[5,6)`、600,000 平方公里完整 25 km 报警前缀和
2,000 次配对物理事件块 Bootstrap。`S1-S0` 与 `S1-SP` 在信息增益和召回中各组成两成员
Bonferroni 家族，同时区间为 `1.25%–98.75%`。通过必须同时满足：三窗口信息增益点估为正、
至少 2/3 折的信息增益和召回增益均为正且各自折中位数为正、两项宏信息增益同时下界大于 0、
`S1-S0` 固定面积召回至少 `+5 pp` 且同时下界大于 0、`S1-SP` 同面积召回点估和同时下界大于 0，
以及跨区、去最大贡献区和 1/7 天可用延迟门。

信息增益只使用支持域内 M5–6 目标；严格召回保留全研究区目标，支持域外目标强制为三模型共同
未命中。两套分母和最低事件数分别冻结。Bootstrap 分层增加 support bit，并冻结 PCG64 namespace、
抽样顺序和 NumPy linear 分位数。
区间只表示固定日历、已观察分层大小和已拟合模型下的事件重采样稳定性，不等于震群独立或新的
独立验证；必须另报震群 component 数、事件抽样单位数和去主导震群诊断。

跨区门对 `S1-S0` 和 `S1-SP` 分别计算 39 区可加信息增益与召回贡献。每项区域和必须闭合到总体
宏指标；每个对比、每个指标至少两个“该指标有目标事件且可加贡献为正”的区域。零事件区只参与
可加闭合，不得充当跨区阳性证据。按 zone ID 并列规则移除各指标自己的最大正贡献事件区，保持
原九宫格分母，残余信息增益和召回都必须仍大于 `1e-12`。

第一版目标盲 adapter 必须显式同时返回 25 km `query_grid` 和
`cell_id → construction_zone_id`。`query_grid` 必含冻结 CRS、格 ID、row/column、目标无关本地
坐标和精确研究区裁剪面积；区域映射只含编号、不得含坐标或 geometry。禁止公开原始格网坐标表/
裁剪 geometry，但结果后可用冻结坐标渲染不嵌入原始表的派生历史图。研究区文件、投影几何、格网
算法及源码身份均须在 code tag 后、目标读取前核对。若不能合法返回 15,697 格和 39 个非空区域，
立即停止。

阶段 2S 分两步：

1. **2S-0目标盲预登记**：冻结 `configs/causal_seismicity_screen.yaml`、独立 fold manifest、
   输入合同、协议测试和验收；提交、推送并冻结
   `v0.2.3-causal-seismicity-screen-protocol`。本步不得读取 `data/processed`、目标或成绩。
2. **2S-1唯一历史开发筛查**：只能在协议标签远端核对后实现独立 `seismoflux.stage2s` 最小
   代码；code tag 后先完成无目标 study-area/grid/cell-zone 接口预检，再以两个 `O_EXCL` receipt
   依次注册唯一 attempt 和认领唯一 target read。目录路径只物理打开一次，同一流读入不可变 bytes
   并算哈希，只能从同一 `BufferReader` 解析；随后先用全震级重建并逐字段核对 fold4 支持。
   三折必须按 `1→2→3` 严格顺序执行：每折先冻结 `fold fit receipt`，再按 issue 日期递增写
   issue seal，最后写 fold seal；后一折 fit view 只有在前一折 seal 后才开放。三折 master seal
   写成前不得构造、暴露或评分 assessment membership。更早 assessment 事件后来成为合法历史或
   后一折 fit 标签时，只能通过对应受限原始字段接口使用，禁止把先前命中或成绩带入。

本协议冻结后允许的新增 foundational 修订数为 0。任一 P0、门失败或证据不足都停止 Stage 2S
并保留 75 km KDE，不得看结果后改 60/90/365 天、指数衰减、随机 lag、面积、区域或模型重试。
开发通过只表示复用开发期内的一条 Stage 2S 时间因果信号值得新前瞻封存验证；2022–2023 和
2024–2025 均不得再次称为独立 Stage 2S 验证。静态图和离线交互页必须标为“历史冻结 assessment
回溯”，同时展示 `S0/S1/SP`、面积、数据截止和失败案例；本阶段禁止生成“当前预测”图。当前
前瞻预测另立协议并永久归档，不得回填或冒充 G8。
三模型共享同一 M5–6 率，因此本阶段主要判断未来地震相对更可能落在哪里，不判断总数是否增加或
具体哪天发生。2S-1 先交付不依赖目标成绩的数据—方法—因果时间线；交互页固定展示 S0 截止、
R/RP 窗口、available_at 截止、每折权重/共同率、fold/horizon、seal hash 和实际报警面积。
完整合同见 `docs/causal_seismicity_screen_protocol.md`。

#### 阶段 2S-1 正式执行结果（2026-07-30）

代码提交 `4188523991926c51a7fbd9314d36395cc9bfad62` 和远端标签
`v0.2.3-causal-seismicity-screen-code` 核验通过。第一次正式入口因隔离工作树未挂载 ignored
输入而在任何 receipt 或目录读取前停止；只恢复冻结逻辑相对路径后，第二次入口完成无目标
preflight，并依次原子写入 attempt ledger 和 target-read receipt。真实地震目录随后只读取并
解析一次，fold4 支持域重建完成。

最后一个成功进度为 `fold4_support_rebuilt`。随后主科学路径在 fold-fit receipt 和任何预测封印
形成前发生异常；正式 CLI 没有保留该原始异常。异常处理器准备写不可变 terminal record 时，
重读已落盘的 preflight receipt，并把其中已经规范编码的浮点标记再次送入 canonical JSON
编码器，触发第二个保留键错误：

`reserved canonical JSON key at $.bindings.aligned_grid_identity.layers.12.5.cell_size_km: $seismoflux_type`

第二个异常覆盖了原始 science 异常，因此原始失败原因现已不可恢复，不能把上述保留键错误误称为
模型计算的主失败。它同时证明冻结 code tag 的 terminal/finalizer 存在 foundational P0。本次
attempt 已消费，但没有形成 fold-fit/issue/fold/master prediction seal、whole-run record、
模型评分或正式静态/交互结果；terminal record 也没有形成。该缺失作为治理异常保留，不得手工
补写。不得修改冻结 code tag 后重开目录、恢复当前 attempt 或重新评分。

本轮分类为 `no_material_progress`：它没有提供 S1 相对 S0/SP 的正面或负面预测证据。Stage 2S
按 foundational P0 停止，继续保留 G1-LS 的 75 km KDE。下一步先做不读取目标成绩的科学路线
复审，并把选定的新问题、门和停止条件写入本蓝图、验收、提交和推送。若复审仍选择近期地震活动，
只能另立新的前瞻封存协议，使用本次之后尚未发生的目标 cohort；不得再把 2022–2025 称为独立
验证，也不得修复后重跑本次历史目标。

### 阶段2P：真正前瞻近期地震活动证据采集（2026-07-31）

#### 阶段 2P-0 目标盲路线复审

Stage 2S 停止提交 `43da42e276739f45800e48b8f36dc60bcd03b8af` 推送后，只读路线复审确认：
当前唯一真实正证据是长期 75 km KDE 相对均匀背景的信息增益；近期地震、异常、构造和复杂模型
均未证明固定面积召回增量。2022–2025 已用于背景开发/验证，包含 2026 年事件的目录又已被
Stage 2S 整体解析一次，因此没有可安全包装成“真正未见”的历史独立 cohort。下一项科学工作
必须从尚未发生的目标开始。

路线优先级冻结为：

1. 先启动最小近期地震活动前瞻筛查；
2. 异常增量作为第二个独立前瞻挑战者，在首条前瞻线开始后另立协议，不得阻塞第一期；
3. 构造先验只在 2026-07-13 之后的 90/180/365 天前瞻慢窗口另行检验；
4. 当前不重开 ETAS，不进入树模型或神经网络。

选定的新问题是：在冻结支持域、7/30/90 天和“完整格累计面积不超过 600,000 平方公里”的同一
规则下，每期因果重建的“长期 75 km KDE + 最近 30 天 M4+ 事件 KDE”能否同时超过同起报长期
KDE 和“同起报长期 KDE + 再前一个 30 天事件 KDE”，提高未来唯一去重 M5–6 物理地震的支持域
空间信息增益和全研究区严格召回？

#### 阶段 2P-1A 前瞻预登记冻结协议

外行版：每周四同时封存三张不能事后修改的地图。`P0` 表示长期多发区，`P1` 在它上面加入最近
30 天地震，`PP` 则加入再早一个 30 天的地震作为公平对照。三张图必须使用同一时刻获得的同一份
数据和同一套“不超过 600,000 平方公里”的完整格前缀规则，并记录各自实际完整格面积。未来地震
成熟后，只在预先约定的一次正式判定中检查 P1 是否同时超过 P0 和 PP。本阶段只是把考试规则
写死，没有运行真实预测，也没有证明效果提升。

完整逐字段预登记以 `docs/phase2p1_prospective_preregistration.md` 为准；其内容是本蓝图
Stage 2P-1A 的组成部分。2026-07-31 候选审计已 GO，同一候选已唯一跃迁为 `accepted`；
当前文档状态为 `accepted`，科学价值分类为
`necessary_enabler`，直接预测提升为无。当前 `execution_authorized=false`、
`real_issue_authorized=false`、`protocol_frozen=true`。顺序固定为：候选字节完成独立审计并
得到 GO（此时尚未完成阶段）→在同一候选中唯一一次改为 accepted/frozen（已完成）→对精确最终字节重跑
全部验收→提交、推送、annotated tag 与远端 peeled commit 回读→才声明阶段完成。下一步仅允许
在以上全部完成后进入 Stage 2P-1B 纯合成同路径验收。

阶段边界不得偷换：2P-1A 只冻结记录 schema、规范字节、数据表/预测工件 profile、TSA 信任根、
远端发布身份、状态机和统计语义，并验证合同会失败闭合；它不实现或声称真实 ASN.1/CMS、目录表、
预测数组和评价字节已经可重建。2P-1B 才用纯合成数据实现并逐字节演练 acquisition→表→三模型
预测→展示→成熟真值→revision→input freeze→result seal 全链。合成全链和代码标签未验收前，
`real_issue_authorized` 始终为 false，更不得读取真实未来目标来验证实现。

只允许三个确认性模型，`T` 为规则起报时刻，`Q=T-15min` 为共同查询结束和起源时间截止：

- `P0(T)`：冻结 G1-LS 选出的投影、支持域、局部 Mc/资格、训练起点、边界归一、网格和 75 km
  Gaussian KDE 方法；每期从同一个 `T` 前不可变快照中截止 `Q` 的事件重建长期密度，不能沿用
  2023 年旧密度；
- `P1(T) = 0.5 * P0(T) + 0.5 * R30(T)`；
- `PP(T) = 0.5 * P0(T) + 0.5 * RP30(T)`。

P0、R30 和 RP30 必须使用同一 `T` 前快照、同一可见截止和同一选择函数。P0 使用冻结训练起点至
`Q` 的届时可见事件；R30 使用 `(Q-30d,Q]` 的届时可见 M4+ 事件；RP30 使用
`(Q-60d,Q-30d]` 的届时可见 M4+ 事件，两窗都是精确 30 天。RP30 是 T 时已知但起源较早的窗口，
不能把可见截止移到 T-30d。旧 Stage 2S 的 RP 选择器、runner、production/RoleSession、
fold/master seal、已消费
attempt、alpha/带宽拟合、Bootstrap 和正式 gate 均禁止复用。空的 R30 或 RP30 必须使对应组合
逐数组精确退化为 P0。固定 0.5 只是预登记的归一密度混合系数，不是贡献率、最优权重或绝对发震
概率。

本地地震目录只作为 P0 的历史基线，固定截止为 `2026-07-09T04:25:56Z`；它不提供此后的前瞻
增量，也不作为未来真值。截止后的预测增量和目标真值统一来自 USGS ANSS Comprehensive
Earthquake Catalog（ComCat），并保存请求、响应头、原始响应字节和 SHA-256。权威入口为
[USGS FDSN Event Web Service](https://earthquake.usgs.gov/fdsnws/event/1/)、
[ComCat](https://earthquake.usgs.gov/data/comcat/index.php)、
[ComCat DOI](https://doi.org/10.5066/F7MS3QZH)、
[ANSS Data and Products Policy](https://www.usgs.gov/media/files/anss-data-and-products-policy) 和
[USGS Data Licensing](https://www.usgs.gov/data-management/data-licensing)。必须先完成完整
60 天同源洗脱，使首期 R30 和 RP30 都只来自 ComCat。ComCat 在中国的 M4 覆盖、震级口径和
修订节奏可能不同于本地目录；洗脱只消除 R30/PP 换源混杂，不证明完全性或等价性。覆盖差异只
记录并限制结论范围，不设置效果相关硬阈值。一次官方抓取或冻结的目标无关支持证据未按时取得，
该期可记 `missed_issue`；官方来源、政策、schema 或目标无关支持证据持续中断或实质变化时暂停
后续发行并先作目标盲修订。不得补发，也不得查看目标后换源或调参。为避免 API 边界歧义，查询
固定下探至 `minmagnitude=3.9`，解析后本地按数值严格过滤 `mag>=4.0`。
`count` 与 `query` 都必须保存冻结参数对象、规范 GET URL 的 UTF-8 SHA-256、响应头和原始响应
哈希；`count` 使用相同选择参数但明确排除 `orderby/limit/offset/includeallorigins/
includeallmagnitudes`。解析计数必须与 attempt 和 acquisition 中的计数一致，达到 20,000 时禁止
再发 query 并闭锁失败，不能只保存一个自报计数。count 固定 `format=geojson`，HTTP 200 必须
以 JSON/GeoJSON 内容类型成功解析。区间请求不得发送仅适用于 `eventid` 的
`includesuperseded`；`reviewstatus` 也不得发送而使用官方默认 `all`，两者均不进入规范 URL。

切源边界跨源去重固定为起源时间差不超过 300 秒、WGS84 椭球距离不超过 50 km 且
`abs(ΔM)<=0.5`；候选按时间差、距离、震级差和稳定 ID 确定性一对一匹配，local 记录优先作为
anchor，ComCat ID 只作 crosswalk。不满足全部阈值不得人工合并。

规则起报为每周四 `00:00 Asia/Shanghai`。第一期不得早于
`2026-09-10 00:00:00 Asia/Shanghai`，且必须是 Stage 2P-1 协议标签和代码标签远端对象及
peeled commit 均回读核验完成后的下一次规则起报；不能补发已经错过的同周时刻。公式是
`max(2026-09-10规则时刻, 两个远端标签核验完成后的下一规则时刻)`。
`TargetCohortDefinition` 必须嵌入两个 annotated tag 的远端 URL、tag object id、peeled commit、
核验时刻、响应哈希和 receipt 哈希；peeled commit 分别等于 cohort 的 protocol/code commit。
同一 cohort 还冻结 parser、规范化、去重、修订、模型、评价、展示、语义校验器和环境锁的结构化
代码清单，后续五类记录中的代码身份必须逐项相等，不能看到未来目标后换实现。

每期正式抓取必须在 `Q=T-15min` 到达后开始，并在 T 前完成源快照、P0/P1/PP 预测、封印和远程
时间戳；只能纳入 `origin_time<=Q` 且 `available_at/first_seen_at<T` 的记录。源封印绑定来源、
请求、许可、抓取时刻、HTTP 状态和响应头、原始字节与哈希、parser/schema、配置、首次可用与
去重代码、三层计数与哈希、新鲜度、P0/R30/RP30 有序事件集合及上一期记录哈希。

所有 schema 要求 `remote_timestamp` 的正式记录都用同一 RFC3161 core：按
`seismoflux_canonical_json_v1` 规范编码，并且只且恰好排除顶层 `timestamp_attempt_evidence`、
`remote_timestamp`、`content_sha256`；禁止递归排除，也禁止嵌套 RFC3161 proof/token 字段。
原始记录文件必须逐字节等于对严格解析对象重新得到的唯一规范 JSON 字节；仅仅“能解析”或
“规范化后哈希相同”不够。`record_core_frozen_at_utc` 与 `timestamp_deadline_utc` 必须保留在
core 中，proof deadline 与顶层 deadline 相等；选中尝试满足
`core_frozen<=request_started<=genTime<=response_received<=deadline` 且
`genTime<deadline`。cohort deadline 等于 `valid_from`；成熟真值、真值修订和评价冻结等于
`core_frozen+5min`。

Issue 使用两段不可混淆的证明。完整按时预测候选必须最迟在 `T-5min` 冻结，候选 TSA 的
`genTime<T-5min`；成功时才原子安装为 `on_time`。若在形成候选前已失败，则不生成预测；若完整
候选已经生成但两家 TSA 都失败，则只保留本地受限、内容寻址的失败候选，明确
`prediction_generated=true`、`prediction_installed=false`。两种失败都必须最迟在 `T-4min`
冻结最终 `missed_issue` 审计 core，并用与候选尝试分离的 RFC3161 请求在 T 前取得审计 token；
审计 token 失败即暂停科学连续性，不能安装正式漏期记录或继续 cohort。这样既证明预测在截止前
确实存在，也不能把时间戳失败的预测冒充已发布预测。

TSA 有序主备固定为 DigiCert 主、Sectigo 备；只有主站形成可验证失败证据后才能尝试备站，不能
临时换第三家或改变顺序。协议标签预先固定两家 TSA 的端点、允许 policy OID、精确 DER 信任锚
和注册表哈希；验证仅声明在 token `genTime` 上沿该固定锚的离线路径有效，不联网检查 CRL/OCSP，
因此不得声称当前未吊销或完整 PKI 非吊销保证。token 的 message imprint、证书路径、策略 OID、
时序和原始字节哈希必须机械验证。最终 `content_sha256` 仍按仅排除自身的记录哈希规则独立计算。
token 是哈希绑定附件，不是第六类正式记录。

验证器固定为锁文件中的 Python `cryptography==49.0.0` 与 `asn1crypto==1.5.1` 实现，不依赖
系统 OpenSSL CLI；`.tsq/.tsr` 在 Git 忽略的本地受限目录
`data/interim/stage2p/timestamp_attachments/` 中，按 record type、preimage 和 attempt 序号
create-only 保存并重新打开验字节。

五类正式记录必须携带同一 `code_manifest_sha256`。外部
`TrustedReleaseManifest` 必须来自已远端回读的 annotated tag/peeled commit，而不能从当前记录
或未验证工作树自证：Stage 2P-1A 只允许 `stage2p1b_required`，Stage 2P-1B 合成全链通过后才允许
`stage2p1b_synthetic_accepted`。代码提交中的协议配置和记录 schema 必须与协议提交逐字节一致，
并以两个提交各自的 blob/hash 身份证明。manifest、tag receipt、source/prediction/truth 嵌套
身份以及 prediction/replay visualization 的规范前像都明确排除自己的 ID/hash 字段后重算，
不得形成自引用或接受任意自报哈希。

跨五类正式生命周期验收必须按冻结的工件注册表取得每期原始响应、规范化/去重/因果视图、事件集、
固定格几何、三路 float64 密度、完整评分与排名行、报警前缀与 mask、forecast bundle、静态/交互
展示，以及 selected-exposure、alarm-area、target、cluster、point/region/cluster contribution、
bootstrap、effect/result bundle 工件。目标盲 exposure 清单必须逐行冻结 issue/horizon、选择顺位、
prediction seal、窗口和真值可用状态；报警面积清单必须逐期从 PredictionSeal/ForecastBundle
重建 P0/P1/PP 三路实际面积和最大差。每项都要按固定序列化、row schema、排序与内容哈希从实际
字节机械重建；缺工件、角色/profile 不符或只剩自报摘要时失败闭合。

正式状态只允许五类只追加记录：

1. `TargetCohortDefinition`；
2. `IssueInputSnapshotRecord`；
3. `MatureTruthSnapshotRecord`（temporally independent mature snapshot 或不可用状态）；
4. `TruthRevisionRecord`；
5. `EvaluationFreezeRecord`，用 `phase=input_freeze|result_seal` 闭合唯一正式结果，不增设
   第六类记录。

`IssueInputSnapshotRecord.status=on_time|missed_issue`。`on_time` 必须嵌套源快照、P0/P1/PP
预测封印、完整报警前缀、哈希和 `valid_from`。`missed_issue` 不安装 source snapshot 或
prediction seal；候选形成前失败时 `prediction_generated=false`，候选 TSA 失败时则允许且必须
引用 T-5 前已冻结的本地受限失败候选，但仍固定 `prediction_installed=false`，公开状态不能包含
密度、报警格或伪造的按时字段。
`scheduled_issue_sequence` 对每个规则周四递增并包含 missed；`on_time_issue_sequence` 只对按时
issue 递增，missed 时为 null。52/104 检查点只按 on_time 序号，漏期不消耗 104 个按时期上限。
scheduled 序号最多 130；第 130 个规则周四仍未取得 104 个按时 issue 时以证据不足停止。
这个 130 上限优先于第 52 期检查：若第 130 期才累计到 52–103 个按时期，也只能走 cap stop，
不得打开或延迟正式 look。
记录创建后不得覆盖、截断、删除、重命名或重排。原生对象只规范编码一次；禁止把已经规范编码的
JSON 解码后再送入规范编码器。

每个 `(issue,horizon)` 的 ComCat 真值窗口为 `(T,T+h]`，在 `T+h+30d` 首次成熟。真值必须由
与 issue 输入抓取分离的请求取得，称为 temporally independent mature snapshot（时间上独立
抓取的成熟快照）；这里不声称事件统计独立，也禁止复用 issue 输入响应。真值查询在本地严格过滤
`T<preferred_origin_time<=T+h`。固定重试时刻为 `0h、+6h、+24h、+72h、+168h`，按顺序取第一
个完整成功响应并停止，只有全部失败时才实际完成五次并追加
`MatureTruthSnapshotRecord.status=truth_snapshot_unavailable`，该 exposure 不可评分，不能当作
零事件、临时加试或用后续 issue 替补。每个实际 `input_freeze` 检查点只允许一次 formal-freeze
尝试：第 52 个按时 issue 可有一次；只有 52 的基本样本门未满足且从未打开效果行时，第 104 个
按时 issue 才允许再有一次。不得重试、替换失败快照或按结果改查法；52 的 manifest/revision 在
104 形成后仍永久只追加保留。任何修订只能新增 `TruthRevisionRecord`，不能覆盖成熟快照。
正式评价统一采用当次 `input_freeze` 绑定的单一 formal-freeze source snapshot 的 preferred
origin/magnitude/location/identity；
若 preferred origin 修订跨过 `T`、`T+h` 或相邻 exposure 边界，必须记录旧归属和修订后唯一
归属，按修订后 preferred origin 确定性重归属且不得双计。后续修订也不得回填、重算或更换已
封存的模型版本、输入、预测数组和报警前缀。
每次 formal freeze 先用一条覆盖该检查点全部入选 exposure 的规范查询取得**唯一一份原始
GeoJSON 响应**；官方接口没有可供锁定的 provider-native catalog version，因而身份由规范请求、
响应头、原始响应字节和抓取时刻共同给出，禁止分批拼接、动态分页或临时替换数据源。冻结原始字节
后在本地一次性重建五张规范 JSONL 表：全响应规范化行、全局去重行、preferred 字段行、全部窗口
归属行、窗口目标绑定行。每张表固定 row schema、排序、序列化、行数、文件/内容哈希；全局震群只
在评价冻结层对入选窗口的唯一 M5–6 目标并集计算，不进入单个 TargetSet 或 TruthRevision 身份。

当次 `TruthRevisionRecord` 只把成熟快照和历史 revision 链与当次五表作确定性比较；新增、删除、
科学字段修改、去重合并/拆分必须由可观测的前后行给出，不把无法区分的“晚报”或“源修正”当作
可验证原因。当前 `EvaluationFreezeRecord` 只引用当前 manifest；更早检查点的 manifest/revision
链继续按原哈希保留，禁止把两个检查点的表跨 manifest 拼接。TruthRevision 使用独立 revision
attempt，不复用成熟真值的 0/6/24/72/168h retry 语义。
真值成熟或修订后必须另增静态 SVG 和完全离线交互回放证据，引用原 prediction visualization
哈希并绑定成熟目标集；原起报图/HTML 不得修改。逐事件叠加默认只在本地受限范围展示，且回放
证据必须记录零外链、零远程脚本、零网络抓取和零未成熟目标叠加。

局部 Mc 过高只影响相应本地支持单元，不得抬高其他区域阈值或改变其他区域。三模型共享冻结的
研究区、39 区映射、50/25/12.5 km 对齐网格、局部资格和相同事件率；三路 KDE 训练事件只取
G1-LS 冻结支持格内合格事件，连续密度也只在该支持域作边界归一，支持域外不补极小密度。信息
增益只用支持域内唯一 M5–6 目标；严格召回
仍用全研究区唯一 M5–6 目标作分母，支持域外目标对三模型共同记未命中。报警统一按 25 km 格
“质量/精确裁剪面积”排序，以冻结并列规则选择累计精确面积不超过 600,000 平方公里的完整前缀。
600,000 只是上限；每个模型都封存完整排名、实际前缀、格数、实际面积和终止原因，不得切割末格、
跳格或扩区凑满。未耗尽时必须同时封存下一排名格的位置、ID、精确面积和行哈希，并证明它放不进
剩余预算；三模型实际面积最大差由三路面积重算且不超过一个 25 km 完整格的 625 平方公里。
正式 input freeze 还必须嵌入目标盲 `AlarmAreaManifest`：每个被任一入选 exposure 使用的
prediction seal 恰好一行，按 scheduled issue 顺序绑定 ForecastBundle 和三路 float64 面积。
结果阶段的 `AlarmAreaComparison` 只能由该清单重算；任意裸 64 位摘要、缺期或替换 seal 都失败。

7/30/90 天分别只按 on_time 状态、issue time 和 horizon 从早到晚贪心选择不重叠 exposure，
并保留零事件 exposure；事件数、坐标、命中和分数均不得影响选择。已入选 exposure 若为
`truth_snapshot_unavailable`，禁止用后续 issue 替补或重跑贪心，该 horizon 在该检查点不可评价。
每个 input freeze 必须先嵌入 `SelectedExposureManifest`，从全部按时 prediction seal 的有序轴
机械重建三层选择；三个 horizon 摘要、truth-availability 摘要和 formal window binding 都只能
从同一清单的确定性投影得到，不能由操作者填写摘要后换样本。
主样本门为至少 20 个唯一去重 `M5_6=[5.0,6.0)` 物理事件和至少 10 个独立震群块；M6+ 只作
探索。震群块固定为事件间起源时间差不超过 30 天且 WGS84 距离不超过 75 km 的无向连通分量。
实现精确为绝对起源时差 `<=2592000` 秒、`pyproj.Geod 3.7.2` 的 WGS84 `inv` float64 距离
`<=75000.0m`、零比较容差，然后取传递闭包；component id 是固定命名空间与排序唯一 event id
规范字节的 SHA-256。

每个 horizon 的 `A-B` 信息增益精确估计为支持域内唯一 M5–6 目标的
`Σln(f_A(x_e)/f_B(x_e))/N_h_supported`（nats/event）；`f` 是支持域连续边界归一后在事件投影
坐标直接求值的密度，禁止格查找/插值/格均值，共同率和积分补偿相消。模型严格召回为全研究区
唯一 M5–6 目标的
`Σhit_M(e)/N_h_all_region`，其中报警区是 600,000 平方公里上限下的实际完整格前缀。两个指标
都先逐 horizon 计算，再按 7/30/90 天固定各 `1/3` 宏平均。无支持域目标、全区分母为零、已选
真值不可用或任一坐标密度为零/非有限值时该 horizon 不可评价，不得加 density floor；对数比用
deterministic float64 pairwise summation，绝对容差 `1e-12`。

使用 PCG64 根种子 147 和 2,000 次配对震群块 Bootstrap。若有 B 个块，每次有放回抽 B 个；
同一抽样用于全部模型、对比和 horizon，重复块按乘数重复全部贡献，并重算每个 horizon 的支持域/
全区分母和宏平均。唯一确认性 family 有四个端点：P1-P0 与 P1-PP 的宏信息增益和严格召回增益。
使用 Bonferroni 95% familywise 双侧同时区间，每端点取 98.75% percentile 区间，即用 NumPy
`method=linear` 取 `0.625%` 与 `99.375%` 分位数；四个下界必须同时大于 0，且 P1-P0 严格召回
点估计至少 `+5` 个百分点。四端点还必须通过冻结 39 区的可加闭合、分别移除最大正贡献区域后
仍为正、以及分别移除最大正贡献震群块后仍为正。
随机流必须直接由 `numpy.random.Generator(numpy.random.PCG64(147))` 初始化；命名空间只作身份
标签，不参与派生第二个种子。打开效果行前一次生成并冻结完整 2,000×B 索引矩阵，禁止丢弃或
重抽复本；任何复本在任一 horizon 的支持域或全区分母为零时，立即在盲态记证据不足，不得看效果。

最多只看一次确认性结果。第 52 个按时 issue 的全部 90 天窗口成熟后，先追加
`EvaluationFreezeRecord.phase=input_freeze`，只绑定 cohort/revision、目标无关 exposure、可评价
性、N/B 水位、预测/代码/协议/环境/随机流和输出身份，不含效果行。只有三个 horizon 都可评价、
N>=20 且 B>=10 时才授权打开效果；随后必须立即追加且只能追加一个直接引用该 input hash 的
`phase=result_seal`，一次封存四端点、区间、稳健性、总门和决定，中间不得插入另一套输入或第二
次计算。第 52 期门不足时不创建 result seal、继续盲积累到第 104 个按时期；第 104 期仍有 horizon
不可评价、N<20 或 B<10 时记 `evidence_insufficient` 并停止 P1。若第 52 期已打开结果，无论
成败都不能在第 104 期再看。任一路径最多一个 `result_seal`；闭合失败即停止，不得重跑补救。
input freeze 的选中 TSA 响应必须先于 `effect_rows_opened_at_utc`；effect open 又必须先于
result core freeze 和 result TSA。result seal 除预登记的 phase/sequence、链哈希、结果与
时间戳证明字段外，所有 input freeze 科学输入都必须逐项规范字节相等。
预测静态图和离线交互页随每个按时 issue 立即封存和展示；累计命中、信息增益、置信区间和胜负
在唯一正式 look 前始终密封。
`horizon.evaluable` 必须由完整 exposure、真值可用 exposure、两个事件分母、震群数和密度有限
正值机械重算；false 必须带与证据一致的结构化原因，不能由操作者自报以绕过第 52 期门。评价代码
commit/hash 与 `uv.lock`/`pyproject.toml` 哈希在 input freeze 就冻结，result seal 的全部输入逐项
复制并与 nested input hash 闭合。formal freeze 成功时 N/B、target、window、cluster 和 horizon
字段必须为实际值；仅机械证明入选 exposure 为空时允许规范空集。count/query/parse/本地冻结失败
或 scheduled-cap 未正式冻结时，这些科学字段必须为 null/unavailable，禁止用 0 或空 TargetSet
冒充已观测零。

解封后无论成功或失败都必须生成精确 `ResultBundleManifest`。成功分支才允许完整 effect、
alarm-area comparison、四端点 bootstrap、endpoint 和 robustness 槽；失败分支按
`effect_rows_open→alarm_area_comparison→bootstrap→endpoint_evaluation→robustness_evaluation
→result_bundle_install→result_seal` 固定状态机，只保留此前已经原子完成的完整槽，当前阶段的
部分产物仅列入排序去重的 audit 哈希，未完成槽必须为 null。`InvalidConfirmatoryResult` 引用的是
该 invalid manifest 的精确文件 SHA，不代表成功结果；仍永久停止、不得重跑或针对测试调参。

大型任务至少为用户保留 2 个物理 CPU 核，默认
`worker_count=min(8,max(1,physical_core_count-2))`，并限制 BLAS/OpenMP 内层线程。GPU 只允许
作为与 CPU 数学等价的加速器；输出不能在冻结容差内等价并产生相同报警前缀时停用 GPU，不能为
用 GPU 而改变模型或增加候选。

公开仓库允许代码、协议、配置、测试、环境锁、来源 URL、许可说明、请求模板、哈希、计数、聚合
25 km 相对强度、报警前缀以及许可允许的叠加。USGS 自有数据默认属于美国公共领域，但 ComCat
伙伴来源可能存在例外；因此 ComCat 原始字节、逐事件规范化/去重行、精确坐标、本地历史目录和
受限明细始终默认只存本地，公开事件叠加必须逐项做来源与许可复核。未获公开资格不阻断本地受控
研究；凭据和绝对受限路径永不入库。

任一合同不能闭合则不进入 2P-1B；纯合成链出现 foundational P0 则停止并复审；真实期使用 Q 后
起源事件、T 后可用数据、三模型不同快照/支持规则/面积上限规则、记录或历史预测可被改写时使当前
版本失效并停止。官方或目标无关证据按时供给失败可记 missed，持续中断则暂停并作目标盲修订。
正式门失败、104 个按时期仍不可评价/样本不足或稳健性失败均停止 P1、保留 P0，不得针对已观察
cohort 改窗口、权重、带宽、面积、震群、统计门或模型，也不得用旧 Stage 2S、异常、Hawkes、
神经网络或锁定测试补救。

Stage 2P 是研究性前瞻证据采集，不是阶段 10 业务影子，不表示 G8 通过。异常挑战者必须在任何
P 效果指标解封前另立协议和有效日，并只在两者较晚 `valid_from` 之后的共同新 cohort 比较贡献。
Stage 2P-1A 的验收栏保持待定；不得在实际验收、提交、推送和远端标签回读前声称完成。
未来结论只适用于冻结的 ComCat 获取/修订链和固定 0.5 候选，不能外推为全部近期地震模型。

### 阶段3：动态异常轨迹特征库

任务：

- 重建全部报告期的异常状态；
- 生成多尺度、多时间窗、多学科轨迹特征；
- 建立观测覆盖和缺报特征；
- 验证任一起报日不使用未来报告。

交付：`anomaly_state_history.parquet`、`anomaly_feature_store.parquet`、特征字典和审计图。

验收：随机抽取起报日可以完整解释每个特征的来源记录。

### 阶段4：可解释异常增量模型

阶段 4 分成两个不可合并的门。先做 **阶段 4A 开发科学筛查**：

- 以阶段 2R 已通过的 75 km KDE 为当前最好合法背景；
- 只比较 `KDE`、`KDE+覆盖控制`、`KDE+单期异常`、`KDE+动态异常`；
- 使用同一支持域、目标、未来窗口、积分域和 600,000 平方公里报警面积；
- 只允许一次包含三个滚动折、7/30/90 天、时间置乱、空间置乱和物理事件 Bootstrap 的开发科学 attempt；
- 按地区和窗口报告增量，并执行去最大贡献区的稳健性检查；
- 不读取独立正式验证目标或锁定测试，不加入新 ETAS 候选、树模型、弱监督、自监督、断层交互或大型神经网络。

阶段 4A 当前唯一执行协议为 `0.4.4`，标签为
`v0.3.4-kde-anomaly-increment-{protocol,code,result}`。`0.4.2` / `0.4.3` 及其
`v0.3.2-*` / `v0.3.3-*` 标签因目标读取前发现可执行性矛盾而保留为 `superseded` 历史证据，
不得再提供执行授权：公开背景 registry 只作身份与审计摘要，不是 KDE 验证或推理 payload；历史
模型路径只作不要求存在且运行时禁读的未跟踪声明。唯一注册目录读取会话必须先从同一内存 catalog
中选择 `origin_time_utc/available_at <= 2019-12-31T16:00:00Z`、研究区内的全部目录震级，重建并
核验 `fold_4` 支持与 Mc；支持重建前按 `M>=4` 过滤禁止。然后 KDE 训练才在重建的保留支持域内取
`M>=4` 事件，以预选 75 km 带宽重物化 KDE，并让该 catalog 继续供训练和评分；禁止第二次打开、
重选带宽、改变 Mc/支持域/积分域或使用 `final_validation`。本地支持合同固定为
`data/manifests/background_local_support_manifest.json`（SHA-256
`632278416dfc717dbcb9d2eae048a4f13cdf7737a31e6e5e704a9dd17d7cef8d`，
manifest ID `local-support-bundle-69cecbee9093a21d`），重物化必须按机器协议逐项复现事件数、时长、
日率、训练/参数身份、归一化质量、三网格收敛、support/domain ID、Mc、面积和有序 cell-mass
receipt；2019 截止后变化不得改变背景身份，历史入选行变化必须失败关闭。

`0.4.4` 进一步冻结四个受限空间工件的项目相对路径、byte count、整文件 SHA-256、Arrow/JSON
schema 与跨工件关系，并冻结 ASCII exposure/issue ID、`Asia/Shanghai` 零时到 UTC 的转换以及
`(T,T+h]` 窗口。正式 adapter 只能从已认证阶段 3 表构造 issue tables、snapshots、既有 query-grid
DTO 和 state-to-stratum mapping；connectors 与 zone geometry 只核验不返回，禁止据此重建格网、
候选或区域。精确合同见
`docs/phase4_kde_target_blind_executability_amendment.md`。代码标签前必须用纯合成数据证明
observed、时间置乱和空间置乱走同一条“重建—9/17/22 特征组装—仅 fit 行预处理—ridge 重拟合—
assessment prediction 冻结—无目标统计量”路径；若这一次验收仍出现新的基础性 P0，停止异常路线
并保留 KDE，不再追加工程层。

2026-07-30 代码级终审已经触发上述硬停止：`cell_mapping` 中的
`cell_id → construction_zone_id` 在正式 adapter 核验后被丢弃，冻结四字段接口无法支持强制的
39 区贡献和 leave-one-region-out；同时拟合草稿没有按 7 天 composite-midpoint 展开补偿积分。
纯合成链虽有 229 个工程测试通过，但阶段总验收为 `FAIL`，不构成预测效果证据。开发目标、
独立验证和锁定测试读取仍为 0，唯一开发 attempt 未消耗；`v0.3.4-*-code/result` 不得创建，
异常增量路线停止并保留 75 km KDE。后续只能先做目标盲科学路线复审，再把选定的新问题、评价门
和停止条件写入本唯一蓝图，完成独立验收、提交和推送后才可实施；不得用 `0.4.5`、阶段 4B、
阶段 5、复杂模型或锁定测试绕过本停止结论。详细证据见
`docs/phase4_kde_0_4_4_synthetic_acceptance_and_foundational_stop.md`。

target-read ledger 是 Git-ignored 的单个 canonical `.json` mapping，只允许在已注册且身份相同的
attempt ledger 核验后，从零态原子 CAS 到 `logical_open_count=1`，并记录 entry hash 与前序零态
hash；打开态不可改写、回退或再次打开。背景与 Stage 3 置乱代码只准使用 `0.4.4` 精确整文件哈希/
symbol allowlist；完整 `local_support_runtime.build_local_support_runtime` 仅可作纯合成测试参照，
正式运行只能用底层原语构造 `fold_4`。旧 R2 scoring/formal orchestrator、`placebo_source.py`、
`placebo_runtime.py`、带宽选择、旧 gate 和旧随机语义继续禁止；`placebo.py` 仅为哈希封印的
import-only 传递依赖，其旧 DTO 与 build 函数不得直接调用。本修订不改变下列任何科学门。

阶段 4A 交付：系数与效应图、信息增益、置乱分布、Molchan 图、固定面积召回曲线、目标盲预测展示和本地历史回溯展示。

阶段 4A 的通过必须包含 600,000 平方公里固定面积严格召回至少提高 5 个百分点且 95% 区间排除无改善；“达到同召回所需面积下降”只作描述，不能替代本门。动态轨迹 `B2` 未超过单期快照 `B1` 时，只否定动态轨迹；若 `B1` 相对 KDE 和覆盖控制独立通过全部信息增益、置乱、固定面积和跨折/跨区门，可保留 `B1` 作为唯一待独立验证候选。只有 `B1`、`B2` 都没有合法增量时，才把当前异常路线判为未发现增量。

阶段 4A 只判断“是否出现值得一次独立验证的异常增量线索”，不得宣称 G2/G3 已正式通过。若两个异常候选都失败或证据不足，立即形成负结果或证据不足报告，科学价值决策为调整或停止，并暂停复杂异常模型；不得换折、改面积、改特征或查看同一开发成绩后重试。

只有阶段 4A 通过并完成科学价值复审、提交、推送和结果标签后，才可另立 **阶段 4B 独立验证**。阶段 4B 必须在打开独立验证目标前重新冻结协议、代码、输入和唯一 attempt；其验收才对应 G2，并结合阶段 4A 的跨折证据判断 G3。阶段 4B 不通过时形成可信负结果，停止异常复杂化，不得转入阶段 5 或大型模型。

### 阶段5：长期构造先验

任务：

- 断层交叉映射；
- 距离、密度、离逝率和长期得分特征；
- 真实迹线与简化几何敏感性；
- 与动态异常交互消融。

交付：构造特征库、空间先验图和分窗口对照。

验收：G4。

### 阶段6：弱监督和自监督候选模型

任务：

- 注意力多实例模型；
- 不规则时间序列自监督编码；
- 与GAM、梯度提升模型同条件比较；
- 注意力稳定性和实例贡献审计。

交付：模型卡、消融、特征贡献和失败案例。

验收：G5。未超过简单模型则不采用。

### 阶段7：标记震级与分层模型

任务：

- M5以上事件强度；
- 条件震级分布；
- M5-6和M6+积分结果；
- 区域、窗口和震级层级汇聚；
- 小样本不确定性。

交付：震级模型卡、样本水位和校准图。

验收：H4门控。M6+样本不足时保持探索状态。

### 阶段8：危险区集合优化

任务：

- 连续强度数值积分；
- 等面积网格收敛；
- 面积和区域数Pareto前沿；
- 连通、紧凑、碎片和重叠约束；
- 严格和70 km容差覆盖。

交付：危险区GeoJSON、区域报告、Pareto图和空间对比图。

验收：G6。

### 阶段9：锁定测试和最终模型选择

任务：

- 冻结配置、代码和模型；
- 一次性运行锁定测试；
- 对照所有基线；
- 生成模型采用决定；
- 不根据测试结果继续调参。

交付：最终验收报告、模型注册表和不可变测试清单。

验收：G7。

### 阶段10：交互展示和前瞻运行

任务：

- 静态图和交互图；
- 冻结模型推理；
- 每期永久归档；
- 窗口成熟调度；
- 前瞻累计仪表板。

交付：用户入口、交互图、历史版本浏览、运行手册。

验收：G8只能在足够前瞻数据成熟后评价。

## 27. 推荐仓库结构

```text
SeismoFlux/
├─ AGENTS.md
├─ README.md
├─ pyproject.toml
├─ uv.lock
├─ configs/
│  ├─ base.yaml
│  ├─ data_sources.yaml
│  ├─ research_protocol.yaml
│  └─ operating_points.yaml
├─ data/
│  ├─ manifests/
│  ├─ contracts/
│  ├─ interim/
│  └─ processed/
├─ benchmarks/
│  └─ legacy/
├─ src/seismoflux/
│  ├─ cli.py
│  ├─ config.py
│  ├─ data/
│  ├─ features/
│  ├─ models/
│  │  ├─ background/
│  │  ├─ anomaly/
│  │  ├─ tectonic/
│  │  └─ marked/
│  ├─ intensity/
│  ├─ regions/
│  ├─ evaluation/
│  ├─ forecast/
│  ├─ registry/
│  └─ visualization/
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  ├─ leakage/
│  └─ regression/
├─ scripts/
├─ docs/
├─ models/
│  ├─ registry/
│  └─ frozen/
├─ outputs/
│  ├─ experiments/
│  ├─ backtests/
│  ├─ prospective/
│  └─ releases/
└─ .github/workflows/
```

原始大文件放在仓库外或受控数据目录，Git只跟踪清单、配置、代码、文档和小型验收摘要。

## 28. 推荐技术栈

基础：

- Python 3.11；
- `uv`或等价锁文件工具；
- NumPy、SciPy、Polars或Pandas、PyArrow、DuckDB；
- GeoPandas、Shapely、PyProj；
- scikit-learn、statsmodels或可靠GAM实现；
- PyTorch仅用于通过门控后的MIL或时间编码；
- Pydantic和Pandera用于数据契约；
- pytest、Ruff、mypy；
- Plotly或自包含HTML用于交互展示。

原则：

- ETAS等核心领域逻辑优先采用经过验证的实现；
- 不为追求框架统一而手写所有数值算法；
- 所有依赖固定版本；
- 核心模型提供纯CPU路径；
- 深度模型和GPU是可选加速，不是项目启动前提。

## 29. 配置示例

```yaml
project:
  name: SeismoFlux
  timezone: Asia/Shanghai
  random_seed: 147

study_area:
  polygon: data/processed/china_mainland.geojson
  include_external_trigger_buffer_km: 300
  equal_area_crs: "custom_albers_china"

forecast:
  horizons_days: [7, 30, 90, 180, 365]
  magnitude_bins:
    - {id: M5_6, min: 5.0, max_exclusive: 6.0}
    - {id: M6_plus, min: 6.0, max_exclusive: null}

integration:
  base_cell_km: 25
  refine_cell_km: 12.5
  convergence_cells_km: [50, 25, 12.5]

regions:
  component_counts: [5, 8, 10, 12]
  union_area_budgets_km2: [300000, 450000, 600000, 750000, 960000]
  operational_max_components: 10
  operational_max_union_area_km2: 960000
  strict_coverage_buffer_km: 0
  tolerant_coverage_buffer_km: 70

parallel:
  reserve_physical_cores: 2
  max_workers: 12
  nested_parallelism: false
```

## 30. 命令行契约

```powershell
seismoflux inventory --config configs/base.yaml
seismoflux ingest --config configs/base.yaml
seismoflux validate-data --config configs/base.yaml
seismoflux build-background --config configs/base.yaml
seismoflux build-anomaly-history --config configs/base.yaml
seismoflux train --experiment anomaly-gam-v1
seismoflux backtest --experiment anomaly-gam-v1
seismoflux optimize-regions --experiment anomaly-gam-v1
seismoflux freeze --experiment anomaly-gam-v1
seismoflux forecast --issue-date YYYY-MM-DD --model frozen/model-id
seismoflux mature --as-of YYYY-MM-DD
seismoflux render --issue-id ISSUE_ID
seismoflux validate-release --issue-id ISSUE_ID
```

每条命令必须支持 `--dry-run` 或等价检查模式，并写出机器可读运行清单。

## 31. 测试要求

### 31.1 单元测试

- 时间和时区解析；
- 空结束时间的右删失处理；
- 多期异常状态重建；
- 测地距离和投影面积；
- 震级档边界；
- 预测窗口积分；
- 物理地震去重；
- 点在多边形内和70 km缓冲覆盖；
- 区域并集面积和重叠处理。

### 31.2 性质测试

- 条件强度始终非负；
- 长窗口积分不小于相同震级档的短窗口积分；
- 输出区域不超过数量和面积预算；
- 同一地震最多计一次召回；
- 网格加密后总积分收敛；
- 无异常输入时增量模型退化为背景模型。

### 31.3 防泄漏测试

- 起报日后首次可用的异常不得进入特征；
- 未来结束时间不得回填；
- 未来报告缺席不得提前使用；
- 训练、验证和测试物理事件无交叉；
- 目标震中不得影响推理候选和积分网格细化；
- 锁定测试配置不可被训练命令修改。

### 31.4 集成和回归测试

- 小型合成数据可完成从接入到出图全链；
- 固定样例输出哈希稳定；
- 冻结模型推理不触发训练；
- 历史预测归档不可覆盖；
- Windows路径和中文文件名可正常处理。

## 32. 计算和并行策略

- 数据处理使用列式存储和向量化计算；
- 跨滚动折、参数配置和空间块可以并行；
- 禁止内外两层同时占满所有核心；
- 默认保留2个物理核心供系统使用；
- 每次运行记录CPU、内存、耗时和并行度；
- 长任务分阶段保存检查点；
- 启动前估算任务数量，运行中输出进度和最近心跳；
- 训练是否卡死通过进程、CPU利用率、输出更新时间和阶段清单共同判断。

## 33. 可视化和用户界面

### 33.1 静态图

每个震级档和窗口输出：

- 条件强度面；
- 候选危险区和顺位；
- 强度峰值；
- 国界、断层和地块边界；
- 数据截止时间、模型版本、总面积和证据等级；
- 明确标注“条件强度”或“期望事件强度”，不得误写为绝对概率。

### 33.2 交互图

支持切换：

- 起报日期；
- 震级档；
- 预测窗口；
- 背景、背景加异常、背景加异常和断层；
- 强度面、危险区、异常点和上一期对比；
- 模型版本、前瞻状态和样本水位。

界面要求：

- 色标从低到高蓝到红；
- 候选区边界足够深；
- 上一期区域用紫色虚线；
- 图框外为白色并裁剪；
- 支持局部放大，线宽和符号大小不随缩放放大；
- 底图图例顺序统一；
- 异常按学科显示不同符号；
- 候选区顺位与证据等级分开说明；
- 提供一句话结果解读和限制；
- 桌面及窄屏均无横向溢出。

### 33.3 历史版本

交互图读取不可变的前瞻归档。任何起报版本生成后永久保留，不能因后来是否命中而删除或替换。

## 34. 周度运行与重新训练

### 34.1 新异常表到来

默认流程：

1. 接入并验证新表；
2. 更新异常状态和动态特征；
3. 使用冻结模型推理；
4. 生成候选区、图件和归档；
5. 不重新训练。

### 34.2 新地震到来

1. 更新目录并去重；
2. 标记哪些历史窗口已经成熟；
3. 更新前瞻评估，不修改旧预测；
4. 判断是否达到重训触发条件。

### 34.3 重训触发

满足以下任一条件才进入候选重训：

- 新增独立目标地震达到预登记数量；
- 每半年或每年一次的计划审查；
- 输入分布发生明显漂移；
- 数据解析或特征定义发生版本变化；
- 背景目录完整度发生实质变化。

重训生成挑战者模型，不直接替换冠军模型。挑战者必须通过完整历史回测、锁定验证和前瞻门控。

## 35. 前瞻归档结构

```text
outputs/prospective/YYYY/MM/DD/ISSUE_ID/
├─ issue_manifest.json
├─ input_manifest.json
├─ model_manifest.json
├─ intensity_m5_6.parquet
├─ intensity_m6_plus.parquet
├─ regions.geojson
├─ regions.csv
├─ static_maps/
├─ interactive_map.html
├─ validation.json
└─ maturity_status.json
```

归档目录写入后只允许追加成熟状态和评估文件，原始预测强度、区域和模型清单不可改写。

## 36. Git和分支治理

新项目不要再次形成几十个长期版本分支。

推荐规则：

- `main`只放通过阶段验收的稳定代码；
- 每项工作使用短生命周期 `codex/<feature>` 分支；
- 同一时间只保留一个主要挑战者分支；
- 每个阶段完成后合并、打标签并删除已完成分支；
- 未采用实验写入实验注册表后删除代码分支，结果由标签和报告保留；
- 一个工作树只绑定一个分支；
- 任何工作树存在未跟踪模型或输出时不得删除；
- 大型模型和结果不直接提交Git，只提交清单、摘要和可复现配置；
- 正式前瞻版本用标签和GitHub Release固定。

推荐标签：

- `v0.1.0-data-contract`；
- `v0.2.0-background-baselines`；
- `v0.3.0-anomaly-increment`；
- `v0.4.0-region-optimizer`；
- `v0.5.0-locked-evaluation`；
- `v1.0.0-prospective-shadow`。

## 37. 模型卡和实验注册

每个实验必须生成：

- `experiment_id`；
- 研究假设；
- 数据截止日期和哈希；
- 特征列表；
- 模型和参数；
- 训练、验证、测试分界；
- 随机种子；
- 基线；
- 所有指标和置信区间；
- 是否通过门控；
- 失败原因；
- 代码提交和环境锁文件；
- 是否允许进入前瞻影子。

只汇报最优实验是不允许的，所有正式尝试都必须登记，防止选择性报告。

## 38. 风险和停止条件

### 38.1 主要风险

- 异常时期短，独立强震样本少；
- 异常与地震的对应关系未知；
- 报告制度变化可能伪装成异常数量变化；
- 地震活动聚集可能解释大部分可预测性；
- M6+样本无法支撑复杂模型；
- 长窗口之间高度重叠；
- 反复使用同一测试段会造成隐性过拟合。

### 38.2 必须停止的情况

- 数据泄漏无法排除；
- 背景模型不能稳定复现；
- 动态异常不超过时间置乱对照；
- 改善只出现在单一切分或单一地区；
- 召回提高完全来自报警面积扩大；
- 深度模型不超过简单可解释模型；
- 锁定测试失败后试图继续针对该测试集调参。

停止并形成可信负结果，比继续生成新版本号更有价值。

## 39. 项目最终验收定义

### 39.1 最低成功

- 完成无泄漏的数据和前瞻归档体系；
- 建立可复现的最佳合法背景，并对 ETAS 可评价性形成可审计结论；
- 定量回答动态异常是否提供增量；
- 在固定面积预算下输出可解释危险区；
- 即使异常无增量，也形成可信的负结论。

### 39.2 方法成功

- 异常增强模型在滚动验证和锁定测试中超过当前最佳合法背景；ETAS可评价时还必须同时超过ETAS；
- 在同面积条件下严格区域召回有实际意义的提高；
- 置乱检验不能复制该提高；
- 改善不局限于一个地震或一个地区；
- 简单模型和复杂模型的采用有明确证据。

### 39.3 业务影子成功

- 至少完成连续、不可变的真正前瞻运行；
- 输入、模型和结果可以完整追溯；
- 达到预登记的样本和成熟窗口水位；
- 报警面积、召回和稳定性处于可接受范围；
- 用户界面不会把强度误解为绝对概率。

50 km精定位不作为v1.0的发布门槛。只有区域召回达到稳定水平后才单独立项。

## 40. 首轮执行清单

新项目代理应严格依次执行：

1. 创建 `SeismoFlux` 空目录和独立Git仓库。
2. 写入本交接文档和项目级 `AGENTS.md`。
3. 建立Python 3.11环境、`pyproject.toml`和锁文件。
4. 创建推荐目录结构和最小CLI。
5. 建立数据源清单和SHA-256，不解析模型。
6. 编写数据契约和解析测试。
7. 完成异常表、地震目录、断层和底图接入。
8. 输出独立的数据质量报告并人工抽查。
9. 锁定研究区、时间语义、事件去重和切分规则。
10. 写入预登记研究协议并打第一个标签。
11. 建立均匀Poisson和空间变化Poisson基线。
12. 建立并验证ETAS基线。
13. 重建多期异常轨迹和观测覆盖。
14. 训练可解释异常增量模型并做置乱检验。
15. 只有G2、G3通过后才尝试MIL或自监督模型。
16. 加入长期断层先验并做分窗口消融。
17. 建立共享标记震级模型。
18. 完成强度积分收敛审计。
19. 完成面积预算Pareto前沿和区域优化。
20. 冻结操作点并一次性运行锁定测试。
21. 生成最终模型卡、用户说明、静态图和交互图。
22. 启动真正前瞻影子运行并永久归档每一期。

## 41. 建议写入新项目AGENTS.md的规则

```markdown
# SeismoFlux development rules

1. 始终使用中文向用户汇报，代码标识符使用英文。
2. 首要目标是在受控报警面积下提高独立物理地震区域召回。
3. 任何起报日不得使用其后的异常报告、地震或人工结果。
4. 不得使用真实震中生成推理候选、空间加密位置或区域边界。
5. 人工预测地点、震级和时间默认禁止作为训练特征或标签。
6. 动态异常必须由全部历史报告期重建，不能只读取最新一期。
7. 所有异常模型必须与可评价的ETAS背景比较；ETAS不可评价时，与当前最佳合法背景模型比较，并始终做异常置乱对照。
8. 锁定测试只运行一次，失败后不得针对测试集继续调参。
9. 每一阶段必须先完成测试、验收、提交和推送，再进入下一阶段。
10. 不覆盖历史前瞻预测，不根据后来是否命中选择保留版本。
11. 相对强度、评分和顺位不得称为绝对发震概率。
12. 简单模型未通过前不得直接进入大型神经网络。
13. 大型任务使用受控多核，默认至少保留2个物理核心。
14. 每次汇报展示完整工作线、当前阶段、通过门控和下一步。
15. 不复制LocationPred的旧模型和脚本，只迁移原始输入和只读基准。
16. 每个阶段性结果形成后必须立即完成科学价值复审：说明它对最终预测目标是直接提升、必要使能还是没有实质推动，并给出证据；没有实质推动时停止继续堆叠工程，先复审和调整方案。
```

## 42. 参考研究

以下资料用于确定技术方向，不代表其结果可以直接迁移到中国大陆数据：

1. [Incorporating Non-Seismicity Precursors Into Earthquake Probabilistic Forecasting Model](https://agupubs.onlinelibrary.wiley.com/doi/10.1029/2025GL117972)：把异常作为外部激发加入ETAS，并通过随机异常时间检验增量。
2. [A neural encoder for earthquake rate forecasting](https://www.nature.com/articles/s41598-023-38033-9)：在时空点过程框架中学习地震目录表示，并与ETAS比较。
3. [Using Deep Learning for Flexible and Scalable Earthquake Forecasting](https://agupubs.onlinelibrary.wiley.com/doi/10.1029/2023GL103909)：神经时间点过程与ETAS基准及样本规模讨论。
4. [A benchmark database of ten years of prospective next-day earthquake forecasts](https://www.nature.com/articles/s41597-025-05766-3)：长期前瞻冻结、模型比较和数据归档参考。
5. [Earthquake prediction and its optimization](https://agupubs.onlinelibrary.wiley.com/doi/10.1029/91JB03095)：漏报与报警时空体积的优化框架。
6. [Seismically Informed Reference Models Enhance AI-Based Earthquake Prediction Systems](https://agupubs.onlinelibrary.wiley.com/doi/10.1029/2023JB028037)：地震学参考模型和Molchan评估的重要性。
7. [Attention-based Deep Multiple Instance Learning](https://proceedings.mlr.press/v80/ilse18a.html)：包级弱标签和实例贡献注意力。
8. [IMTS is Worth Time × Channel Patches](https://proceedings.mlr.press/v267/hu25y.html)：不规则多变量时间序列的遮挡自监督表示。

## 43. 最终交接结论

SeismoFlux不应被理解为LocationPred的下一版本。它是一个问题定义、模型目标和验证体系均重新设计的新项目。

最优先的科学任务不是立即做复杂神经网络，而是建立可信的地震活动背景强度，然后严格检验动态异常能否解释背景模型未解释的部分。危险区应由连续强度在面积预算下直接优化得到，多个地震通过点过程自然表达，50 km定位放到区域召回稳定之后。

新项目获得更好结果的可能性主要来自三个变化：

1. 拟合目标与真实业务目标一致；
2. 动态异常、地震背景和长期构造各自承担清晰角色；
3. 任何提升都必须超过ETAS、置乱异常和固定面积基线。

这条路线不能保证显著提高地点预测能力，但能够保证最终结论比继续候选点排序更科学、更可审计，也更有机会在受控虚报代价下提高区域召回。
